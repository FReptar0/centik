---
phase: 22-visual-qa-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/dashboard/KPICard.tsx
  - src/components/dashboard/KPIGrid.tsx
  - src/components/dashboard/RecentTransactions.tsx
  - src/components/charts/TrendAreaChart.tsx
  - src/components/charts/ExpenseDonutChart.tsx
  - src/components/charts/BudgetBarChart.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/layout/FAB.tsx
  - src/components/layout/PageHeader.tsx
  - src/components/layout/PeriodSelector.tsx
  - src/components/layout/MobileMoreSheet.tsx
  - src/app/globals.css
autonomous: false
requirements:
  - QA-01
  - QA-07
must_haves:
  truths:
    - "Dashboard hero balance card has visible dot-matrix texture overlay"
    - "All KPI cards show monospaced IBM Plex Mono values with muted $ prefix"
    - "Charts render with no grid lines, 1.5px stroke, and 4px dot endpoints"
    - "Recent transactions display MoneyAmount with color-coded direction"
    - "Bottom navigation shows icons only with 4px chartreuse dot below active icon"
    - "Sidebar uses surface background with accent active state"
    - "FAB is pill-shaped chartreuse with black icon"
    - "Period selector shows StatusDot on current period"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard page with Glyph Finance tokens"
    - path: "src/components/dashboard/KPICard.tsx"
      provides: "KPI card with dot-matrix hero variant and monospaced values"
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Icon-only bottom nav with dot indicator"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Desktop sidebar with Glyph Finance styling"
  key_links:
    - from: "src/components/dashboard/KPICard.tsx"
      to: "src/components/ui/MoneyAmount.tsx"
      via: "MoneyAmount component for monetary display"
      pattern: "MoneyAmount|rawValue|moneyVariant"
    - from: "src/components/layout/MobileNav.tsx"
      to: "src/components/ui/StatusDot.tsx"
      via: "StatusDot for active tab indicator"
      pattern: "StatusDot"
---

<objective>
Audit and fix the Dashboard page and Navigation components against STYLE_GUIDE.md specifications.

Purpose: The Dashboard is the landing page and first impression. Navigation (sidebar, bottom tabs, FAB) is visible on every page. These must be pixel-perfect against the Glyph Finance design system specs.

Output: Dashboard and navigation components that match STYLE_GUIDE.md exactly, with all visual discrepancies fixed.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@STYLE_GUIDE.md
@UX_RULES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard page visual audit and fixes (QA-01)</name>
  <files>
    src/app/page.tsx,
    src/components/dashboard/KPICard.tsx,
    src/components/dashboard/KPIGrid.tsx,
    src/components/dashboard/RecentTransactions.tsx,
    src/components/charts/TrendAreaChart.tsx,
    src/components/charts/ExpenseDonutChart.tsx,
    src/components/charts/BudgetBarChart.tsx,
    src/app/globals.css
  </files>
  <action>
    Start the dev server (`npm run dev`). Read each Dashboard-related component file and compare against STYLE_GUIDE.md specs. Check and fix ALL of the following:

    **Hero KPICard (balance card):**
    - MUST have `dot-matrix-hero` class for texture overlay (STYLE_GUIDE.md > Identidad Visual > Textura Dot-Matrix: hero cards EXCLUSIVELY)
    - Background must be `--color-surface-elevated` (#141414) -- NO borders, NO shadows
    - Padding must be 24px (generous KPI card padding per STYLE_GUIDE.md > Espaciado)
    - Border-radius must be `--radius-lg` (16px)

    **All KPI cards:**
    - Monetary values MUST render through MoneyAmount component with IBM Plex Mono font
    - "$" prefix must be muted in `--color-text-tertiary` at smaller size
    - Values color-coded: positive=#00E676, negative=#FF3333, neutral=#E8E8E8
    - Card surface: `--color-surface-elevated` with NO visible borders, NO shadows
    - Labels should be in Label style (12px, uppercase, letter-spacing +2px, `--color-text-secondary`)

    **Charts (TrendAreaChart, ExpenseDonutChart, BudgetBarChart):**
    - NO CartesianGrid or grid lines anywhere
    - Stroke width exactly 1.5px (not 2px)
    - Dot endpoints: 4px solid circles at data points
    - Area fill: gradient from stroke color at 10-15% opacity at top to 0% at bottom
    - Bar charts: rectangular tops (no border-radius), ~60% bar width
    - Donut: thin ring (innerRadius ~70% of outerRadius)
    - Chart container: `--color-surface-elevated` background, 20px padding, `--radius-lg` border-radius
    - Tooltips: card-style with `--color-surface-elevated` background, `--radius-md`, IBM Plex Mono for amounts
    - Colors must use hardcoded CHART_COLORS constants (Glyph Finance hex values)

    **Recent Transactions:**
    - MoneyAmount rendering with direction colors (green income, red expense)
    - Transaction rows: no borders on cards, 1px `--color-border-divider` (#222222) separator between stacked items
    - Category icons in circular containers with category color at 12% opacity background
    - Dates in Meta style (11px, `--color-text-tertiary`)

    **Dashboard page layout:**
    - Background: `--color-bg` (#000000)
    - Section spacing: 32px between major sections
    - KPI grid: responsive columns (2 on mobile, 3 on tablet, 3-4 on desktop)
    - "VER TODO" link pattern on recent transactions: Accent badge variant with right arrow, chartreuse, uppercase

    For each discrepancy found: fix the code, note what was wrong and what was changed.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build && npm test</automated>
  </verify>
  <done>
    Dashboard page renders with: dot-matrix texture on hero card, monospaced monetary values with muted $ prefix, minimal charts (no grids, 1.5px stroke, 4px dots), color-coded recent transactions. Zero visual discrepancies against STYLE_GUIDE.md. Build and tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Navigation components visual audit and fixes (QA-07)</name>
  <files>
    src/components/layout/Sidebar.tsx,
    src/components/layout/MobileNav.tsx,
    src/components/layout/FAB.tsx,
    src/components/layout/PageHeader.tsx,
    src/components/layout/PeriodSelector.tsx,
    src/components/layout/MobileMoreSheet.tsx
  </files>
  <action>
    Read each navigation component file and compare against STYLE_GUIDE.md and UX_RULES.md specs. Check and fix ALL of the following:

    **Bottom Tab Bar (MobileNav) -- UX_RULES.md 3.2:**
    - Position: fixed bottom, height 64px
    - Background: `--color-surface` (#0A0A0A)
    - 5 items: Inicio, Movimientos, Deudas, Presupuesto, Mas
    - Icons ONLY -- NO text labels. Icons at 20px in `--color-text-secondary` (#999999) for inactive
    - Active indicator: 4px chartreuse dot positioned 8px below the active icon (StatusDot component)
    - Active icon does NOT change color -- only the dot communicates active state
    - Verify StatusDot pulse animation is present (2.5s cycle)

    **Sidebar -- UX_RULES.md 3.1:**
    - Width: 240px on desktop (lg), collapsed on tablet (md)
    - Background: `--color-surface` (#0A0A0A)
    - Active item: `--color-accent` at 15% opacity background + `--color-accent` text
    - Inactive items: `--color-text-secondary` (#999999), hover changes to `--color-text-primary`
    - Label visible on each item (not icon-only on desktop)
    - StatusDot on current period indicator within sidebar

    **FAB:**
    - Pill-shaped (border-radius: 9999px) -- verify it is `--radius-full`
    - Background: `--color-accent` (#CCFF00)
    - Icon: black (#000000)
    - Size: 48px circular
    - Position: floating above center of tab bar on mobile
    - Scale press interaction: `transform: scale(0.98)` on active

    **PageHeader:**
    - Title in Heading level (20px, Satoshi Semibold, `--color-text-primary`)
    - Subtitle with period in `--color-text-secondary`
    - Action button on the right

    **PeriodSelector:**
    - Current period has StatusDot (pulsing 2.5s) next to name
    - Closed periods show lock icon
    - Period text in proper typography level

    **MobileMoreSheet:**
    - Opens as bottom sheet (85vh per modal spec)
    - Items show icon (20px) + label in Body level (14px, `--color-text-primary`)
    - Drag handle: 4px height, 40px width, `--color-border-divider`, centered with 12px top margin

    For each discrepancy found: fix the code, note what was wrong and what was changed.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build && npm test</automated>
  </verify>
  <done>
    Navigation components match specs: icon-only bottom nav with dot indicator, sidebar with proper active states and StatusDot, pill FAB in chartreuse, page headers with correct typography, period selector with StatusDot on current period. Build and tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Dashboard and Navigation</name>
  <action>
    Human verifies Dashboard page and navigation components match STYLE_GUIDE.md specifications.

    1. Open http://localhost:3000 in browser
    2. Dashboard checks:
       - Hero balance card has subtle dot-matrix texture visible on close inspection
       - KPI values show in IBM Plex Mono with small muted "$" prefix
       - Charts have no grid lines, thin strokes, small dot endpoints
       - Recent transactions show colored amounts (green for income, red for expense)
    3. Navigation checks (resize browser for each):
       - Mobile (less than 768px): Bottom tabs show icons only, active tab has chartreuse dot below, FAB floats above center
       - Desktop (1024px+): Sidebar visible with text labels, active item highlighted in chartreuse
       - Period selector shows pulsing chartreuse dot next to current period
    4. Verify no shadows visible anywhere, no borders on cards (only separators between stacked items)
  </action>
  <verify>Visual inspection confirms all Dashboard and Navigation elements match STYLE_GUIDE.md</verify>
  <done>User approves Dashboard and Navigation visual fidelity, or provides list of remaining issues to fix</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors and zero warnings
- `npm test` passes with zero failures
- Dashboard visually matches STYLE_GUIDE.md hero card, KPI, chart, and transaction specs
- Navigation visually matches UX_RULES.md sections 3.1, 3.2, 3.3, 3.4
</verification>

<success_criteria>
- Dashboard hero card shows dot-matrix texture
- All monetary values use MoneyAmount with IBM Plex Mono and muted $ prefix
- Charts are minimal (no grids, thin strokes, dot endpoints)
- Bottom nav is icon-only with dot indicator
- Sidebar has correct active states
- FAB is pill-shaped chartreuse
- Period selector has StatusDot on current period
- Zero visual discrepancies remain against spec documents
</success_criteria>

<output>
After completion, create `.planning/phases/22-visual-qa-accessibility/22-01-SUMMARY.md`
</output>
