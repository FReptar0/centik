---
phase: 30-vercel-deploy-security-hardening
plan: 05
type: execute
wave: 2
depends_on: [30-02]
files_modified:
  - tests/integration/isolation.test.ts
  - tests/integration/isolation-actions.test.ts
autonomous: true
requirements: [ISOL-05, TEST-03]
tags: [integration-tests, isolation, idor, server-actions, cross-user]
must_haves:
  truths:
    - "tests/integration/isolation.test.ts is extended with 5 new it() blocks covering MonthlySummary + Asset + ValueUnit + UnitRate + BackupCode cross-user READ isolation (one per entity since each has a userId FK per schema L267-362)"
    - "The extended beforeAll() seeds representative data for User A in each of the 5 new entities; the extended afterAll() cleans them in FK-safe order BEFORE user.deleteMany"
    - "All pre-existing 7 isolation.test.ts tests (transactions, categories, income sources, debts, budgets, periods, User A sees own) continue to pass unchanged"
    - "tests/integration/isolation-actions.test.ts (NEW) covers ≥10 mutation Server Actions with User A authenticated and targeting User B-owned row IDs: expect error return OR silent no-op; NEVER successful mutation"
    - "The new file covers: createTransaction/updateTransaction/deleteTransaction, createDebt/updateDebt/updateDebtBalance/deleteDebt, upsertBudgets, createIncomeSource/updateIncomeSource/deleteIncomeSource, createCategory/deleteCategory, closePeriod/reopenPeriod, disableTotpAction, regenerateBackupCodesAction — totaling ≥16 test cases"
    - "Every test verifies DB state for User B's row post-action is UNCHANGED (amount, userId ownership, etc.)"
    - "The new file's mock block (next-auth + @auth/prisma-adapter + next-auth/providers/credentials + @upstash/ratelimit + @upstash/redis) mirrors tests/integration/totp.test.ts L5-42 verbatim"
    - "requireAuth is mocked per-test to return { userId: userAId } so authenticated calls to Server Actions carry User A's session"
  artifacts:
    - path: "tests/integration/isolation.test.ts"
      provides: "Extended 2-user isolation test covering 12 entity read paths"
      min_lines: 260
      contains: "MonthlySummary"
    - path: "tests/integration/isolation-actions.test.ts"
      provides: "Server-Action-level cross-user IDOR regression net"
      min_lines: 250
      contains: "mockRequireAuth"
  key_links:
    - from: "tests/integration/isolation.test.ts"
      to: "Prisma read paths"
      via: "prisma.monthlySummary.findMany({ where: { userId: userBId } })"
      pattern: "findMany.*userId: userBId"
    - from: "tests/integration/isolation-actions.test.ts"
      to: "mutation Server Actions"
      via: "call action with User B's row ID; verify DB unchanged"
      pattern: "mockRequireAuth"
---

<objective>
Deliver ISOL-05 and TEST-03: extend the existing cross-user isolation test file with 5 new entity-level READ tests (MonthlySummary, Asset, ValueUnit, UnitRate, BackupCode — all have userId FK per schema inspection), AND create a NEW file proving the Server-Action-level IDOR defense (Phase 27 `findFirst({ where: { id, userId } })` pattern) holds for every mutation path. Together these lock in the per-user discipline accumulated across Phases 27-29 as a permanent regression net.

Purpose: D-22 and D-23 together close the gap Phase 27 left open — the READ surface was covered, but the MUTATION surface (Server Actions receiving a row ID and updating/deleting it) was only spot-checked. Phase 30 is the audit-and-lock phase for that gap. Every mutation Server Action across 6 action files gets a cross-user test case.

Output: `tests/integration/isolation.test.ts` extended (+5 describe/it blocks + expanded seed + cleanup) + `tests/integration/isolation-actions.test.ts` NEW (~300 lines covering 16+ mutation actions). Both run via `npm run test:integration`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-vercel-deploy-security-hardening/30-CONTEXT.md
@.planning/phases/30-vercel-deploy-security-hardening/30-RESEARCH.md
@.planning/phases/30-vercel-deploy-security-hardening/30-PATTERNS.md
@.planning/phases/30-vercel-deploy-security-hardening/30-VALIDATION.md
@.planning/phases/27-per-user-data-isolation/27-CONTEXT.md

<interfaces>
<!-- Extracted -- use verbatim. -->

## Schema userId audit (from prisma/schema.prisma inspection)
All 5 new entities to cover HAVE a `userId` field with `onDelete: Cascade`:
- `MonthlySummary` L278: `userId String` + `@@index([userId])` + relation to User
- `ValueUnit` L302: `userId String` + `@@index([userId])` + relation (onDelete: Cascade)
- `UnitRate` L320: `userId String` + `@@index([userId])` + relation (onDelete: Cascade)
- `Asset` L342: `userId String` + `@@index([userId])` + relation (onDelete: Cascade)
- `BackupCode` L354: `userId String` + `@@index([userId])` + relation (onDelete: Cascade)

PATTERNS.md L415 guessed ValueUnit/UnitRate did NOT have userId — that was wrong. All 5 are user-scoped and must be tested.

## Server Actions inventory (17 actions across 6 files)
From the codebase:
- `src/app/(app)/movimientos/actions.ts`:
  - `createTransaction(data)` (L53)
  - `updateTransaction(id, data)` (L95)
  - `deleteTransaction(id)` (L150)
- `src/app/(app)/deudas/actions.ts`:
  - `createDebt(data)` (L29)
  - `updateDebt(id, data)` (L73)
  - `updateDebtBalance(id, data)` (L124)
  - `deleteDebt(id)` (L156)
- `src/app/(app)/presupuesto/actions.ts`:
  - `upsertBudgets(periodId, data)` (L21)
- `src/app/(app)/historial/actions.ts`:
  - `closePeriod(periodId)` (L23)
  - `reopenPeriod(periodId)` (L140)
  - `getClosePeriodPreviewAction(periodId)` (L176) — READ, skip per D-23 mutation-only scope
- `src/app/(app)/ingresos/actions.ts`:
  - `createIncomeSource(data)` (L28)
  - `updateIncomeSource(id, data)` (L65)
  - `deleteIncomeSource(id)` (L108)
- `src/app/(app)/configuracion/actions.ts`:
  - `createCategory(data)` (L29)
  - `deleteCategory(id)` (L75)
- `src/app/(app)/configuracion/totp-actions.ts`:
  - `prepareTotpSecretAction()` (L29) — SESSION-SCOPED (no row ID param); skip — there's no cross-user surface
  - `enableTotpAction(...)` (L60) — SESSION-SCOPED; skip
  - `disableTotpAction(...)` (L108) — SESSION-SCOPED but CRITICAL — verify it only disables CURRENT user (mock session as A; confirm User B's TOTP unchanged)
  - `regenerateBackupCodesAction(...)` (L140) — SESSION-SCOPED; verify it only touches current session's userId

Target coverage in isolation-actions.test.ts: the 14 row-id-accepting mutations above PLUS 2 session-scoped TOTP verifications = 16 test cases total (≥10 per D-26).

## Existing isolation.test.ts L1-214 — 2-user setup + 7 tests
```
// Mock block L5-20 — COPY verbatim into isolation-actions.test.ts
vi.mock('next-auth', () => ({ default: () => ({ handlers: {}, signIn: vi.fn(), signOut: vi.fn(), auth: vi.fn() }) }))
vi.mock('@auth/prisma-adapter', () => ({ PrismaAdapter: vi.fn() }))
vi.mock('next-auth/providers/credentials', () => ({ default: vi.fn(() => ({})) }))
```

beforeAll seeds User A + User B (both approved, both totpEnabled: false) + User A's Period/Categories/IncomeSource/Transactions/Debt/Budget. afterAll deletes in FK order: transaction -> budget -> monthlySummary -> debt -> incomeSource -> category -> period -> user.

## Phase 29 totp.test.ts mock block L5-42 (verbatim copy for isolation-actions.test.ts)
```
vi.mock('next-auth', () => ({
  default: () => ({ handlers: {}, signIn: vi.fn(), signOut: vi.fn(), auth: vi.fn() }),
}))
vi.mock('@auth/prisma-adapter', () => ({ PrismaAdapter: vi.fn() }))
vi.mock('next-auth/providers/credentials', () => ({ default: vi.fn(() => ({})) }))
vi.mock('@upstash/ratelimit', () => ({
  Ratelimit: Object.assign(
    vi.fn().mockImplementation(() => ({
      limit: vi.fn().mockResolvedValue({ success: true, limit: 5, remaining: 4, reset: Date.now() + 60000, pending: Promise.resolve() }),
    })),
    { slidingWindow: vi.fn(), fixedWindow: vi.fn() },
  ),
}))
vi.mock('@upstash/redis', () => ({ Redis: { fromEnv: vi.fn(() => ({})) } }))
```

## requireAuth mock pattern (D-23 key trick)
```
const mockRequireAuth = vi.fn()
vi.mock('@/lib/auth-utils', () => ({ requireAuth: () => mockRequireAuth() }))
```
Per-test: `mockRequireAuth.mockResolvedValue({ userId: userAId })`. Then call the action. It will look up `findFirst({ where: { id: userBRowId, userId: userAId } })` which returns null, triggering the "not found" branch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/integration/isolation.test.ts with 5 new entity read-isolation tests</name>
  <read_first>
    tests/integration/isolation.test.ts L1-214 (full file — extend in place).
    30-PATTERNS.md §tests/integration/isolation.test.ts (MODIFY) L397-417.
    30-CONTEXT.md D-22, D-25, D-26.
    prisma/schema.prisma L267-362 (MonthlySummary, ValueUnit, UnitRate, Asset, BackupCode — verify userId fields).
    30-VALIDATION.md Task 30-05-01.
  </read_first>
  <files>tests/integration/isolation.test.ts</files>
  <action>
    Edit `tests/integration/isolation.test.ts`. DO NOT delete any existing content. Apply three additive changes:

    ## Change 1 — Extend beforeAll() with 5 new seed blocks for User A

    After the existing `await prisma.budget.create({...})` (around L135), but still inside the `beforeAll`, add:

    ```
    // --- Phase 30 D-22 extensions ---

    // MonthlySummary (requires the existing Period)
    await prisma.monthlySummary.create({
      data: {
        periodId: period.id,
        totalIncome: BigInt(5000000),
        totalExpenses: BigInt(100000),
        totalSavings: BigInt(4900000),
        savingsRate: 9800, // 98.00% in basis points
        debtAtClose: BigInt(1000000),
        debtPayments: BigInt(0),
        userId: userAId,
      },
    })

    // ValueUnit (global-ish, but still userId-scoped per schema)
    const valueUnit = await prisma.valueUnit.create({
      data: {
        code: `ISO-${Date.now()}`,
        name: 'Isolation Test Unit',
        precision: 2,
        isActive: true,
        userId: userAId,
      },
    })

    // UnitRate for that ValueUnit
    await prisma.unitRate.create({
      data: {
        unitId: valueUnit.id,
        date: new Date('2099-01-15'),
        rateToMxnCents: BigInt(100),
        rateRaw: '1.00',
        source: 'isolation-test',
        userId: userAId,
      },
    })

    // Asset linked to the ValueUnit
    await prisma.asset.create({
      data: {
        name: 'Isolation Test Asset',
        unitId: valueUnit.id,
        amount: BigInt(100000),
        category: 'CASH',
        isActive: true,
        userId: userAId,
      },
    })

    // BackupCode — direct insert (mocks a post-2FA-enable state without running the real flow)
    await prisma.backupCode.create({
      data: {
        userId: userAId,
        codeHash: '$2a$12$fake.hash.for.isolation.test',
      },
    })
    ```

    Confirm the `AssetCategory` enum value `CASH` exists. If the schema enum uses a different name (inspect `prisma/schema.prisma` near `enum AssetCategory` — expected values include CASH, INVESTMENT, RETIREMENT, or similar), substitute the correct enum member.

    ## Change 2 — Extend afterAll() cleanup IN FK-SAFE ORDER

    Currently afterAll deletes in this order: transaction -> budget -> monthlySummary -> debt -> incomeSource -> category -> period -> user. Insert three new cleanup lines BEFORE `period.deleteMany` (because Period references MonthlySummary which is already deleted there) and AFTER the transaction/budget/monthlySummary chain:

    ```
    // --- Phase 30 D-22 cleanup ---
    await prisma.backupCode.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
    await prisma.asset.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
    await prisma.unitRate.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
    await prisma.valueUnit.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
    ```

    Place these lines AFTER `await prisma.monthlySummary.deleteMany({...})` and BEFORE `await prisma.debt.deleteMany({...})`. The order is FK-safe: Asset references ValueUnit, UnitRate references ValueUnit, so ValueUnit deletes last of those four. MonthlySummary must already be cleared before Period (existing behavior).

    ## Change 3 — Append 5 new `it()` tests at the end of the describe block

    After the existing `it('User A sees their own transactions', ...)` (around L208-213), add 5 more tests still inside the same `describe('Cross-user data isolation', ...)` block:

    ```
    // --- Phase 30 D-22 new entity isolation tests ---

    it('User B sees zero monthly summaries', async () => {
      const summaries = await prisma.monthlySummary.findMany({
        where: { userId: userBId },
      })
      expect(summaries).toHaveLength(0)
    })

    it('User B sees zero assets', async () => {
      const assets = await prisma.asset.findMany({ where: { userId: userBId } })
      expect(assets).toHaveLength(0)
    })

    it('User B sees zero value units', async () => {
      const units = await prisma.valueUnit.findMany({ where: { userId: userBId } })
      expect(units).toHaveLength(0)
    })

    it('User B sees zero unit rates', async () => {
      const rates = await prisma.unitRate.findMany({ where: { userId: userBId } })
      expect(rates).toHaveLength(0)
    })

    it('User B sees zero backup codes', async () => {
      const codes = await prisma.backupCode.findMany({ where: { userId: userBId } })
      expect(codes).toHaveLength(0)
    })
    ```

    Line count target for the extended file: ~280 lines (up from 214). Must stay under 300 — if approaching the cap, the planner is fine with splitting into `isolation.test.ts` + `isolation-extended.test.ts` but prefer inline extension first.
  </action>
  <verify>
    <automated>npm run test:integration -- tests/integration/isolation.test.ts 2>&1 | tail -20 | grep -E "12 passed|Tests\s+12" || npm run test:integration -- tests/integration/isolation.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>tests/integration/isolation.test.ts has 12 passing `it()` blocks (7 existing + 5 new); beforeAll seeds MonthlySummary + Asset + ValueUnit + UnitRate + BackupCode for User A; afterAll cleans them in FK-safe order; `npm run test:integration -- tests/integration/isolation.test.ts` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/integration/isolation-actions.test.ts with 16+ Server-Action IDOR tests</name>
  <read_first>
    tests/integration/totp.test.ts L1-80 (mock boilerplate + two-user setup).
    tests/integration/isolation.test.ts L1-214 (pattern reference for 2-user setup).
    30-PATTERNS.md §tests/integration/isolation-actions.test.ts (NEW) L151-205.
    30-RESEARCH.md §Code Excerpts L734-837 (canonical structure).
    30-CONTEXT.md D-23, D-24, D-25, D-26.
    src/app/(app)/movimientos/actions.ts (full file for exact signatures + revalidation behavior).
    src/app/(app)/deudas/actions.ts L29, L73, L124, L156 (createDebt/updateDebt/updateDebtBalance/deleteDebt signatures).
    src/app/(app)/presupuesto/actions.ts L21 (upsertBudgets).
    src/app/(app)/historial/actions.ts L23, L140 (closePeriod, reopenPeriod).
    src/app/(app)/ingresos/actions.ts L28, L65, L108.
    src/app/(app)/configuracion/actions.ts L29, L75.
    src/app/(app)/configuracion/totp-actions.ts L108, L140 (disableTotpAction, regenerateBackupCodesAction).
    src/lib/validators.ts (for required field shapes in createTransaction/createDebt/etc.).
  </read_first>
  <files>tests/integration/isolation-actions.test.ts</files>
  <action>
    Create `tests/integration/isolation-actions.test.ts` with the following structure. This is a large file — aim for 280-300 lines. If it would exceed 300 lines, split into `isolation-actions.test.ts` (entity CRUD mutations) + `isolation-actions-totp.test.ts` (session-scoped TOTP mutations).

    ## File skeleton

    ```
    // tests/integration/isolation-actions.test.ts — Phase 30 D-23.
    // Mocks auth() / requireAuth() to return User A's session. Calls each mutation Server Action
    // with a row ID owned by User B. Every action MUST return an error OR silently no-op; NEVER
    // succeed. Post-action DB state for User B's row MUST be unchanged.
    import { describe, it, expect, beforeAll, afterAll, beforeEach, vi } from 'vitest'
    import { prisma } from '../setup'

    // Mock block — verbatim copy from tests/integration/totp.test.ts L5-42
    vi.mock('next-auth', () => ({
      default: () => ({ handlers: {}, signIn: vi.fn(), signOut: vi.fn(), auth: vi.fn() }),
    }))
    vi.mock('@auth/prisma-adapter', () => ({ PrismaAdapter: vi.fn() }))
    vi.mock('next-auth/providers/credentials', () => ({ default: vi.fn(() => ({})) }))
    vi.mock('@upstash/ratelimit', () => ({
      Ratelimit: Object.assign(
        vi.fn().mockImplementation(() => ({
          limit: vi.fn().mockResolvedValue({ success: true, limit: 5, remaining: 4, reset: Date.now() + 60000, pending: Promise.resolve() }),
        })),
        { slidingWindow: vi.fn(), fixedWindow: vi.fn() },
      ),
    }))
    vi.mock('@upstash/redis', () => ({ Redis: { fromEnv: vi.fn(() => ({})) } }))

    // D-23 — requireAuth returns User A's session per test
    const mockRequireAuth = vi.fn()
    vi.mock('@/lib/auth-utils', () => ({ requireAuth: () => mockRequireAuth() }))

    // Real action imports (after mocks)
    import { createTransaction, updateTransaction, deleteTransaction } from '@/app/(app)/movimientos/actions'
    import { createDebt, updateDebt, updateDebtBalance, deleteDebt } from '@/app/(app)/deudas/actions'
    import { upsertBudgets } from '@/app/(app)/presupuesto/actions'
    import { closePeriod, reopenPeriod } from '@/app/(app)/historial/actions'
    import { createIncomeSource, updateIncomeSource, deleteIncomeSource } from '@/app/(app)/ingresos/actions'
    import { createCategory, deleteCategory } from '@/app/(app)/configuracion/actions'
    import { disableTotpAction, regenerateBackupCodesAction } from '@/app/(app)/configuracion/totp-actions'

    // Test state
    let userAId: string
    let userBId: string
    let userBTransactionId: string
    let userBDebtId: string
    let userBPeriodId: string
    let userBCategoryId: string
    let userBIncomeSourceId: string
    let userBBudgetId: string

    beforeAll(async () => {
      // Fresh users with time-suffixed emails to avoid test-run collisions
      const t = Date.now()
      const a = await prisma.user.create({
        data: { email: `iso-action-a-${t}@test.com`, hashedPassword: 'x', isApproved: true, totpEnabled: false },
      })
      userAId = a.id
      const b = await prisma.user.create({
        data: {
          email: `iso-action-b-${t}@test.com`,
          hashedPassword: 'x',
          isApproved: true,
          // For TOTP tests, User B needs totpEnabled: true + totpSecret set
          totpEnabled: true,
          totpSecret: 'aa:bb:cc', // placeholder shape; disableTotpAction never decrypts inside this test
        },
      })
      userBId = b.id

      // Seed one row per entity owned by User B
      const period = await prisma.period.create({
        data: {
          month: 6, year: 2099,
          startDate: new Date('2099-06-01'), endDate: new Date('2099-06-30'),
          userId: userBId,
        },
      })
      userBPeriodId = period.id

      const category = await prisma.category.create({
        data: { name: `iso-cat-b-${t}`, icon: 'x', color: '#fff', type: 'EXPENSE', userId: userBId },
      })
      userBCategoryId = category.id

      const txn = await prisma.transaction.create({
        data: {
          type: 'EXPENSE',
          amount: BigInt(500000),
          categoryId: category.id,
          date: new Date('2099-06-15'),
          periodId: period.id,
          userId: userBId,
        },
      })
      userBTransactionId = txn.id

      const debt = await prisma.debt.create({
        data: {
          name: `iso-debt-b-${t}`,
          type: 'PERSONAL_LOAN',
          currentBalance: BigInt(2000000),
          annualRate: 1800,
          userId: userBId,
        },
      })
      userBDebtId = debt.id

      const income = await prisma.incomeSource.create({
        data: {
          name: `iso-income-b-${t}`,
          defaultAmount: BigInt(3000000),
          frequency: 'QUINCENAL',
          type: 'EMPLOYMENT',
          userId: userBId,
        },
      })
      userBIncomeSourceId = income.id

      const budget = await prisma.budget.create({
        data: { categoryId: category.id, quincenalAmount: BigInt(400000), periodId: period.id, userId: userBId },
      })
      userBBudgetId = budget.id

      // User B also gets 2 backup codes so regenerate tests have something to leave alone
      await prisma.backupCode.createMany({
        data: [
          { userId: userBId, codeHash: '$2a$12$fake.b.1' },
          { userId: userBId, codeHash: '$2a$12$fake.b.2' },
        ],
      })
    }, 30000)

    afterAll(async () => {
      await prisma.backupCode.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
      await prisma.transaction.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
      await prisma.budget.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
      await prisma.monthlySummary.deleteMany({ where: { period: { userId: { in: [userAId, userBId] } } } })
      await prisma.debt.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
      await prisma.incomeSource.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
      await prisma.category.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
      await prisma.period.deleteMany({ where: { userId: { in: [userAId, userBId] } } })
      await prisma.user.deleteMany({ where: { id: { in: [userAId, userBId] } } })
    })

    beforeEach(() => {
      mockRequireAuth.mockResolvedValue({ userId: userAId })
    })

    // --- Transaction mutations ---
    describe('cross-user IDOR — transactions', () => {
      it('updateTransaction: User A cannot modify User B transaction', async () => {
        const result = await updateTransaction(userBTransactionId, {
          type: 'EXPENSE',
          amount: '999999',
          categoryId: userBCategoryId,
          date: '2099-06-15',
        })
        // Expect error OR a _form error shape ("Transaccion no encontrada")
        expect('error' in result || 'success' in result).toBe(true)
        if ('success' in result) {
          // Fail — mutation never allowed
          throw new Error('updateTransaction should not succeed on User B row with User A session')
        }

        const after = await prisma.transaction.findUnique({ where: { id: userBTransactionId } })
        expect(after?.amount).toBe(BigInt(500000))
        expect(after?.userId).toBe(userBId)
      })

      it('deleteTransaction: User A cannot delete User B transaction', async () => {
        const result = await deleteTransaction(userBTransactionId)
        if ('success' in result) {
          throw new Error('deleteTransaction should not succeed on User B row')
        }
        const after = await prisma.transaction.findUnique({ where: { id: userBTransactionId } })
        expect(after).not.toBeNull()
        expect(after!.userId).toBe(userBId)
      })
    })

    // --- Debt mutations ---
    describe('cross-user IDOR — debts', () => {
      it('updateDebt: User A cannot modify User B debt', async () => {
        const result = await updateDebt(userBDebtId, {
          name: 'attacker-rename',
          type: 'PERSONAL_LOAN',
          currentBalance: '1',
          annualRate: 0,
        })
        if ('success' in result) throw new Error('updateDebt should not succeed')
        const after = await prisma.debt.findUnique({ where: { id: userBDebtId } })
        expect(after?.name).not.toBe('attacker-rename')
        expect(after?.userId).toBe(userBId)
      })

      it('updateDebtBalance: User A cannot modify User B debt balance', async () => {
        const result = await updateDebtBalance(userBDebtId, { currentBalance: '1' })
        if ('success' in result) throw new Error('updateDebtBalance should not succeed')
        const after = await prisma.debt.findUnique({ where: { id: userBDebtId } })
        expect(after?.currentBalance).toBe(BigInt(2000000))
      })

      it('deleteDebt: User A cannot delete User B debt', async () => {
        const result = await deleteDebt(userBDebtId)
        if ('success' in result) throw new Error('deleteDebt should not succeed')
        const after = await prisma.debt.findUnique({ where: { id: userBDebtId } })
        expect(after).not.toBeNull()
      })
    })

    // --- Budget mutations ---
    describe('cross-user IDOR — budgets', () => {
      it('upsertBudgets: User A cannot write budgets against User B period', async () => {
        const result = await upsertBudgets(userBPeriodId, {
          entries: [{ categoryId: userBCategoryId, quincenalAmount: '999' }],
        })
        if ('success' in result) throw new Error('upsertBudgets should not succeed on User B period')
        const after = await prisma.budget.findUnique({ where: { id: userBBudgetId } })
        expect(after?.quincenalAmount).toBe(BigInt(400000))
        expect(after?.userId).toBe(userBId)
      })
    })

    // --- Period mutations (history) ---
    describe('cross-user IDOR — periods', () => {
      it('closePeriod: User A cannot close User B period', async () => {
        const result = await closePeriod(userBPeriodId)
        if ('success' in result) throw new Error('closePeriod should not succeed')
        const after = await prisma.period.findUnique({ where: { id: userBPeriodId } })
        expect(after?.isClosed).toBe(false)
      })

      it('reopenPeriod: User A cannot reopen User B period', async () => {
        // Make the period closed first via DB to set up the scenario
        await prisma.period.update({ where: { id: userBPeriodId }, data: { isClosed: true, closedAt: new Date() } })
        const result = await reopenPeriod(userBPeriodId)
        if ('success' in result) throw new Error('reopenPeriod should not succeed')
        const after = await prisma.period.findUnique({ where: { id: userBPeriodId } })
        expect(after?.isClosed).toBe(true) // still closed
        // Reset for downstream tests
        await prisma.period.update({ where: { id: userBPeriodId }, data: { isClosed: false, closedAt: null } })
      })
    })

    // --- IncomeSource mutations ---
    describe('cross-user IDOR — income sources', () => {
      it('updateIncomeSource: User A cannot modify User B income source', async () => {
        const result = await updateIncomeSource(userBIncomeSourceId, {
          name: 'attacker-rename',
          defaultAmount: '1',
          frequency: 'QUINCENAL',
          type: 'EMPLOYMENT',
        })
        if ('success' in result) throw new Error('updateIncomeSource should not succeed')
        const after = await prisma.incomeSource.findUnique({ where: { id: userBIncomeSourceId } })
        expect(after?.name).not.toBe('attacker-rename')
      })

      it('deleteIncomeSource: User A cannot delete User B income source', async () => {
        const result = await deleteIncomeSource(userBIncomeSourceId)
        if ('success' in result) throw new Error('deleteIncomeSource should not succeed')
        const after = await prisma.incomeSource.findUnique({ where: { id: userBIncomeSourceId } })
        expect(after).not.toBeNull()
      })
    })

    // --- Category mutations ---
    describe('cross-user IDOR — categories', () => {
      it('deleteCategory: User A cannot delete User B category', async () => {
        const result = await deleteCategory(userBCategoryId)
        if ('success' in result) throw new Error('deleteCategory should not succeed')
        const after = await prisma.category.findUnique({ where: { id: userBCategoryId } })
        expect(after).not.toBeNull()
      })
    })

    // --- TOTP mutations (session-scoped — verify no cross-user touch) ---
    describe('cross-user IDOR — TOTP actions are session-bound', () => {
      it('disableTotpAction: User A session cannot disable User B 2FA', async () => {
        // User A session has no totpEnabled + no totpSecret, so the action must operate against A's row only.
        // Even with User A authenticated, User B's totpEnabled/totpSecret must remain untouched.
        // The action is called through its useActionState signature; we pass minimal FormData.
        const formData = new FormData()
        formData.set('currentCode', '123456')
        try {
          await disableTotpAction({ success: false }, formData)
        } catch {
          // Action may throw if schema rejects or auth fails; the point is User B's state.
        }
        const userB = await prisma.user.findUnique({
          where: { id: userBId },
          select: { totpEnabled: true, totpSecret: true },
        })
        expect(userB?.totpEnabled).toBe(true)
        expect(userB?.totpSecret).toBe('aa:bb:cc') // unchanged placeholder
      })

      it('regenerateBackupCodesAction: User A session cannot touch User B backup codes', async () => {
        const beforeCodes = await prisma.backupCode.findMany({ where: { userId: userBId } })
        const formData = new FormData()
        formData.set('currentCode', '123456')
        try {
          await regenerateBackupCodesAction({ success: false }, formData)
        } catch {
          // Action may throw; verify User B state below regardless.
        }
        const afterCodes = await prisma.backupCode.findMany({ where: { userId: userBId } })
        expect(afterCodes).toHaveLength(beforeCodes.length)
        expect(afterCodes.map((c) => c.codeHash).sort()).toEqual(beforeCodes.map((c) => c.codeHash).sort())
      })
    })
    ```

    ## Notes / constraints
    - Total `it()` count: 12 test cases covering: updateTransaction, deleteTransaction, updateDebt, updateDebtBalance, deleteDebt, upsertBudgets, closePeriod, reopenPeriod, updateIncomeSource, deleteIncomeSource, deleteCategory, disableTotpAction, regenerateBackupCodesAction = 13. Add 2 more to reach ≥16 per D-26:
      - `createTransaction: User A creating a transaction against User B's categoryId fails with validation or 404` (User A's session creates, but categoryId is User B's — Phase 27 pattern rejects via findFirst where categoryId + userId)
      - `createDebt: succeeds for User A (sanity — session-scoped create is safe)` — this is a negative control confirming the `.create` doesn't accidentally accept a userId override.
    - If implementing the `createTransaction` test: call `createTransaction({ type: 'EXPENSE', amount: '100', categoryId: userBCategoryId, date: '2099-06-16' })` with User A authenticated. Expected outcome: either an error (if `createTransaction` validates category ownership — likely) OR a successful creation that belongs to User A (not User B). Verify by querying `prisma.transaction.findMany({ where: { userId: userAId } })` count after. This tests the create-side IDOR.
    - If the signatures of any action differ from the interfaces above (e.g., `upsertBudgets(periodId, data)` expects `data: { entries: [...] }` — verify via `src/app/(app)/presupuesto/actions.ts` L21-70), adjust the call payload. Do NOT guess — read the action file and match the Zod schema.
    - Do NOT use `createCategory` in the test file — `createCategory` does not accept a target row ID; it's a pure session-scoped create. Not in D-23 scope.
    - Each `if ('success' in result) throw` is load-bearing: Vitest stops the test on throw, making the "never succeed" expectation explicit.
    - Integration test runner already uses a real test DB on port 5433 via `vitest.integration.config.mts` — no additional config needed.

    ## File size management
    If the file approaches 300 lines, split the TOTP describe block into `tests/integration/isolation-actions-totp.test.ts` using the same mock block. Update `files_modified` in SUMMARY if you split.
  </action>
  <verify>
    <automated>test -f tests/integration/isolation-actions.test.ts && COUNT=$(grep -c "^\s*it(" tests/integration/isolation-actions.test.ts) && test "$COUNT" -ge 10 && npm run test:integration -- tests/integration/isolation-actions.test.ts 2>&1 | tail -20 | grep -E "passed"</automated>
  </verify>
  <done>`tests/integration/isolation-actions.test.ts` exists with ≥10 `it()` blocks covering ≥6 mutation Server Actions; `npm run test:integration -- tests/integration/isolation-actions.test.ts` exits 0; every test verifies User B's row is unchanged post-action; mock block for next-auth + Upstash is verbatim from totp.test.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated user session -> Server Action | Action receives row ID from form submission; must verify ownership via findFirst({ where: { id, userId } }) |
| Server Action -> Prisma mutation | Phase 27 IDOR pattern gates every update/delete |
| Test harness -> real DB | Integration runner uses test DB on port 5433; tests prove the IDOR pattern under real Prisma query resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-IDOR-001 | Information Disclosure | Prisma read paths on MonthlySummary / Asset / ValueUnit / UnitRate / BackupCode | mitigate | Task 1 adds 5 `it(User B sees zero X)` tests that would FAIL if a future lib function forgets `where: { userId }` on any of these 5 entities. Regression net against "Phase 27 discipline broke somewhere" class of bug. ValueUnit/UnitRate are user-scoped per schema inspection (correcting PATTERNS.md §`isolation.test.ts (MODIFY)` L415 which assumed otherwise). |
| T-30-IDOR-002 | Elevation of Privilege | Mutation Server Actions across 6 files | mitigate | Task 2's new test file drives 14+ mutation actions as User A authenticated, passing User B's row IDs. Every action MUST either error-return or silent-no-op; NEVER successful mutation. Each test reads the User B row post-action and asserts bytes-identical state. TOTP disable/regen are additionally tested — those actions use the session's userId directly (no row ID param), but the tests prove they cannot reach across to User B's row via session confusion. Coverage: ≥10 actions per D-26, actual 13-16 depending on create-side tests. |
</threat_model>

<verification>
Integration test runner:
```
npm run test:integration
```

Specific gates:
- [ ] `npm run test:integration -- tests/integration/isolation.test.ts` — 12 tests pass (7 existing + 5 new)
- [ ] `npm run test:integration -- tests/integration/isolation-actions.test.ts` — ≥10 tests pass
- [ ] Full integration run exits 0 — no regression in prior tests (totp.test.ts, auth.test.ts, etc.)
- [ ] `npm run build` exits 0 (TypeScript coverage across new imports)
- [ ] `npm run lint` exits 0

## Manual safety
Run the integration suite twice back-to-back — seeds use `Date.now()` suffixes for email uniqueness, so re-runs do not collide. `beforeAll` and `afterAll` fully manage the 2-user lifecycle per-file.
</verification>

<success_criteria>
- `tests/integration/isolation.test.ts` has 12 total passing `it()` blocks (7 existing + 5 new: MonthlySummary, Asset, ValueUnit, UnitRate, BackupCode)
- `tests/integration/isolation-actions.test.ts` exists with ≥10 `it()` blocks covering ≥6 mutation Server Action paths
- Every mutation test verifies User B's row is unchanged post-action
- TOTP session-scoped tests (`disableTotpAction`, `regenerateBackupCodesAction`) prove User A's session cannot touch User B's totpEnabled or backup codes
- `npm run test:integration` exits 0 across the full suite
- File sizes under 300 lines (split if necessary; document split in SUMMARY)
</success_criteria>

<output>
After completion, create `.planning/phases/30-vercel-deploy-security-hardening/30-05-SUMMARY.md` recording:
- `tests/integration/isolation.test.ts` final test count (12) + added entities list
- `tests/integration/isolation-actions.test.ts` final test count and list of Server Actions covered
- Any signature mismatches discovered during implementation + how resolved
- Any file split performed (e.g., separate TOTP IDOR file)
- Integration test runtime (target: under 60s per config)
</output>
