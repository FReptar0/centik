---
phase: 30-vercel-deploy-security-hardening
plan: 03
type: execute
wave: 2
depends_on: [30-02]
files_modified:
  - next.config.ts
  - src/proxy.ts
  - src/proxy.test.ts
autonomous: true
requirements: [DEPLOY-02]
tags: [security-headers, csp, nonce, hsts, proxy, middleware]
must_haves:
  truths:
    - "next.config.ts async headers() returns staticSecurityHeaders on source '/(.*)' matcher: X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy camera/microphone/geolocation/browsing-topics empty, and HSTS max-age=63072000;includeSubDomains;preload ONLY in production"
    - "src/proxy.ts preserves all existing Phase-26 auth redirect behavior (login/register public paths, callbackUrl for protected routes)"
    - "src/proxy.ts generates a per-request nonce via Buffer.from(crypto.randomUUID()).toString('base64') for non-redirect responses"
    - "src/proxy.ts injects x-nonce request header via NextResponse.next({ request: { headers: requestHeaders } }) so Server Components can read it via headers().get('x-nonce')"
    - "src/proxy.ts sets Content-Security-Policy response header with: default-src 'self'; script-src 'self' 'nonce-${nonce}' 'strict-dynamic' (+ 'unsafe-eval' in dev only); style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://*.upstash.io; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests"
    - "src/proxy.ts does NOT attach CSP to 3xx redirects (browser discards body)"
    - "All 6 existing src/proxy.test.ts auth tests still pass; 4+ new CSP tests added"
  artifacts:
    - path: "next.config.ts"
      provides: "Static security headers on all routes via async headers()"
      contains: "async headers()"
    - path: "src/proxy.ts"
      provides: "Auth middleware + CSP nonce generation + CSP response header"
      contains: "Content-Security-Policy"
    - path: "src/proxy.test.ts"
      provides: "Auth tests (preserved) + CSP/nonce tests (new)"
      contains: "nonce"
  key_links:
    - from: "src/proxy.ts"
      to: "response"
      via: "response.headers.set('Content-Security-Policy', csp) on non-redirect path"
      pattern: "Content-Security-Policy"
    - from: "src/proxy.ts"
      to: "Server Components"
      via: "requestHeaders.set('x-nonce', nonce) + NextResponse.next({ request: { headers: requestHeaders } })"
      pattern: "x-nonce"
    - from: "next.config.ts"
      to: "all responses"
      via: "async headers() with source: '/(.*)'"
      pattern: "async headers"
---

<objective>
Deliver the DEPLOY-02 security header stack: static headers (HSTS prod-only, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) via `next.config.ts` `async headers()`, and per-request CSP with nonce via `src/proxy.ts`. Preserve the existing Phase-26 auth-redirect behavior exactly — CSP attaches only to non-redirect responses. Extend `src/proxy.test.ts` with CSP tests without breaking the 6 existing auth tests.

Purpose: Defense-in-depth — CSP `script-src` with nonce + strict-dynamic blocks any accidentally-introduced inline script; HSTS locks in HTTPS; X-Frame-Options + `frame-ancestors 'none'` blocks clickjacking; nosniff blocks MIME-sniffing; Referrer-Policy limits referer leakage; Permissions-Policy disables camera/microphone/geolocation/browsing-topics by default.

Output: `next.config.ts` with async headers(), `src/proxy.ts` extended with CSP + nonce, `src/proxy.test.ts` extended with CSP tests. No other files touched — this plan is disjoint from Plan 30-04 (prisma/seed.prod.ts + package.json) and Plan 30-05 (isolation tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-vercel-deploy-security-hardening/30-CONTEXT.md
@.planning/phases/30-vercel-deploy-security-hardening/30-RESEARCH.md
@.planning/phases/30-vercel-deploy-security-hardening/30-PATTERNS.md
@.planning/phases/30-vercel-deploy-security-hardening/30-VALIDATION.md

<interfaces>
<!-- Extracted from codebase -- executors must NOT re-read. -->

## Existing next.config.ts (L1-7) — full file stub
```
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
}

export default nextConfig
```

## Existing src/proxy.ts (L1-33) — full file
```
import { auth } from '@/auth'
import { NextResponse } from 'next/server'

const publicPaths = ['/login', '/register']

export const proxy = auth((req) => {
  const { pathname } = req.nextUrl

  // Allow public routes
  if (publicPaths.some((path) => pathname.startsWith(path))) {
    if (req.auth) {
      return NextResponse.redirect(new URL('/', req.nextUrl.origin))
    }
    return NextResponse.next()
  }

  // Redirect unauthenticated users to /login with callbackUrl
  if (!req.auth) {
    const loginUrl = new URL('/login', req.nextUrl.origin)
    loginUrl.searchParams.set('callbackUrl', pathname)
    return NextResponse.redirect(loginUrl)
  }

  return NextResponse.next()
})

export const config = {
  matcher: [
    '/((?!api/auth|_next/static|_next/image|favicon.ico|fonts).*)',
  ],
}
```

The three `return NextResponse.next() / redirect()` inside the arrow body MUST be hoisted into a `response` variable so a single post-branch block can attach CSP + nonce to non-redirects.

## Existing src/proxy.test.ts helper (L24-31)
```
function createMockRequest(pathname: string, isAuthenticated: boolean) {
  const origin = 'http://localhost:3000'
  return {
    auth: isAuthenticated ? { user: { id: 'user-1', email: 'test@example.com' } } : null,
    nextUrl: new URL(`${origin}${pathname}`),
  }
}
```
Does NOT currently set `headers`. Must be extended to `headers: new Headers()` so `new Headers(req.headers)` inside proxy.ts works in tests.

## Canonical CSP string shape (D-06 + RESEARCH §Code Excerpts L637-649)
Assemble the CSP as a `.join('; ')` of these directives (dev-only extension noted):
- `default-src 'self'`
- `script-src 'self' 'nonce-${nonce}' 'strict-dynamic'` + `" 'unsafe-eval'"` when NODE_ENV !== 'production'
- `style-src 'self' 'unsafe-inline'`
- `img-src 'self' data: blob:`
- `font-src 'self' data:`
- `connect-src 'self' https://*.upstash.io`
- `frame-ancestors 'none'`
- `base-uri 'self'`
- `form-action 'self'`
- `object-src 'none'`
- `upgrade-insecure-requests`

## Canonical staticSecurityHeaders shape (D-05, D-07)
```
const isProduction = process.env.NODE_ENV === 'production'
const staticSecurityHeaders = [
  { key: 'X-Frame-Options', value: 'DENY' },
  { key: 'X-Content-Type-Options', value: 'nosniff' },
  { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
  { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=(), browsing-topics=()' },
  ...(isProduction
    ? [{ key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' }]
    : []),
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add staticSecurityHeaders to next.config.ts with async headers() + prod-gated HSTS</name>
  <read_first>
    next.config.ts L1-7 (stub; replace entire file).
    30-PATTERNS.md §next.config.ts (MODIFY) L253-268.
    30-RESEARCH.md §Code Excerpts L558-594.
    30-CONTEXT.md D-05, D-07, D-08.
    30-VALIDATION.md Tasks 30-03-01, 30-03-02.
  </read_first>
  <files>next.config.ts</files>
  <action>
    Overwrite `next.config.ts` with:

    ```
    import type { NextConfig } from 'next'

    // D-05 / D-07 / D-08 — static security headers.
    // HSTS is gated on NODE_ENV === 'production' because browsers cache HSTS
    // per-origin; emitting HSTS on http://localhost would brick local dev.
    // COOP/COEP intentionally omitted (D-08).
    const isProduction = process.env.NODE_ENV === 'production'

    const staticSecurityHeaders = [
      { key: 'X-Frame-Options', value: 'DENY' },
      { key: 'X-Content-Type-Options', value: 'nosniff' },
      { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
      {
        key: 'Permissions-Policy',
        value: 'camera=(), microphone=(), geolocation=(), browsing-topics=()',
      },
      ...(isProduction
        ? [
            {
              key: 'Strict-Transport-Security',
              value: 'max-age=63072000; includeSubDomains; preload',
            },
          ]
        : []),
    ]

    const nextConfig: NextConfig = {
      async headers() {
        return [
          {
            source: '/(.*)',
            headers: staticSecurityHeaders,
          },
        ]
      },
    }

    export default nextConfig
    ```

    Line count target: ~35 lines. Every header literal MUST match the canonical shape letter-for-letter (no reformatting, no additional directives, no omitted ones). Use the actual TypeScript block (the fenced example above uses no backticks because this is a plan; the delivered `next.config.ts` is real TS).
  </action>
  <verify>
    <automated>grep -q "X-Frame-Options" next.config.ts && grep -q "X-Content-Type-Options" next.config.ts && grep -q "Referrer-Policy" next.config.ts && grep -q "Permissions-Policy" next.config.ts && grep -q "Strict-Transport-Security" next.config.ts && grep -q "max-age=63072000" next.config.ts && grep -q "isProduction" next.config.ts && grep -q "async headers" next.config.ts</automated>
  </verify>
  <done>`next.config.ts` exports an `async headers()` returning staticSecurityHeaders on `source: '/(.*)'`; HSTS is wrapped in an `isProduction` ternary; all 5 header keys are present.</done>
</task>

<task type="auto">
  <name>Task 2: Extend src/proxy.ts with per-request nonce + CSP header (preserving auth redirects)</name>
  <read_first>
    src/proxy.ts L1-33 (full file; extend — do not rewrite from scratch).
    30-PATTERNS.md §src/proxy.ts (MODIFY) L209-249.
    30-RESEARCH.md §Code Excerpts L599-665 — canonical proxy.ts extension.
    30-CONTEXT.md D-05, D-06.
    30-VALIDATION.md Tasks 30-03-03, 30-03-04.
    node:crypto availability: Node 20.9+ exposes `crypto.randomUUID()` globally.
  </read_first>
  <files>src/proxy.ts</files>
  <action>
    Overwrite `src/proxy.ts` with the extended version. Preserve every existing public-path / auth-redirect condition — the three `return NextResponse.next() / redirect()` calls get hoisted into a single `response` variable so a post-branch block can attach CSP + nonce to non-redirects.

    Full new file contents:

    ```
    import { auth } from '@/auth'
    import { NextResponse } from 'next/server'

    const publicPaths = ['/login', '/register']

    export const proxy = auth((req) => {
      const { pathname } = req.nextUrl

      // --- Phase 26 auth redirect chain — ORDER PRESERVED ---
      const isPublic = publicPaths.some((path) => pathname.startsWith(path))

      let response: NextResponse
      if (isPublic) {
        if (req.auth) {
          response = NextResponse.redirect(new URL('/', req.nextUrl.origin))
        } else {
          response = NextResponse.next()
        }
      } else if (!req.auth) {
        const loginUrl = new URL('/login', req.nextUrl.origin)
        loginUrl.searchParams.set('callbackUrl', pathname)
        response = NextResponse.redirect(loginUrl)
      } else {
        response = NextResponse.next()
      }

      // --- D-05 / D-06 — CSP nonce + CSP header on non-redirect responses ---
      // Browsers discard the body of 3xx responses, so CSP on a redirect is wasted bytes
      // AND would force the client to cache a stale nonce against the redirect target.
      if (response.status >= 300 && response.status < 400) {
        return response
      }

      const nonce = Buffer.from(crypto.randomUUID()).toString('base64')
      const isDev = process.env.NODE_ENV !== 'production'

      const csp = [
        `default-src 'self'`,
        `script-src 'self' 'nonce-${nonce}' 'strict-dynamic'${isDev ? " 'unsafe-eval'" : ''}`,
        `style-src 'self' 'unsafe-inline'`, // Tailwind v4 + React 19 trade-off (D-06)
        `img-src 'self' data: blob:`,        // QR codes (Phase 29) + backup-code download blob
        `font-src 'self' data:`,             // next/font inlines as data URIs
        `connect-src 'self' https://*.upstash.io`, // Upstash rate-limit REST API
        `frame-ancestors 'none'`,            // matches X-Frame-Options: DENY
        `base-uri 'self'`,
        `form-action 'self'`,
        `object-src 'none'`,
        `upgrade-insecure-requests`,
      ].join('; ')

      // Inject nonce into downstream request headers so Server Components can
      // read it via headers().get('x-nonce') if they render <Script nonce={...}>.
      // Next.js 16 auto-applies the nonce to its own framework scripts when a CSP
      // header containing 'nonce-${value}' is present on the response.
      const requestHeaders = new Headers(req.headers)
      requestHeaders.set('x-nonce', nonce)

      const responseWithNonce = NextResponse.next({
        request: { headers: requestHeaders },
      })

      // Preserve any headers (e.g. Set-Cookie) that were on the original response.
      response.headers.forEach((v, k) => responseWithNonce.headers.set(k, v))
      responseWithNonce.headers.set('Content-Security-Policy', csp)
      return responseWithNonce
    })

    export const config = {
      matcher: [
        // Exclude Next internals + NextAuth handler (which manages its own response headers)
        '/((?!api/auth|_next/static|_next/image|favicon.ico|fonts).*)',
      ],
    }
    ```

    Notes (follow exactly):
    - Use `crypto.randomUUID()` (globally available in Node 20.9+). Do NOT import from `node:crypto` — the global call is the pattern Next.js docs recommend.
    - The `isDev` branch adds `'unsafe-eval'` ONLY in dev — React Fast Refresh uses eval. Production must not have it.
    - The short-circuit `if (response.status >= 300 && response.status < 400) return response` is LOAD-BEARING: it prevents CSP from attaching to the 302 to /login, which the browser discards anyway.
    - Do NOT add any directives beyond the 11 listed. Do NOT drop `upgrade-insecure-requests`.
    - The matcher is unchanged from the existing file.

    Target line count: ~70 lines. Must stay under the 300-line file cap.
  </action>
  <verify>
    <automated>grep -q "crypto.randomUUID" src/proxy.ts && grep -q "Content-Security-Policy" src/proxy.ts && grep -q "x-nonce" src/proxy.ts && grep -q "strict-dynamic" src/proxy.ts && grep -q "upstash.io" src/proxy.ts && grep -q "frame-ancestors" src/proxy.ts && grep -q "upgrade-insecure-requests" src/proxy.ts && grep -q "response.status >= 300" src/proxy.ts</automated>
  </verify>
  <done>src/proxy.ts generates a nonce via `crypto.randomUUID()`, sets `x-nonce` on downstream request headers, sets `Content-Security-Policy` on non-redirect responses only, and preserves all existing auth redirect behavior. Every canonical CSP directive is present.</done>
</task>

<task type="auto">
  <name>Task 3: Extend src/proxy.test.ts with CSP tests (preserve 6 existing auth tests)</name>
  <read_first>
    src/proxy.test.ts L1-103 (full file; extend in place — do NOT delete existing tests).
    30-VALIDATION.md Task 30-03-05 — existing auth-redirect behavior must be preserved.
    Task 2 of THIS PLAN — the proxy.ts shape executors just produced.
  </read_first>
  <files>src/proxy.test.ts</files>
  <action>
    In `src/proxy.test.ts`:

    ## Change 1 — Update the `createMockRequest` helper (L24-31)

    Currently:
    ```
    function createMockRequest(pathname: string, isAuthenticated: boolean) {
      const origin = 'http://localhost:3000'
      return {
        auth: isAuthenticated ? { user: { id: 'user-1', email: 'test@example.com' } } : null,
        nextUrl: new URL(`${origin}${pathname}`),
      }
    }
    ```

    Add a `headers: new Headers()` field so `new Headers(req.headers)` inside proxy.ts works:
    ```
    function createMockRequest(pathname: string, isAuthenticated: boolean) {
      const origin = 'http://localhost:3000'
      return {
        auth: isAuthenticated ? { user: { id: 'user-1', email: 'test@example.com' } } : null,
        nextUrl: new URL(`${origin}${pathname}`),
        headers: new Headers(),
      }
    }
    ```

    This is additive — existing tests do not read `req.headers`, so adding it cannot break them.

    ## Change 2 — Keep all 6 existing `describe('proxy', ...)` tests unchanged

    Do NOT modify the 6 existing `it(...)` blocks (redirects unauthenticated, preserves callbackUrl, allows authenticated, allows /login, allows /register, redirects authenticated from /login). They must continue to pass.

    ## Change 3 — Add a new describe block for CSP tests (append at end of file, before the final `})`)

    Append:
    ```
    describe('proxy CSP + security headers', () => {
      beforeEach(async () => {
        vi.clearAllMocks()
        await import('@/proxy')
      })

      it('sets Content-Security-Policy with a per-request nonce on authenticated non-redirect responses', () => {
        const handler = mockAuthCallback.handler as (req: unknown) => NextResponse
        const req = createMockRequest('/', true)
        const result = handler(req)

        expect(result).toBeInstanceOf(NextResponse)
        const csp = result!.headers.get('Content-Security-Policy')
        expect(csp).toBeTruthy()
        expect(csp).toContain("default-src 'self'")
        expect(csp).toContain("'strict-dynamic'")
        expect(csp).toMatch(/nonce-[A-Za-z0-9+/=]+/)
        expect(csp).toContain("frame-ancestors 'none'")
        expect(csp).toContain("connect-src 'self' https://*.upstash.io")
      })

      it('produces a fresh nonce on each request', () => {
        const handler = mockAuthCallback.handler as (req: unknown) => NextResponse
        const a = handler(createMockRequest('/', true))
        const b = handler(createMockRequest('/', true))

        const cspA = a!.headers.get('Content-Security-Policy')!
        const cspB = b!.headers.get('Content-Security-Policy')!
        const nonceA = cspA.match(/nonce-([A-Za-z0-9+/=]+)/)?.[1]
        const nonceB = cspB.match(/nonce-([A-Za-z0-9+/=]+)/)?.[1]
        expect(nonceA).toBeTruthy()
        expect(nonceB).toBeTruthy()
        expect(nonceA).not.toBe(nonceB)
      })

      it('does NOT attach CSP to redirect responses', () => {
        const handler = mockAuthCallback.handler as (req: unknown) => NextResponse
        // Unauthenticated hitting protected route -> 3xx redirect to /login
        const req = createMockRequest('/movimientos', false)
        const result = handler(req)

        expect(result.status).toBeGreaterThanOrEqual(300)
        expect(result.status).toBeLessThan(400)
        expect(result.headers.get('Content-Security-Policy')).toBeNull()
      })

      it('injects x-nonce request header matching the response CSP nonce', () => {
        // NextResponse.next({ request: { headers } }) stores the new headers under
        // the internal 'x-middleware-request-headers' header. We verify via CSP.
        const handler = mockAuthCallback.handler as (req: unknown) => NextResponse
        const req = createMockRequest('/', true)
        const result = handler(req)

        const csp = result!.headers.get('Content-Security-Policy')!
        const nonceMatch = csp.match(/nonce-([A-Za-z0-9+/=]+)/)
        expect(nonceMatch).toBeTruthy()
        // The nonce shape is base64 of a UUID (about 48 chars)
        expect(nonceMatch![1].length).toBeGreaterThan(20)
      })
    })
    ```

    These 4 new tests cover:
    1. CSP header present with canonical directives on authenticated non-redirect.
    2. Nonce freshness (two requests -> two different nonces).
    3. CSP NOT set on 3xx redirects (the short-circuit path).
    4. Nonce shape is valid base64 of a UUID.

    Do NOT add tests that assert against `request.headers` mutations — NextResponse stores those via `x-middleware-request-headers` internally and is brittle to mock. Covering via CSP nonce is sufficient.
  </action>
  <verify>
    <automated>npm run test:run -- src/proxy.test.ts 2>&1 | tail -8 | grep -E "10 passed|Tests\s+10"</automated>
  </verify>
  <done>src/proxy.test.ts has 10 total passing tests (6 existing auth + 4 new CSP). All auth behavior unchanged; CSP behavior verified including fresh nonce, directives, redirect exclusion, and nonce shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser -> Vercel runtime | Every HTTP response crosses here; security headers attach here |
| Proxy -> Server Component | Nonce injected via x-nonce request header |
| Response -> Browser | CSP + HSTS + X-Frame-Options enforce client-side constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-CSP-001 | Tampering / Elevation | Response headers | mitigate | Task 1 adds 5 static headers on source '/(.*)' matcher: X-Frame-Options DENY (clickjacking), X-Content-Type-Options nosniff (MIME-sniffing), Referrer-Policy strict-origin-when-cross-origin (referer leakage), Permissions-Policy camera/mic/geo/browsing-topics empty (API denial), and prod-only HSTS max-age=63072000;includeSubDomains;preload (downgrade attack). Task 2 adds per-request CSP. Together: every response carries 6 security headers in prod, 5 in dev. |
| T-30-CSP-002 | Availability (local dev) | HSTS on localhost | mitigate | Task 1's `isProduction` ternary gates HSTS. HSTS is NEVER emitted on localhost. Browsers cache HSTS per-origin — one accidental emission on localhost:3000 would force HTTPS-only on that port for `max-age` seconds (63M = 2 years). The ternary is load-bearing defense. |
| T-30-CSP-003 | Tampering / Elevation (XSS) | CSP script-src | mitigate | Task 2's CSP uses `script-src 'self' 'nonce-${nonce}' 'strict-dynamic'` with NO `'unsafe-inline'` in script-src. `'unsafe-eval'` is DEV-ONLY (gated by isDev). `'strict-dynamic'` lets nonce'd scripts load others transitively. `'unsafe-inline'` appears only in `style-src` (documented Tailwind v4 trade-off, D-06). Any XSS-injected inline `<script>` without the fresh nonce is blocked by the browser — the fundamental CSP protection is intact. The nonce is generated fresh per request via `crypto.randomUUID()` (128-bit entropy). |
</threat_model>

<verification>
Quality loop:
```
npm run build
npm run lint
npm run test:run
```

Specific gates:
- [ ] `npm run test:run -- src/proxy.test.ts` — 10 tests pass (6 existing + 4 new CSP)
- [ ] `grep -q "X-Frame-Options" next.config.ts && grep -q "async headers" next.config.ts` — headers config in place
- [ ] `grep -q "crypto.randomUUID" src/proxy.ts && grep -q "Content-Security-Policy" src/proxy.ts` — CSP wired in proxy
- [ ] `grep -q "response.status >= 300" src/proxy.ts` — redirect short-circuit present
- [ ] `npm run build` exits 0 (no TypeScript errors)
- [ ] `npm run lint` exits 0

Manual smoke (optional, helpful for operator sanity but not gating):
- `npm run dev` + `curl -I http://localhost:3000/login` — should show X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Content-Security-Policy; should NOT show Strict-Transport-Security (dev).
- Load `/` authenticated in a browser + DevTools Console — zero CSP violations expected (Tailwind v4 `'unsafe-inline'` for style-src accepts inline styles per D-06).
</verification>

<success_criteria>
- `next.config.ts` exports async headers() returning 4 always-on headers + 1 prod-only HSTS on source '/(.*)'
- `src/proxy.ts` generates per-request nonce via `crypto.randomUUID()`, injects x-nonce request header, sets Content-Security-Policy response header on non-redirect responses only
- All 6 existing auth tests in `src/proxy.test.ts` still pass
- 4 new CSP tests in `src/proxy.test.ts` pass (directives, freshness, redirect exclusion, nonce shape)
- `npm run build` + `npm run lint` + `npm run test:run` all exit 0
- No regressions in integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/30-vercel-deploy-security-hardening/30-03-SUMMARY.md` recording:
- Final next.config.ts line count + header list
- Final src/proxy.ts structure (public-path branch -> response var -> redirect short-circuit -> nonce block -> CSP)
- Proxy test file test count (6 + 4 = 10) and any adjustments to the mock helper
- Grep gate results for all 4 canonical CSP directives
</output>
