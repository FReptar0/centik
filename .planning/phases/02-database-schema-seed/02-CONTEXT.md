# Phase 2: Database Schema + Seed - Context

**Gathered:** 2026-04-04
**Status:** Ready for planning

<domain>
## Phase Boundary

Complete Prisma schema for all 7 MVP entities (IncomeSource, Transaction, Category, Debt, Budget, Period, MonthlySummary) and 3 v2 entities (ValueUnit, UnitRate, Asset) with full fields, relations, indexes, and enums. Create migrations, write an idempotent seed script with realistic demo data including sample transactions and one closed month. The stub Period model from Phase 1 gets replaced with the full schema.

</domain>

<decisions>
## Implementation Decisions

### Seed data realism
- Use realistic non-zero amounts throughout — NOT zero balances
- Debts: credit card ~$15K balance, personal loan ~$80K — realistic Mexican amounts in centavos (BigInt)
- Budgets: quincenal amounts per category (e.g., Comida $2K, Servicios $1K) — non-zero to exercise BigInt paths
- Income sources: made-up amounts, this is demo data — user will replace with real data later
- Include 10-15 sample transactions for the current month (mix of income and expenses across categories)
- Include 1 closed previous month with MonthlySummary snapshot (enables trend chart and history table from day one)

### v2 entities
- Include FULL schema from DFR.md for ValueUnit, UnitRate, and Asset — all fields, all types, all constraints
- Include full relational integrity: Asset → ValueUnit FK, UnitRate → ValueUnit FK with proper cascading
- Seed default value units: MXN (precision=2, no provider), UDI (precision=6, Banxico SIE API), UMA (precision=2, manual/annual)
- No UI/API for v2 entities — schema and seed only

### Seed idempotency
- Use upsert by unique key: categories by name, periods by month+year, income sources by name, etc.
- Running seed twice must produce no errors and no duplicates
- Safe for development resets — `npx prisma migrate reset` triggers seed automatically

### Seed configuration
- TypeScript seed script: `prisma/seed.ts` executed via `tsx`
- Configure seed command in `prisma.config.ts` (Prisma 7 native approach)
- Runs via `npx prisma db seed`

### Schema source of truth
- DFR.md sections 2.1-2.10 are the authoritative specification for all entities
- All field names, types, relations, and indexes must match DFR.md exactly
- Enums: TransactionType, Frequency, DebtType, PaymentMethod, CategoryType, AssetCategory
- All monetary fields as BigInt (centavos), interest rates as Int (basis points)
- All IDs as cuid() generated by Prisma
- Indexes: Transaction(periodId, date), Transaction(categoryId), Budget(periodId, categoryId) UNIQUE, Period(month, year) UNIQUE, UnitRate(unitId, date) UNIQUE, Asset(unitId)

### Claude's Discretion
- Exact demo amounts for income sources, transactions, debts (as long as they're realistic and non-zero)
- Transaction descriptions and dates for sample data
- Migration naming convention
- Whether to split seed into separate files per entity or keep in one seed.ts

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `prisma/schema.prisma`: Stub Period model exists, needs full replacement with complete schema
- `prisma.config.ts`: Prisma 7 CLI config already exists — add seed command here
- `src/lib/prisma.ts`: PrismaClient singleton with PrismaPg adapter already configured
- `generated/prisma/`: Client output directory already set up

### Established Patterns
- Prisma 7 with `prisma-client-js` generator and `../generated/prisma` output
- PrismaPg driver adapter for PostgreSQL connection
- `dotenv` loaded in prisma.config.ts for DATABASE_URL
- `tsx` already installed as dev dependency (from Phase 1)

### Integration Points
- `prisma/schema.prisma`: Replace stub with full schema
- `prisma.config.ts`: Add seed configuration
- `prisma/seed.ts`: New file — seed script
- `.env`: DATABASE_URL already configured for dev DB (port 5432)
- `.env.test`: DATABASE_URL already configured for test DB (port 5433)

</code_context>

<specifics>
## Specific Ideas

- Demo data is temporary — user will replace with real financial data after initial setup
- The closed previous month enables testing trend charts and history table without needing to manually create and close a period during development
- Sample transactions should span multiple categories and include both income and expenses to make Dashboard development (Phase 7) immediately productive

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 02-database-schema-seed*
*Context gathered: 2026-04-04*
