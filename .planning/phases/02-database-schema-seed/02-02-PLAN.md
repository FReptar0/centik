---
phase: 02-database-schema-seed
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - prisma/seed.ts
  - tests/setup.ts
  - tests/integration/seed.test.ts
autonomous: true
requirements: [DB-05, DB-06, DB-07, DB-08, DB-09]

must_haves:
  truths:
    - "npx prisma db seed creates all reference data: 8 categories, 2 income sources, 2 debts, 2 periods, budgets, 10-15 transactions, 1 monthly summary, 3 value units"
    - "Running seed twice produces no errors and no duplicate records"
    - "All seeded monetary values are non-zero BigInt centavos"
    - "One previous month is closed with a MonthlySummary snapshot"
    - "Integration tests verify seed data correctness and idempotency"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "Idempotent seed script with realistic demo data for all entities"
      contains: "async function main"
    - path: "tests/integration/seed.test.ts"
      provides: "Integration tests verifying seed data correctness and idempotency"
      contains: "describe.*seed"
    - path: "tests/setup.ts"
      provides: "Test DB connection setup for integration tests"
      contains: "PrismaClient"
  key_links:
    - from: "prisma/seed.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient generated types"
      pattern: "prisma\\.(category|period|incomeSource|debt|budget|transaction|monthlySummary|valueUnit|unitRate)\\.upsert"
    - from: "prisma/seed.ts"
      to: "generated/prisma/client"
      via: "import PrismaClient"
      pattern: "import.*PrismaClient.*from.*generated/prisma"
    - from: "tests/integration/seed.test.ts"
      to: "prisma/seed.ts"
      via: "verifies seed output by querying DB"
      pattern: "prisma\\.(category|period)\\.(findMany|count)"
---

<objective>
Write an idempotent TypeScript seed script with realistic non-zero demo data for all entities, and integration tests that verify seed correctness and idempotency.

Purpose: Provide meaningful demo data from day one so every subsequent phase (dashboard, transactions, debts, budgets, history) has data to render during development. The closed previous month enables trend charts and history views immediately.
Output: prisma/seed.ts, tests/setup.ts, tests/integration/seed.test.ts
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-schema-seed/02-CONTEXT.md
@.planning/phases/02-database-schema-seed/02-RESEARCH.md
@DFR.md

# Need Plan 01 SUMMARY for schema details and migration state:
@.planning/phases/02-database-schema-seed/02-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs from research and Phase 1 -->

From prisma.config.ts (seed command already configured):
```typescript
migrations: {
  seed: 'npx tsx prisma/seed.ts',
},
```

From 02-RESEARCH.md — Seed script pattern (CRITICAL: must create own PrismaClient):
```typescript
import 'dotenv/config'
import { PrismaClient } from '../generated/prisma/client'
import { PrismaPg } from '@prisma/adapter-pg'

const adapter = new PrismaPg({
  connectionString: process.env.DATABASE_URL!,
})
const prisma = new PrismaClient({ adapter })

async function main() {
  // Seed operations using prisma.model.upsert()
}

main()
  .then(async () => { await prisma.$disconnect() })
  .catch(async (e) => {
    console.error(e)
    await prisma.$disconnect()
    process.exit(1)
  })
```

From 02-RESEARCH.md — Upsert patterns:
```typescript
// Simple unique field:
await prisma.category.upsert({
  where: { name: 'Comida' },
  update: {},
  create: { name: 'Comida', icon: 'utensils', color: '#fb923c', ... },
})

// Composite unique (month + year):
await prisma.period.upsert({
  where: { month_year: { month: 4, year: 2026 } },
  update: {},
  create: { month: 4, year: 2026, ... },
})

// Composite unique (periodId + categoryId):
await prisma.budget.upsert({
  where: { periodId_categoryId: { periodId: '...', categoryId: '...' } },
  update: {},
  create: { ... },
})
```

From vitest.integration.config.mts:
```typescript
test: {
  environment: 'node',
  include: ['tests/integration/**/*.test.ts'],
  setupFiles: ['tests/setup.ts'],
  pool: 'forks',
  poolOptions: { forks: { singleFork: true } },
}
```

From DFR.md 2.3 — Seed categories (exact data):
| Nombre | Icono (Lucide) | Color | Tipo |
|--------|----------------|-------|------|
| Comida | utensils | #fb923c | EXPENSE |
| Servicios | zap | #60a5fa | EXPENSE |
| Entretenimiento | clapperboard | #a78bfa | EXPENSE |
| Suscripciones | smartphone | #f472b6 | EXPENSE |
| Transporte | car | #fbbf24 | EXPENSE |
| Otros | package | #94a3b8 | EXPENSE |
| Empleo | briefcase | #34d399 | INCOME |
| Freelance | laptop | #22d3ee | INCOME |

From DFR.md 2.8 — Seed value units:
| Code | Name | Precision | Refresh | Provider |
|------|------|-----------|---------|----------|
| MXN | Peso Mexicano | 2 | 0 | Base currency, no API |
| UDI | Unidad de Inversion | 6 | 24h | Banxico SIE API (serie SP68257) |
| UMA | Unidad de Medida y Actualizacion | 2 | 8760h | INEGI / DOF |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write idempotent seed script with realistic demo data</name>
  <files>prisma/seed.ts</files>
  <action>
Create prisma/seed.ts as a standalone TypeScript seed script. It MUST instantiate its own PrismaClient with PrismaPg adapter (cannot import the app singleton from src/lib/prisma.ts). Import from '../generated/prisma/client'.

Structure the seed in dependency order using helper functions for readability. If the file exceeds 300 lines, extract the data constants into a separate file prisma/seed-data.ts and import them.

**Seed dependency order and data:**

1. **Categories** (DB-05): Upsert 8 categories by name (unique). Use exact data from DFR.md 2.3 table. All are isDefault: true, isActive: true. sortOrder: 1-8 in order listed. CategoryType enum values: 6 EXPENSE + 2 INCOME.

2. **Periods** (DB-08): Upsert 2 periods by month_year composite unique:
   - Previous month (March 2026): month=3, year=2026, startDate=2026-03-01, endDate=2026-03-31, isClosed=true, closedAt=2026-04-01T00:00:00Z
   - Current month (April 2026): month=4, year=2026, startDate=2026-04-01, endDate=2026-04-30, isClosed=false
   - Use `new Date(year, month, 0)` pattern for last day of month to handle variable month lengths

3. **Income Sources** (DB-06): Upsert 2 by name (unique):
   - "TerSoft (Empleo)": defaultAmount = BigInt('2500000') ($25,000.00 quincenal), frequency = QUINCENAL, type = "EMPLOYMENT", isActive = true
   - "Freelance": defaultAmount = BigInt('1500000') ($15,000.00 variable), frequency = VARIABLE, type = "FREELANCE", isActive = true

4. **Debts** (DB-07): Upsert 2 by name (unique):
   - "Tarjeta Nu": type = CREDIT_CARD, currentBalance = BigInt('1500000') ($15,000.00), creditLimit = BigInt('5000000') ($50,000.00), annualRate = 4500 (45.00%), minimumPayment = BigInt('75000') ($750.00), cutOffDay = 15, paymentDueDay = 5, isActive = true
   - "Prestamo Personal": type = PERSONAL_LOAN, currentBalance = BigInt('8000000') ($80,000.00), originalAmount = BigInt('12000000') ($120,000.00), annualRate = 2400 (24.00%), monthlyPayment = BigInt('350000') ($3,500.00), remainingMonths = 24, isActive = true

5. **Value Units** (DB-09): Upsert 3 by code (unique):
   - MXN: name="Peso Mexicano", precision=2, symbol="$", refreshInterval=0, isActive=true
   - UDI: name="Unidad de Inversion", precision=6, symbol="UDI", providerUrl="https://www.banxico.org.mx/SieAPIRest/service/v1/series/SP68257/datos/oportuno", providerPath="$.bmx.series[0].datos[0].dato", providerHeaders={"Bmx-Token": "BANXICO_API_TOKEN"}, refreshInterval=24, isActive=true
   - UMA: name="Unidad de Medida y Actualizacion", precision=2, symbol="UMA", refreshInterval=8760, isActive=true

6. **Budgets** (DB-08): Upsert budget entries for each EXPENSE category for the CURRENT period. Use periodId_categoryId composite unique. Non-zero quincenal amounts:
   - Comida: BigInt('200000') ($2,000.00)
   - Servicios: BigInt('100000') ($1,000.00)
   - Entretenimiento: BigInt('75000') ($750.00)
   - Suscripciones: BigInt('50000') ($500.00)
   - Transporte: BigInt('60000') ($600.00)
   - Otros: BigInt('40000') ($400.00)

7. **Transactions** (DB-08): Create 12-15 sample transactions for the current month (April 2026) spanning multiple categories. Mix of INCOME and EXPENSE. Use captured category IDs and period ID. For income transactions, link to appropriate income source. Use various payment methods. Include realistic descriptions.

   Also create 8-10 transactions for the PREVIOUS month (March 2026) to support the MonthlySummary and trend data.

   IMPORTANT: For transactions, use `prisma.transaction.create()` (not upsert) since transactions have no natural unique key. To maintain idempotency, check if transactions already exist for each period first: `const count = await prisma.transaction.count({ where: { periodId } })`. If count > 0, skip transaction seeding for that period.

8. **MonthlySummary** (DB-08): Upsert for the closed previous period (March 2026) by periodId (unique). Use realistic amounts that roughly match the seeded March transactions:
   - totalIncome: BigInt('5000000') ($50,000.00)
   - totalExpenses: BigInt('3200000') ($32,000.00)
   - totalSavings: BigInt('1800000') ($18,000.00)
   - savingsRate: 3600 (36.00%)
   - debtAtClose: BigInt('9500000') ($95,000.00)
   - debtPayments: BigInt('425000') ($4,250.00)

9. **Unit Rates** (DB-09): Seed sample rates for UDI and UMA value units. Use create-if-not-exists pattern (check by unitId_date composite unique).
   - UDI: date=2026-04-03, rateToMxnCents=BigInt('829') ($8.29), rateRaw="8.290000", source="manual"
   - UMA: date=2026-01-01, rateToMxnCents=BigInt('11340') ($113.40), rateRaw="113.40", source="manual"

Add console.log statements after each entity group to show progress (e.g., "Seeded 8 categories", "Seeded 2 periods", etc.).

At the end, log "Seed completed successfully" on success.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && docker compose up -d && npx prisma migrate reset --force && npx prisma db seed</automated>
  </verify>
  <done>
    - prisma/seed.ts exists and runs without errors via `npx prisma db seed`
    - Running seed twice (via `npx prisma db seed` twice, or `npx prisma migrate reset --force` which seeds automatically) produces no errors and no duplicates
    - 8 categories seeded with correct names, icons, colors, and types from DFR.md
    - 2 income sources with non-zero BigInt amounts
    - 2 debts with non-zero BigInt balances (~$15K CC, ~$80K loan)
    - 2 periods (previous closed, current open)
    - 6 budget entries for current period with non-zero quincenal amounts
    - 12-15 transactions for current month, 8-10 for previous month
    - 1 MonthlySummary for the closed previous month
    - 3 value units (MXN, UDI, UMA) with seed rates for UDI and UMA
  </done>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for seed data correctness and idempotency</name>
  <files>tests/setup.ts, tests/integration/seed.test.ts</files>
  <action>
**First, update tests/setup.ts** to provide a real PrismaClient connection for integration tests. The test DB runs on port 5433 (docker-compose.test.yml). The setup file must:
- Import dotenv/config to load environment variables
- Import PrismaClient from generated/prisma/client and PrismaPg adapter
- Create a PrismaClient instance connected to the test DB (uses DATABASE_URL from .env.test)
- Export the prisma instance for use in test files
- Add beforeAll hook to connect and afterAll hook to disconnect

IMPORTANT: The .env.test file already exists with DATABASE_URL pointing to port 5433. The vitest.integration.config.mts already points to tests/setup.ts as setupFiles. However, vitest does not auto-load .env.test — you need to either:
(a) Load dotenv with path override: `import dotenv from 'dotenv'; dotenv.config({ path: '.env.test' })` OR
(b) Set DATABASE_URL in the test setup directly pointing to the test DB

Use option (a) to keep the connection string in .env.test.

**Then, create tests/integration/seed.test.ts** with the following test suites:

1. **Test suite: "Seed data correctness"**
   - Before all tests in this suite: ensure test DB is clean and seeded by running prisma migrate reset programmatically (use `execSync` from child_process: `execSync('npx prisma migrate reset --force', { env: { ...process.env, DATABASE_URL: testDbUrl } })`)
   - Test: "seeds 8 categories (6 expense, 2 income)" — count categories, verify names match DFR.md list, verify 6 have type EXPENSE and 2 have type INCOME
   - Test: "seeds categories with correct Lucide icon names" — verify each category has the expected icon (utensils, zap, clapperboard, smartphone, car, package, briefcase, laptop)
   - Test: "seeds 2 periods (previous closed, current open)" — count periods, verify one is closed and one is open, verify month/year values
   - Test: "seeds 2 income sources with non-zero amounts" — count income sources, verify TerSoft and Freelance exist, verify defaultAmount > 0n for both
   - Test: "seeds 2 debts with non-zero balances" — count debts, verify credit card and personal loan, verify currentBalance > 0n
   - Test: "seeds budget entries for current period" — count budgets for current period, verify all have quincenalAmount > 0n
   - Test: "seeds sample transactions for both periods" — count transactions per period, verify both have > 0 transactions
   - Test: "seeds MonthlySummary for closed period" — find MonthlySummary, verify it exists and has non-zero totalIncome and totalExpenses
   - Test: "seeds 3 value units (MXN, UDI, UMA)" — count value units, verify codes match
   - Test: "all monetary fields are BigInt" — query a debt and verify typeof currentBalance === 'bigint', query a transaction and verify typeof amount === 'bigint'

2. **Test suite: "Seed idempotency"**
   - Test: "running seed twice produces no errors" — run `execSync('npx prisma db seed', { env: ... })` and verify it does not throw
   - Test: "running seed twice produces no duplicate categories" — count categories after second seed, verify still exactly 8
   - Test: "running seed twice produces no duplicate periods" — count periods, verify still exactly 2

Use `describe` and `it` blocks. Import the prisma client from tests/setup.ts. Use `toBe`, `toBeGreaterThan`, `toBeDefined` matchers. For BigInt comparisons, use `toBe(expectedBigIntValue)` with the `n` suffix.

After writing the tests, run:
1. `docker compose -f docker-compose.test.yml up -d` — start test DB
2. `npm run test:integration` — all tests must pass
3. `npm run build` — verify build still passes
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && docker compose -f docker-compose.test.yml up -d && npm run test:integration</automated>
  </verify>
  <done>
    - tests/setup.ts exports a PrismaClient connected to the test database
    - tests/integration/seed.test.ts contains at least 12 test cases covering all seed entity groups
    - Seed correctness tests verify: category count/names/icons, period count/state, income source amounts, debt balances, budget amounts, transaction counts, MonthlySummary existence, value unit codes, BigInt field types
    - Seed idempotency tests verify: no errors on second run, no duplicate records
    - `npm run test:integration` passes all tests
    - `npm run build` passes with zero errors
  </done>
</task>

</tasks>

<verification>
- `npx prisma db seed` completes without errors
- `npx prisma db seed` run a second time completes without errors (idempotency)
- `npm run test:integration` passes all seed tests
- `npm run build` passes with zero errors
- `npm run quality` passes (build + lint + format:check + test)
</verification>

<success_criteria>
- Seed script creates all reference data with realistic non-zero amounts
- Seed is fully idempotent (safe for migrate reset and repeated runs)
- Integration tests verify every entity group from DB-05 through DB-09
- All BigInt monetary fields are non-zero to exercise serialization paths
- One closed previous month with MonthlySummary enables trend and history views from day one
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-schema-seed/02-02-SUMMARY.md`
</output>
