---
phase: 29-totp-two-factor-authentication
plan: 02
type: execute
wave: 1
depends_on:
  - 29-01
files_modified:
  - src/lib/totp-crypto.ts
  - src/lib/totp-crypto.test.ts
  - src/lib/totp.ts
  - src/lib/totp.test.ts
  - src/lib/backup-codes.ts
  - src/lib/backup-codes.test.ts
  - src/lib/challenge.ts
  - src/lib/challenge.test.ts
  - src/lib/rate-limit.ts
  - src/lib/rate-limit.test.ts
  - src/lib/validators.ts
  - src/lib/validators.test.ts
autonomous: true
requirements:
  - TOTP-01
  - TOTP-02
  - TOTP-03
  - TOTP-04
  - TOTP-05
requirements_addressed:
  - TOTP-01
  - TOTP-02
  - TOTP-03
  - TOTP-04
  - TOTP-05

must_haves:
  truths:
    - "encryptSecret + decryptSecret round-trip any plaintext byte-for-byte"
    - "Mutated ciphertext or authTag causes decryptSecret to throw (AES-GCM tamper detection)"
    - "Missing / non-hex / wrong-length AUTH_TOTP_ENCRYPTION_KEY fails fast at module import"
    - "verifyTotp accepts codes generated within +/-30s (window=1) and rejects codes outside the window"
    - "generateBackupCodes produces 10 distinct 8-hex-char strings (32 bits entropy each)"
    - "consumeBackupCode is single-use and scoped by userId -- User B cannot consume User A's code"
    - "signChallenge + verifyChallenge round-trip returns { userId, email }; expired / tampered / length-mismatched tokens return null"
    - "Rate-limit helper short-circuits to success when RATE_LIMIT_DISABLED=true OR NODE_ENV !== 'production'"
    - "Rate-limit helper returns { success: false, retryAfterMs } on 6th call (mocked limiter)"
    - "getClientIp returns first hop of x-forwarded-for, falls back to x-real-ip, then 127.0.0.1"
    - "4 new Zod schemas (loginPasswordSchema, verifyTotpSchema, enableTotpSchema, disableTotpSchema) reject malformed inputs with Spanish messages"
  artifacts:
    - path: "src/lib/totp-crypto.ts"
      provides: "encryptSecret(plaintext), decryptSecret(stored) -- AES-256-GCM with AUTH_TOTP_ENCRYPTION_KEY"
      exports: ["encryptSecret", "decryptSecret"]
    - path: "src/lib/totp.ts"
      provides: "createTotpSecret(), buildOtpauthUri(secret, email), verifyTotp(secret, code) -- wrappers over otplib v13"
      exports: ["createTotpSecret", "buildOtpauthUri", "verifyTotp"]
    - path: "src/lib/backup-codes.ts"
      provides: "generateBackupCodes(n), formatForDisplay(raw), hashBackupCodes(codes), consumeBackupCode(userId, code)"
      exports: ["generateBackupCodes", "formatForDisplay", "hashBackupCodes", "consumeBackupCode"]
    - path: "src/lib/challenge.ts"
      provides: "signChallenge(userId, email), verifyChallenge(token) -- HMAC-SHA256, 5-min TTL"
      exports: ["signChallenge", "verifyChallenge"]
    - path: "src/lib/rate-limit.ts"
      provides: "loginLimiter, totpLimiter, checkRateLimit(limiter, key), getClientIp() -- Upstash sliding window 5/60s with dev bypass"
      exports: ["loginLimiter", "totpLimiter", "checkRateLimit", "getClientIp"]
    - path: "src/lib/validators.ts"
      provides: "loginPasswordSchema, verifyTotpSchema, enableTotpSchema, disableTotpSchema"
      contains: "loginPasswordSchema"
  key_links:
    - from: "src/lib/totp-crypto.ts"
      to: "process.env.AUTH_TOTP_ENCRYPTION_KEY"
      via: "loadKey() at module import -- throws on missing/invalid"
      pattern: "AUTH_TOTP_ENCRYPTION_KEY"
    - from: "src/lib/challenge.ts"
      to: "process.env.AUTH_SECRET"
      via: "HMAC-SHA256 key derived from AUTH_SECRET buffer"
      pattern: "AUTH_SECRET"
    - from: "src/lib/rate-limit.ts"
      to: "process.env.UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN (or bypass when RATE_LIMIT_DISABLED=true)"
      via: "Redis.fromEnv() inside buildLimiter(), short-circuited in dev/test"
      pattern: "RATE_LIMIT_DISABLED"
    - from: "src/lib/backup-codes.ts"
      to: "prisma.backupCode"
      via: "findMany + updateMany with userId filter"
      pattern: "prisma\\.backupCode"
---

<objective>
Implement the 5 new pure-function utility modules that Waves 2-3 consume:
- `src/lib/totp-crypto.ts` — AES-256-GCM encrypt/decrypt for the TOTP secret (TOTP-02)
- `src/lib/totp.ts` — otplib v13 wrapper (generate secret, build otpauth URI, verify code with window=1) (TOTP-01)
- `src/lib/backup-codes.ts` — generation + bcrypt-hash + atomic single-use consume via `updateMany` (TOTP-04)
- `src/lib/challenge.ts` — HMAC-SHA256 challenge token with 5-minute TTL + constant-time signature compare (TOTP-03)
- `src/lib/rate-limit.ts` — Upstash sliding-window 5-per-60s with dev bypass + `getClientIp` (TOTP-05)

Plus 4 new Zod schemas appended to `src/lib/validators.ts` (D-30) — `loginPasswordSchema`, `verifyTotpSchema`, `enableTotpSchema`, `disableTotpSchema`, all with Spanish error messages.

Each file ships with a paired `.test.ts` that FULLY exercises its exported surface. All existing Wave-0 `it.todo` stubs flip to real `it` cases. Target 100% line coverage on the 5 new lib files (VALIDATION.md requirement for Plan 29-02 tasks).

**Purpose:** These are the cryptographic + behavioral primitives that Plans 29-03 and 29-04 consume. They MUST be bulletproof before any consumer is wired.

**Output:** 5 lib files + 5 matching test files + 4 new Zod schemas + validators test coverage, producing ~600 lines of production code and ~800 lines of tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-totp-two-factor-authentication/29-CONTEXT.md
@.planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md
@.planning/phases/29-totp-two-factor-authentication/29-PATTERNS.md
@.planning/phases/29-totp-two-factor-authentication/29-VALIDATION.md
@.planning/phases/29-totp-two-factor-authentication/29-01-SUMMARY.md
@src/lib/invite-utils.ts
@src/lib/validators.ts
@src/lib/prisma.ts

<interfaces>
<!-- RESEARCH Pattern 1 - AES-GCM with key validated at import (lines 336-378 - COPY VERBATIM, add Spanish JSDoc) -->
<!-- See research file for full snippet; key rules: -->
<!--   - ALGO = 'aes-256-gcm', IV_LENGTH = 12 (NIST SP 800-38D), KEY_HEX_LENGTH = 64 -->
<!--   - loadKey() throws at module import if env var missing / wrong length / non-hex -->
<!--   - encryptSecret returns `iv:ciphertext:authTag` (all hex) -->
<!--   - decryptSecret reverses; cipher.final() throws on tamper -->
<!--   - Exports: encryptSecret, decryptSecret (named, no default) -->

<!-- RESEARCH Pattern 2 - otplib v13 wrapper (lines 386-411) -->
<!-- Imports: import { generateSecret, verify, generateURI } from 'otplib' -->
<!-- Exports: createTotpSecret(): string, buildOtpauthUri(secret, email): string, verifyTotp(secret, code): Promise<boolean> -->
<!-- verifyTotp destructures result.valid from otplib verify({ secret, token, epochTolerance: 30 }) -->
<!-- DO NOT use `import { authenticator } from 'otplib'` - v12 API removed in v13 (Pitfall 5) -->

<!-- RESEARCH Pattern 4 - HMAC-SHA256 challenge (lines 437-474) -->
<!-- Imports: createHmac, timingSafeEqual from 'node:crypto' -->
<!-- CHALLENGE_TTL_MS = 5 * 60 * 1000 (D-16) -->
<!-- signChallenge(userId, email) returns `${payloadB64}.${sig}` -->
<!-- verifyChallenge: pre-check a.length === b.length BEFORE timingSafeEqual (Pitfall 4) -->
<!-- Returns { userId, email } on valid / null on invalid/expired/tampered -->

<!-- RESEARCH Pattern 5 - Rate-limit helper (lines 482-524) -->
<!-- Imports: Ratelimit from '@upstash/ratelimit', Redis from '@upstash/redis', headers from 'next/headers' -->
<!-- isBypassed() returns true when NODE_ENV !== 'production' OR RATE_LIMIT_DISABLED === 'true' -->
<!-- Exports: loginLimiter (Ratelimit | null), totpLimiter (Ratelimit | null), checkRateLimit, getClientIp -->
<!-- Ratelimit.slidingWindow(5, '60 s') per D-25 -->

<!-- RESEARCH Pattern 8 - consumeBackupCode (lines 696-731) -->
<!-- Imports: randomBytes from 'node:crypto', bcrypt from 'bcryptjs', prisma from '@/lib/prisma' -->
<!-- generateBackupCodes(n) returns Array of n raw 8-hex strings -->
<!-- formatForDisplay uppercases + adds dash at pos 4: 'ab12cd34' -> 'AB12-CD34' -->
<!-- hashBackupCodes normalizes (strip dashes + lowercase) BEFORE bcrypt.hash at cost 12 -->
<!-- consumeBackupCode: findMany { userId, usedAt: null } -> loop compare -> on hit updateMany { id, usedAt: null } and check count === 1 -->

<!-- Zod schemas to append to src/lib/validators.ts (RESEARCH lines 916-958, D-30, D-31) -->
<!-- loginPasswordSchema: email (string, email, trim) + password (string, min 1) -->
<!-- verifyTotpSchema: challenge (string, min 10) + code (string, trim, min 6, max 9) -->
<!-- enableTotpSchema: secret (string, min 16) + code (string, regex /^\d{6}$/) -->
<!-- disableTotpSchema: code (string, trim, min 6, max 9) -->
<!-- All error messages Spanish (D-31) matching existing invite/login schemas style -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement totp-crypto + totp + challenge libs with paired tests</name>
  <read_first>
    - src/lib/invite-utils.ts (FULL FILE — module shape template: named exports, JSDoc with requirement IDs, `import { randomBytes } from 'node:crypto'`)
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Pattern 1 AES-GCM (lines 333-381) — `totp-crypto.ts` verbatim
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Pattern 2 otplib v13 (lines 383-414) — `totp.ts` verbatim
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Pattern 4 HMAC challenge (lines 433-477) — `challenge.ts` verbatim
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Pitfall 2 (Edge runtime), Pitfall 3 (key format), Pitfall 4 (timingSafeEqual length), Pitfall 5 (otplib v13 API) — lines 850-874
    - .planning/phases/29-totp-two-factor-authentication/29-PATTERNS.md § `src/lib/totp-crypto.ts` (lines 38-55), § `src/lib/totp.ts` (lines 58-64), § `src/lib/challenge.ts` (lines 89-94)
    - src/lib/totp-crypto.test.ts (Wave-0 stub from Plan 29-01 — replace `it.todo` bullets with real tests)
    - src/lib/totp.test.ts (Wave-0 stub)
    - src/lib/challenge.test.ts (Wave-0 stub)
    - .planning/phases/29-totp-two-factor-authentication/29-VALIDATION.md Tasks 29-02-01, 29-02-02, 29-02-04 (lines 48-51) — automated verify commands
    - src/lib/utils.test.ts (FULL FILE) — existing Vitest idiom, NO React Testing Library setup needed for pure utils
  </read_first>
  <files>src/lib/totp-crypto.ts, src/lib/totp-crypto.test.ts, src/lib/totp.ts, src/lib/totp.test.ts, src/lib/challenge.ts, src/lib/challenge.test.ts</files>
  <behavior>
    **totp-crypto tests (TOTP-02 — T-29-CRYPTO-001):**
    - round-trip: `decryptSecret(encryptSecret(plain)) === plain` for plaintext `'JBSWY3DPEHPK3PXP'` (base32 sample)
    - IV uniqueness: encrypting the same plaintext 1000 times yields 1000 distinct IV prefixes
    - tamper detection: mutating the authTag (flip one hex char) causes `decryptSecret` to throw
    - tamper detection: mutating the ciphertext byte causes `decryptSecret` to throw
    - format: output matches `/^[0-9a-f]+:[0-9a-f]+:[0-9a-f]+$/` and the IV portion is exactly 24 hex chars (12 bytes)
    - key validation: import throws when `AUTH_TOTP_ENCRYPTION_KEY` is unset, too short (63 chars), too long (65 chars), or contains a non-hex char

    **totp tests (TOTP-01, TOTP-03):**
    - `createTotpSecret()` returns a string of base32 alphabet chars (≥16 chars)
    - `buildOtpauthUri(secret, 'user@example.com')` starts with `otpauth://totp/Centik:user@example.com?` and contains `issuer=Centik` + `secret=<secret>`
    - `verifyTotp(secret, code)` returns true for code generated at `Date.now()` (use `vi.useFakeTimers` + real otplib `generate` to produce a current code)
    - `verifyTotp(secret, code)` returns true for code generated 29 seconds ago (still within window=1 / epochTolerance=30s)
    - `verifyTotp(secret, code)` returns false for code generated 120 seconds ago (outside window)
    - `verifyTotp(secret, 'wrong')` returns false without throwing

    **challenge tests (TOTP-03 — T-29-AUTH-004):**
    - round-trip: `verifyChallenge(signChallenge('u1', 'a@b.c'))` returns `{ userId: 'u1', email: 'a@b.c' }`
    - expired: use `vi.useFakeTimers`; sign at T0, advance 5m01s, verify returns null
    - tampered signature: flip one char of the signature half → null
    - tampered payload: flip one char of the payload half → null (because HMAC no longer matches)
    - length mismatch (Pitfall 4): pass a token where `parts[1]` length differs from expected sig length → null (no throw from timingSafeEqual)
    - malformed: pass `'onlyonepart'`, `''`, `'a.b.c'` → null
    - wrong shape in payload: manually build a valid-signature token whose JSON lacks `userId` → null
  </behavior>
  <action>
    Create 3 production files + 3 test files. For each production file, open the matching Wave-0 stub from Plan 29-01, delete its `it.todo` body, and write real `it` cases covering the Behavior section above.

    **1. `src/lib/totp-crypto.ts`** — COPY RESEARCH Pattern 1 verbatim (lines 336-378 of 29-RESEARCH.md) + add Spanish JSDoc headers. Exact exports: `encryptSecret(plaintext: string): string`, `decryptSecret(stored: string): string`. The `loadKey()` call at module top triggers validation on first import (Pitfall 3 — fail fast at boot).

    **2. `src/lib/totp.ts`** — COPY RESEARCH Pattern 2 verbatim (lines 386-411 of 29-RESEARCH.md). Exact exports: `createTotpSecret(): string`, `buildOtpauthUri(secret: string, email: string): string`, `verifyTotp(secret: string, code: string): Promise<boolean>`. Use NAMED imports from otplib (`import { generateSecret, verify, generateURI } from 'otplib'`) — NOT `import { authenticator }` (Pitfall 5, v12 API is removed). Read the `verify` result's `.valid` field — do not boolean-coerce the object.

    **3. `src/lib/challenge.ts`** — COPY RESEARCH Pattern 4 verbatim (lines 437-474 of 29-RESEARCH.md). Exact exports: `signChallenge(userId: string, email: string): string`, `verifyChallenge(token: string): { userId: string; email: string } | null`. `CHALLENGE_TTL_MS = 5 * 60 * 1000` (D-16). The length pre-check on `a.length === b.length` is MANDATORY (Pitfall 4 — timingSafeEqual throws on unequal lengths).

    **Test harness setup for all three test files:**
    - Stub env vars at top of each test file via `vi.stubEnv('AUTH_TOTP_ENCRYPTION_KEY', 'a'.repeat(64))` / `vi.stubEnv('AUTH_SECRET', 'test-secret-32-bytes-of-entropy-ok')` before importing the module under test
    - For key-validation tests: use `vi.resetModules()` + dynamic `await import('@/lib/totp-crypto')` inside each case, stubbing bad values before import to trigger `loadKey()`
    - For clock tests in totp/challenge: `vi.useFakeTimers()` in `beforeEach` and `vi.useRealTimers()` in `afterEach`
    - For otplib time-window tests: call real `generate(secret)` (otplib) with `vi.setSystemTime(new Date('2026-01-01T00:00:00Z'))` to produce a deterministic code, then pass it to `verifyTotp`

    Follow JSDoc convention from `src/lib/invite-utils.ts`: `/** Descripción — D-XX, TOTP-YY */`. Keep Spanish domain terms, but function names/variables stay English.

    All 3 production files stay under 100 lines each (CLAUDE.md: files <300 lines, functions <50 lines — these are well inside).
  </action>
  <verify>
    <automated>npx vitest run src/lib/totp-crypto.test.ts src/lib/totp.test.ts src/lib/challenge.test.ts && grep -q 'export function encryptSecret' src/lib/totp-crypto.ts && grep -q 'export function decryptSecret' src/lib/totp-crypto.ts && grep -q 'export function createTotpSecret' src/lib/totp.ts && grep -q 'export async function verifyTotp' src/lib/totp.ts && grep -q 'export function signChallenge' src/lib/challenge.ts && grep -q 'export function verifyChallenge' src/lib/challenge.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/totp-crypto.ts` exports `encryptSecret` and `decryptSecret` as named functions
    - `src/lib/totp-crypto.ts` imports from `'node:crypto'` (not legacy `'crypto'`)
    - `! grep -q "import.*'crypto'" src/lib/totp-crypto.ts` — uses `node:crypto` prefix only
    - `grep -q "'aes-256-gcm'" src/lib/totp-crypto.ts` — uses correct AES mode
    - `grep -q 'IV_LENGTH.*12' src/lib/totp-crypto.ts` — 96-bit IV per NIST
    - `grep -q 'AUTH_TOTP_ENCRYPTION_KEY' src/lib/totp-crypto.ts` — reads correct env var
    - `src/lib/totp.ts` imports NAMED exports from otplib: `generateSecret`, `verify`, `generateURI`
    - `! grep -q 'authenticator' src/lib/totp.ts` — does NOT use removed v12 API
    - `grep -q 'epochTolerance: 30' src/lib/totp.ts` OR `grep -q 'window.*1' src/lib/totp.ts` — enforces ±30s tolerance
    - `src/lib/challenge.ts` imports `createHmac` and `timingSafeEqual` from `'node:crypto'`
    - `grep -q 'a.length !== b.length' src/lib/challenge.ts` OR `grep -q 'a.length === b.length' src/lib/challenge.ts` — length pre-check present (Pitfall 4)
    - `grep -q '5 \* 60 \* 1000' src/lib/challenge.ts` OR `grep -q '300000' src/lib/challenge.ts` — 5-minute TTL (D-16)
    - `npx vitest run src/lib/totp-crypto.test.ts` exits 0 with all named tests green (round-trip, tamper-ciphertext, tamper-authTag, IV-uniqueness, format-regex, key-validation x4)
    - `npx vitest run src/lib/totp.test.ts` exits 0 with all named tests green (secret-format, uri-shape, verify-at-T0, verify-at-T+29, verify-at-T+120, verify-wrong-code)
    - `npx vitest run src/lib/challenge.test.ts` exits 0 with all named tests green (round-trip, expired, tampered-sig, tampered-payload, length-mismatch, malformed, wrong-shape)
    - No test file contains `it.todo` or `test.skip` in these three files (all stubs promoted)
    - Each production file is under 100 lines; each test file is under 250 lines
  </acceptance_criteria>
  <done>
    3 production files + 3 test files committed. All named behaviors verified. Zero `it.todo` remain in the 3 test files. Each module's exports match the interface declared in `<interfaces>` above.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backup-codes + rate-limit libs with paired tests</name>
  <read_first>
    - src/lib/invite-utils.ts (module shape reference)
    - src/lib/prisma.ts (FULL FILE — singleton pattern for rate-limit module)
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Pattern 5 Rate-limit helper (lines 479-526)
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Pattern 8 Atomic backup-code consume (lines 692-733)
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Mocking @upstash/ratelimit in unit tests (lines 1009-1033) — exact vi.mock() shape
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Pitfall 6 (peer-dep) — confirms @upstash/redis@1.37.0 + @upstash/ratelimit@2.0.8 play nicely
    - .planning/phases/29-totp-two-factor-authentication/29-PATTERNS.md § `src/lib/backup-codes.ts` (lines 66-85) — bcrypt + cost 12 + hash-before-transaction context
    - .planning/phases/29-totp-two-factor-authentication/29-PATTERNS.md § `src/lib/rate-limit.ts` (lines 97-123) — singleton shape + bypass
    - .planning/phases/29-totp-two-factor-authentication/29-CONTEXT.md D-10, D-11, D-24..D-27 (lines 37-40, 60-66)
    - .planning/phases/29-totp-two-factor-authentication/29-VALIDATION.md Tasks 29-02-03, 29-02-05 (lines 50, 52)
    - src/auth.ts lines 32 (bcrypt.compare pattern) — how bcryptjs is imported + called
    - src/lib/backup-codes.test.ts, src/lib/rate-limit.test.ts (Wave-0 stubs from Plan 29-01)
  </read_first>
  <files>src/lib/backup-codes.ts, src/lib/backup-codes.test.ts, src/lib/rate-limit.ts, src/lib/rate-limit.test.ts</files>
  <behavior>
    **backup-codes tests (TOTP-04 — T-29-AUTH-003, T-29-DATA-002):**
    - `generateBackupCodes(10)` returns an array of 10 strings, each matching `/^[0-9a-f]{8}$/`, all unique
    - `generateBackupCodes(3)` returns 3 codes (respects `n` parameter)
    - `formatForDisplay('ab12cd34')` returns `'AB12-CD34'` (uppercase + dash at position 4)
    - `formatForDisplay('ffff0000')` returns `'FFFF-0000'`
    - `hashBackupCodes(['ab12cd34'])` returns an array where `bcrypt.compare('ab12cd34', hash)` succeeds
    - `hashBackupCodes(['AB12-CD34'])` returns a hash that `bcrypt.compare('ab12cd34', hash)` ALSO succeeds (normalization — dashes stripped, lowercased BEFORE hash)
    - `consumeBackupCode(userId, code)` — first call with valid code returns true
    - `consumeBackupCode(userId, code)` — second call with same code returns false (single-use)
    - `consumeBackupCode(userId, code)` — code belonging to a different user returns false (isolation)
    - `consumeBackupCode(userId, 'not-hex')` returns false without throwing (shape check)
    - `consumeBackupCode(userId, 'AB12-CD34')` matches a stored hash of `'ab12cd34'` (dash + case tolerant)

    **rate-limit tests (TOTP-05 — T-29-RATELIMIT-001):**
    - `checkRateLimit(null, 'any-key')` returns `{ success: true }` (bypass when limiter is null)
    - With mocked `Ratelimit` whose `limit()` returns `success: true` for calls 1-5 and `success: false` for call 6:
      - Calls 1-5 return `{ success: true }`
      - Call 6 returns `{ success: false, retryAfterMs: <positive number> }`
    - `getClientIp()` returns `'203.0.113.5'` when `x-forwarded-for: '203.0.113.5, 10.0.0.1'`
    - `getClientIp()` returns `'203.0.113.9'` when `x-forwarded-for: '203.0.113.9'` (single hop)
    - `getClientIp()` returns `'10.0.0.5'` when only `x-real-ip: '10.0.0.5'` is set
    - `getClientIp()` returns `'127.0.0.1'` when neither header is set
    - Bypass: `loginLimiter` is `null` when `RATE_LIMIT_DISABLED=true` (stubEnv before import + resetModules)
    - Bypass: `loginLimiter` is `null` when `NODE_ENV=test` (default in Vitest)
  </behavior>
  <action>
    Create 2 production files + 2 test files.

    **1. `src/lib/backup-codes.ts`** — copy RESEARCH Pattern 8 (lines 696-731) with these explicit exports:

    ```typescript
    export function generateBackupCodes(n?: number): string[]
    export function formatForDisplay(raw: string): string
    export async function hashBackupCodes(codes: string[]): Promise<string[]>  // hashes normalized (lowercase, no dashes) at cost 12
    export async function consumeBackupCode(userId: string, code: string): Promise<boolean>
    ```

    - `hashBackupCodes` MUST normalize input (`code.replace(/-/g, '').toLowerCase()`) BEFORE `bcrypt.hash` so that display-formatted codes (`AB12-CD34`) hash the same as raw codes (`ab12cd34`). This invariant is what lets `consumeBackupCode` accept either form.
    - `consumeBackupCode` MUST: (a) normalize input, (b) shape-check against `/^[0-9a-f]{8}$/` (reject malformed immediately without DB hit), (c) `findMany({ where: { userId, usedAt: null } })`, (d) `bcrypt.compare` loop, (e) on match, `updateMany({ where: { id, usedAt: null }, data: { usedAt: new Date() } })` and return `count === 1`.
    - Import: `import bcrypt from 'bcryptjs'` (matches `src/auth.ts` line 32 idiom — default import).
    - `import prisma from '@/lib/prisma'`.
    - File stays under 80 lines.

    **2. `src/lib/rate-limit.ts`** — copy RESEARCH Pattern 5 (lines 482-524) with these explicit exports:

    ```typescript
    import { Ratelimit } from '@upstash/ratelimit'
    export const loginLimiter: Ratelimit | null  // null in dev/test
    export const totpLimiter: Ratelimit | null
    export type LimitResult = { success: true } | { success: false; retryAfterMs: number }
    export async function checkRateLimit(limiter: Ratelimit | null, key: string): Promise<LimitResult>
    export async function getClientIp(): Promise<string>
    ```

    - The `isBypassed()` helper is an internal function (NOT exported) that returns `true` when `process.env.NODE_ENV !== 'production' || process.env.RATE_LIMIT_DISABLED === 'true'`. D-26.
    - `buildLimiter()` creates a new `Ratelimit` with `Redis.fromEnv()` + `Ratelimit.slidingWindow(5, '60 s')` + `analytics: false` + `prefix: '@centik/ratelimit'`.
    - When bypassed, `loginLimiter` and `totpLimiter` are literally `null` — `checkRateLimit` returns `{ success: true }` immediately.
    - `getClientIp` reads Next.js `headers()` async; first hop of `x-forwarded-for`, fallback `x-real-ip`, fallback `'127.0.0.1'`.
    - File stays under 80 lines.

    **Test harness:**
    - `backup-codes.test.ts` — mock prisma with a factory so that `findMany` can be stubbed per case:
      ```typescript
      vi.mock('@/lib/prisma', () => ({
        default: {
          backupCode: {
            findMany: vi.fn(),
            updateMany: vi.fn(),
          },
        },
      }))
      ```
      Use REAL `bcryptjs` (no mock — compare semantics must be validated). For cross-user test, stub `findMany(userA)` to return hashed row and `findMany(userB)` to return empty array — confirms filter is applied at the Prisma layer.
    - `rate-limit.test.ts` — copy the `vi.mock('@upstash/ratelimit')` + `vi.mock('@upstash/redis')` blocks from RESEARCH lines 1009-1033 VERBATIM. For `getClientIp`, mock `next/headers`:
      ```typescript
      vi.mock('next/headers', () => ({
        headers: vi.fn(),
      }))
      ```
      Cast through to `vi.mocked(headers).mockResolvedValue(new Headers({ 'x-forwarded-for': '...' }))`.
    - For bypass tests, use `vi.stubEnv('RATE_LIMIT_DISABLED', 'true')` + `vi.resetModules()` + dynamic `await import('@/lib/rate-limit')` to pick up the new env.

    **No new deps required** — all deps installed in Plan 29-01.
  </action>
  <verify>
    <automated>npx vitest run src/lib/backup-codes.test.ts src/lib/rate-limit.test.ts && grep -q 'export function generateBackupCodes' src/lib/backup-codes.ts && grep -q 'export function formatForDisplay' src/lib/backup-codes.ts && grep -q 'export async function consumeBackupCode' src/lib/backup-codes.ts && grep -q 'export const loginLimiter' src/lib/rate-limit.ts && grep -q 'export const totpLimiter' src/lib/rate-limit.ts && grep -q 'export async function checkRateLimit' src/lib/rate-limit.ts && grep -q "slidingWindow(5, '60 s')" src/lib/rate-limit.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/backup-codes.ts` exports: `generateBackupCodes`, `formatForDisplay`, `hashBackupCodes`, `consumeBackupCode`
    - `grep -q 'randomBytes(4)' src/lib/backup-codes.ts` — 4 bytes = 8 hex chars = 32 bits entropy per D-10
    - `grep -q 'bcrypt.hash' src/lib/backup-codes.ts` AND `grep -q ', 12)' src/lib/backup-codes.ts` — cost factor 12 per D-11
    - `grep -q 'updateMany' src/lib/backup-codes.ts` AND `grep -q 'usedAt: null' src/lib/backup-codes.ts` — atomic single-use per Pattern 8
    - `grep -q 'userId,' src/lib/backup-codes.ts` — findMany scoped by userId per D-25 / Pitfall 8
    - `src/lib/rate-limit.ts` exports: `loginLimiter`, `totpLimiter`, `checkRateLimit`, `getClientIp`
    - `grep -q "slidingWindow(5, '60 s')" src/lib/rate-limit.ts` — 5-per-60s per D-25
    - `grep -q 'RATE_LIMIT_DISABLED' src/lib/rate-limit.ts` — bypass flag respected per D-26
    - `grep -q "NODE_ENV !== 'production'" src/lib/rate-limit.ts` — non-prod bypass per D-26
    - `grep -q 'x-forwarded-for' src/lib/rate-limit.ts` AND `grep -q 'x-real-ip' src/lib/rate-limit.ts` AND `grep -q '127.0.0.1' src/lib/rate-limit.ts` — fallback chain per D-27
    - `npx vitest run src/lib/backup-codes.test.ts` exits 0 (11 tests covering generation/format/hash/consume/isolation/shape)
    - `npx vitest run src/lib/rate-limit.test.ts` exits 0 (≥8 tests covering bypass/limit-exhaustion/ip-parsing)
    - No `it.todo` remain in these two test files
    - Each production file ≤80 lines, each test file ≤200 lines
  </acceptance_criteria>
  <done>
    `backup-codes.ts` provides generation + display-format + normalize-then-hash + atomic-consume-with-isolation. `rate-limit.ts` provides `loginLimiter` + `totpLimiter` singletons (null in dev/test), `checkRateLimit` bypass-aware wrapper, and `getClientIp` fallback chain. All tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append 4 Zod schemas to validators + test coverage</name>
  <read_first>
    - src/lib/validators.ts (FULL FILE — 190 lines, existing pattern for loginSchema/registerSchema/createInviteSchema at lines 150-189)
    - src/lib/validators.test.ts (existing tests — append new describe blocks alongside existing schema tests)
    - .planning/phases/29-totp-two-factor-authentication/29-RESEARCH.md § Zod schemas (lines 916-958) — copy verbatim
    - .planning/phases/29-totp-two-factor-authentication/29-PATTERNS.md § `src/lib/validators.ts` (lines 493-507) — analog pattern from loginSchema
    - .planning/phases/29-totp-two-factor-authentication/29-CONTEXT.md D-30, D-31 (lines 73-78) — Spanish messages requirement
    - .planning/phases/29-totp-two-factor-authentication/29-VALIDATION.md Task 29-02-06 (line 53)
  </read_first>
  <files>src/lib/validators.ts, src/lib/validators.test.ts</files>
  <behavior>
    - `loginPasswordSchema` — accepts `{ email: 'user@example.com', password: 'a' }`; rejects empty email (`'El correo es requerido'` OR zod default), invalid email (`'Correo electronico no valido'`), empty password (`'La contrasena es requerida'`)
    - `verifyTotpSchema` — accepts `{ challenge: 'xxxxxxxxxx', code: '123456' }`; rejects empty challenge (min 10), short code (<6 chars), long code (>9 chars), missing either field
    - `enableTotpSchema` — accepts `{ secret: 'JBSWY3DPEHPK3PXP', code: '123456' }`; rejects non-6-digit code (`'12345'`, `'abc123'`, `'1234567'`), short secret (<16 chars), missing secret
    - `disableTotpSchema` — accepts `{ code: '123456' }` and `{ code: 'AB12-CD34' }` (9 chars); rejects short (<6), long (>9), missing
    - All error messages Spanish (e.g., `'Ingresa un codigo de 6 digitos'`, `'Codigo invalido'`, `'Sesion expirada'`)
  </behavior>
  <action>
    **Append** (do NOT replace) 4 new schemas at the END of `src/lib/validators.ts`, after the existing `registerSchema` (line 189). Copy the exact Spanish messages from RESEARCH lines 919-957:

    ```typescript
    /** Phase 29 D-30 — email+password for step 1 of the two-step login */
    export const loginPasswordSchema = z.object({
      email: z
        .string({ error: 'El correo es requerido' })
        .email({ error: 'Correo electronico no valido' })
        .trim(),
      password: z
        .string({ error: 'La contrasena es requerida' })
        .min(1, { error: 'La contrasena es requerida' }),
    })

    /** Phase 29 D-30 — challenge + TOTP code (or 8-hex backup) for step 2 of login */
    export const verifyTotpSchema = z.object({
      challenge: z
        .string({ error: 'Sesion expirada' })
        .min(10, { error: 'Sesion expirada' }),
      code: z
        .string({ error: 'Ingresa un codigo' })
        .trim()
        .min(6, { error: 'Codigo invalido' })
        .max(9, { error: 'Codigo invalido' }),
    })

    /** Phase 29 D-30 — enable 2FA wizard step 2: secret (from step 1) + first 6-digit code */
    export const enableTotpSchema = z.object({
      secret: z
        .string({ error: 'Reinicia el asistente' })
        .min(16, { error: 'Reinicia el asistente' }),
      code: z
        .string({ error: 'Ingresa un codigo de 6 digitos' })
        .regex(/^\d{6}$/, { error: 'Ingresa un codigo de 6 digitos' }),
    })

    /** Phase 29 D-30 — disable 2FA / regenerate backup codes: current TOTP or backup code */
    export const disableTotpSchema = z.object({
      code: z
        .string({ error: 'Ingresa un codigo' })
        .trim()
        .min(6, { error: 'Codigo invalido' })
        .max(9, { error: 'Codigo invalido' }),
    })
    ```

    **Test additions** — append to `src/lib/validators.test.ts`, one `describe` block per schema. Each block tests: happy path, each field's error branch, extra-char and too-short cases, and at least one Spanish message match (`expect(result.error.issues[0].message).toMatch(/codigo/i)`). Keep consistent with existing validators.test.ts idiom (`safeParse` + `expect(result.success).toBe(false)`).

    Note: `loginPasswordSchema` is intentionally a new export — do NOT remove the existing `loginSchema` (Plan 29-03 will migrate `loginAction` to use `loginPasswordSchema`; leaving both during the transition prevents breakage if tests import the old name).
  </action>
  <verify>
    <automated>npx vitest run src/lib/validators.test.ts && grep -q 'export const loginPasswordSchema' src/lib/validators.ts && grep -q 'export const verifyTotpSchema' src/lib/validators.ts && grep -q 'export const enableTotpSchema' src/lib/validators.ts && grep -q 'export const disableTotpSchema' src/lib/validators.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export const loginPasswordSchema' src/lib/validators.ts` succeeds
    - `grep -q 'export const verifyTotpSchema' src/lib/validators.ts` succeeds
    - `grep -q 'export const enableTotpSchema' src/lib/validators.ts` succeeds
    - `grep -q 'export const disableTotpSchema' src/lib/validators.ts` succeeds
    - `grep -q 'Ingresa un codigo de 6 digitos' src/lib/validators.ts` — Spanish message per D-31
    - `grep -q "regex(/\^\\\\d{6}\$/" src/lib/validators.ts` OR `grep -q "^\\\\d{6}\$" src/lib/validators.ts` — 6-digit regex for enable
    - Existing `loginSchema` still exported (unchanged) — `grep -q 'export const loginSchema' src/lib/validators.ts` still succeeds
    - `npx vitest run src/lib/validators.test.ts` exits 0 with ALL existing tests + ≥16 new tests (4 per schema) passing
    - `src/lib/validators.ts` is under 300 lines (CLAUDE.md file size limit)
  </acceptance_criteria>
  <done>
    4 new schemas appended to `validators.ts` (existing schemas untouched). Test file covers happy path + each field's error branch + Spanish message presence for all 4 new schemas. `npm run test:run -- src/lib/validators` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.env` -> module globals | `loadKey()` in `totp-crypto.ts` and `getSecret()` in `challenge.ts` read secrets at module init; fail fast if missing |
| Client form input -> Zod validation | All 4 new schemas are consumed by Server Actions in Plan 29-03/04; validation is the FIRST defense before any logic runs |
| `consumeBackupCode(userId, code)` -> `prisma.backupCode` | `userId` parameter MUST come from session via `requireAuth()` in the calling Server Action (Plan 29-03/04); this module takes it on trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-CRYPTO-001 | Information disclosure / Tampering | `totp-crypto.ts` | mitigate | AES-256-GCM with authTag; mutated ciphertext throws at `decipher.final()`. `loadKey()` validates 64-hex-char format at import — fails fast on weak/missing key (Pitfall 3). Plaintext never logged, never returned from any function except `decryptSecret` return value consumed inline. |
| T-29-AUTH-002 | Spoofing | `totp.ts` verifyTotp | mitigate | `epochTolerance: 30` bounds replay to ±30s; no more. otplib v13 `verify().valid` is the only truthiness check (Pitfall 5). No code can be verified outside the current window. |
| T-29-AUTH-003 | Spoofing / Race | `backup-codes.ts` consumeBackupCode | mitigate | Atomic `updateMany({ where: { id, usedAt: null } })` with `count === 1` check — Postgres READ COMMITTED re-evaluates WHERE under row lock. Concurrent `Promise.all([consume, consume])` yields exactly one success (integration-tested in Plan 29-05). |
| T-29-AUTH-004 | Spoofing / Tampering | `challenge.ts` verifyChallenge | mitigate | HMAC-SHA256 with `AUTH_SECRET` (256-bit+). 5-min TTL embedded in signed payload. `timingSafeEqual` after explicit length pre-check (Pitfall 4). Payload JSON parse wrapped in try/catch returning null on malformed input. |
| T-29-RATELIMIT-001 | Denial of service bypass | `rate-limit.ts` checkRateLimit | mitigate | Composite keys in caller (`email:ip` for login, `userId:ip` for verify — Plan 29-03) force attacker to control BOTH to rotate. Dev bypass is explicit (`RATE_LIMIT_DISABLED=true` OR `NODE_ENV !== 'production'`) — not silent. Production will have Upstash provisioned (Phase 30 hardening). |
| T-29-DATA-002 | Elevation of privilege | `backup-codes.ts` consumeBackupCode | mitigate | `findMany({ where: { userId, usedAt: null } })` — `userId` is filter, not just a select column. Cross-user test in Plan 29-05 asserts User B's call with User A's plaintext returns false. |
| T-29-AUTH-005 | Brute-force | `validators.ts` schemas | accept | Zod schemas shape-check inputs; brute-force mitigation is rate-limit-based (T-29-RATELIMIT-001), not schema-based. Accepting this threat at the schema layer is correct. |
</threat_model>

<verification>
## Plan-Level Quality Gate

Run at end of plan, all must pass per CLAUDE.md Quality Loop:

```bash
npm run build                                    # TypeScript strict, zero errors
npm run lint                                     # ESLint zero warnings
npm run format:check                             # Prettier clean
npm run test:run                                 # All unit tests incl. new ones
npx vitest run src/lib/ --coverage               # Coverage sanity check on src/lib/
```

Specifically for this plan's files:

```bash
npx vitest run src/lib/totp-crypto.test.ts src/lib/totp.test.ts src/lib/backup-codes.test.ts src/lib/challenge.test.ts src/lib/rate-limit.test.ts src/lib/validators.test.ts
```

Output-level grep gates (Nyquist Observable Signal 19):

```bash
# Forbidden: Edge runtime on any file that imports node:crypto transitively
! grep -rE "runtime\s*=\s*'edge'" src/app src/lib src/auth.ts

# Forbidden: otplib v12 API in new code
! grep -q 'authenticator\.' src/lib/totp.ts

# Forbidden: timingSafeEqual without length pre-check
grep -q 'a.length !== b.length' src/lib/challenge.ts || grep -q 'a.length === b.length' src/lib/challenge.ts
```
</verification>

<success_criteria>
- 5 new lib files + 5 paired test files + `validators.ts` extended with 4 schemas
- 100% of Wave-0 `it.todo` stubs in these 6 test files have been replaced with real `it` cases
- All tests green: `npx vitest run src/lib/` exits 0
- `npm run build && npm run lint && npm run format:check && npm run test:run` all exit 0
- Each production file <100 lines; `src/lib/validators.ts` stays <300 lines
- No `authenticator` import in `totp.ts` (Pitfall 5 regression check)
- No runtime 'edge' declaration anywhere that imports `totp-crypto.ts` transitively
- `grep -q` commands in `<verification>` all return the expected zero / one matches
</success_criteria>

<output>
After completion, create `.planning/phases/29-totp-two-factor-authentication/29-02-SUMMARY.md` documenting:
- Exported signatures of all 5 new lib files (copy-paste from editor)
- Test counts per file (e.g., `totp-crypto.test.ts: 12 tests, 100% lines`)
- Any deviations from RESEARCH Patterns 1, 2, 4, 5, 8 (there should be none except Spanish JSDoc additions)
- Confirmation that `loginSchema` is still exported (used by legacy Plan 29-03 migration)
- Observation: Plan 29-03 will rename `loginSchema` -> `loginPasswordSchema` in `loginAction` imports
</output>
