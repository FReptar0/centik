---
phase: 17-token-foundation-class-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/lib/constants.ts
  - prisma/seed-data.ts
  - src/components/categories/CategoryForm.tsx
  - src/components/charts/TrendAreaChart.tsx
  - src/components/charts/BudgetBarChart.tsx
  - src/components/charts/ExpenseDonutChart.tsx
  - src/lib/constants.test.ts
  - src/lib/dashboard.test.ts
  - src/lib/budget.test.ts
  - src/app/configuracion/actions.test.ts
  - src/components/layout/FAB.test.tsx
  - src/components/transactions/TransactionForm.test.tsx
  - src/components/transactions/TransactionRow.test.tsx
  - src/components/transactions/TransactionList.test.tsx
autonomous: true
requirements:
  - MIGRATE-02
  - TEST-01

must_haves:
  truths:
    - "constants.ts DEFAULT_CATEGORIES has all 8 categories with desaturated Glyph Finance hex colors"
    - "seed-data.ts category colors match constants.ts exactly"
    - "CategoryForm.tsx COLOR_PRESETS array uses the same desaturated hex values"
    - "All three chart CHART_COLORS objects use Glyph Finance hex values (tooltipBg #141414, axis #666666, positive #00E676, negative #FF3333)"
    - "All 8 test files assert the new hex values instead of old ones"
    - "All 394+ existing tests pass with zero failures"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "DEFAULT_CATEGORIES with 6 desaturated expense colors + 2 income colors"
      contains: "#C88A5A"
    - path: "prisma/seed-data.ts"
      provides: "Seed category colors matching constants.ts"
      contains: "#C88A5A"
    - path: "src/components/charts/TrendAreaChart.tsx"
      provides: "CHART_COLORS with Glyph Finance palette"
      contains: "#00E676"
    - path: "src/lib/constants.test.ts"
      provides: "Test assertions for new category hex colors"
      contains: "#C88A5A"
  key_links:
    - from: "src/lib/constants.ts"
      to: "prisma/seed-data.ts"
      via: "Both files define the same 8 category colors -- they must match"
      pattern: "#C88A5A.*#7A9EC4.*#9B89C4"
    - from: "src/lib/constants.ts"
      to: "src/lib/constants.test.ts"
      via: "Test assertions must match the actual hex values in constants"
      pattern: "toBe.*#C88A5A"
---

<objective>
Update all hardcoded hex color values across constants, seed data, chart components, and test files to match the Glyph Finance desaturated palette.

Purpose: The token migration in Plan 01 changed CSS variable values, but JavaScript-level hex values (constants, chart colors, test fixtures) are not affected by CSS changes. These must be manually updated to the same Glyph Finance palette. Charts use hardcoded hex because Recharts ignores CSS variables. Tests assert specific hex values that now must match the new palette.

Output: constants.ts, seed-data.ts, CategoryForm.tsx, 3 chart files, and 8 test files all use new Glyph Finance hex values. All 394 tests pass.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-token-foundation-class-migration/17-CONTEXT.md

@src/lib/constants.ts
@prisma/seed-data.ts
@src/components/categories/CategoryForm.tsx
@src/components/charts/TrendAreaChart.tsx
@src/components/charts/BudgetBarChart.tsx
@src/components/charts/ExpenseDonutChart.tsx
@src/lib/constants.test.ts

<interfaces>
<!-- Hex color migration map: old -> new -->

Expense category colors (from STYLE_GUIDE.md desaturated palette):
  Comida:          #fb923c -> #C88A5A
  Servicios:       #60a5fa -> #7A9EC4
  Entretenimiento: #a78bfa -> #9B89C4
  Suscripciones:   #f472b6-> #C48AA3
  Transporte:      #fbbf24 -> #C4A84E
  Otros:           #94a3b8 -> #8A9099

Income category colors (Claude's Discretion -- desaturated to match palette):
  Empleo:          #34d399 -> #6BAF8E (desaturated green, harmonizes with muted expense palette)
  Freelance:       #22d3ee -> #7AACB8 (desaturated teal, complementary to income greens)

Chart color migration:
  tooltipBg:       #0a0f1a -> #141414 (surface-elevated)
  tooltipBorder:   #1e293b -> #222222 (border-divider)
  grid:            #1e293b -> REMOVED (CartesianGrid removed -- but hex is kept for now, will be removed in Phase 20)
  axis:            #64748b -> #666666 (text-tertiary)
  positive:        #34d399 -> #00E676
  negative:        #f87171 -> #FF3333
  budgetMuted:     #475569 -> #444444 (text-disabled)

Semantic colors in globals.css (already updated by Plan 01):
  accent:          #22d3ee -> #CCFF00
  positive:        #34d399 -> #00E676
  negative:        #f87171 -> #FF3333
  warning:         #fb923c -> #FF9100
  info:            #60a5fa -> #448AFF
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update constants.ts, seed-data.ts, and CategoryForm.tsx with new hex values</name>
  <files>src/lib/constants.ts, prisma/seed-data.ts, src/components/categories/CategoryForm.tsx</files>
  <action>
    Update all three files atomically -- they must have the SAME 8 hex color values.

    **1. src/lib/constants.ts** -- Update DEFAULT_CATEGORIES color values:
    ```
    Comida:          '#fb923c' -> '#C88A5A'
    Servicios:       '#60a5fa' -> '#7A9EC4'
    Entretenimiento: '#a78bfa' -> '#9B89C4'
    Suscripciones:   '#f472b6' -> '#C48AA3'
    Transporte:      '#fbbf24' -> '#C4A84E'
    Otros:           '#94a3b8' -> '#8A9099'
    Empleo:          '#34d399' -> '#6BAF8E'
    Freelance:       '#22d3ee' -> '#7AACB8'
    ```
    CATEGORY_COLORS map derives from DEFAULT_CATEGORIES automatically -- no change needed there.

    Also update the fallback color in `src/lib/dashboard.ts` line 157:
    `color: cat?.color ?? '#94a3b8'` -> `color: cat?.color ?? '#8A9099'`

    **2. prisma/seed-data.ts** -- Update the same 8 category color values to match constants.ts exactly. Same hex values as above.

    **3. src/components/categories/CategoryForm.tsx** -- There is a `COLOR_PRESETS` or similar array near line 38 with the 8 hardcoded hex color strings. Update to match the same 8 new values:
    ```typescript
    '#C88A5A', '#7A9EC4', '#9B89C4', '#C48AA3', '#C4A84E', '#8A9099', '#6BAF8E', '#7AACB8'
    ```

    **Income category color rationale (Claude's Discretion):** The expense palette uses desaturated earth tones (C88A5A amber, 7A9EC4 blue, etc.). Income colors should harmonize without clashing:
    - Empleo: #6BAF8E -- a muted sage green that complements the positive semantic color (#00E676) without being identical. Lower saturation matches the expense palette.
    - Freelance: #7AACB8 -- a muted teal that differentiates from the accent (#CCFF00) and services blue (#7A9EC4) while staying in the desaturated family.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && grep -c "#fb923c\|#60a5fa\|#a78bfa\|#f472b6\|#fbbf24\|#34d399\|#22d3ee" src/lib/constants.ts prisma/seed-data.ts src/components/categories/CategoryForm.tsx | grep -v ":0$" | wc -l | grep "^0$" && grep -q "#C88A5A" src/lib/constants.ts && grep -q "#C88A5A" prisma/seed-data.ts && echo "PASS: All source files updated to new hex values"</automated>
  </verify>
  <done>constants.ts, seed-data.ts, CategoryForm.tsx, and dashboard.ts all use the 8 new Glyph Finance desaturated category colors with zero old hex values remaining</done>
</task>

<task type="auto">
  <name>Task 2: Update CHART_COLORS in all three chart components</name>
  <files>src/components/charts/TrendAreaChart.tsx, src/components/charts/BudgetBarChart.tsx, src/components/charts/ExpenseDonutChart.tsx</files>
  <action>
    Update the hardcoded CHART_COLORS objects in each chart file. Recharts uses these JavaScript hex strings directly for SVG rendering -- CSS variables do not work here.

    **1. TrendAreaChart.tsx** (line ~21):
    ```typescript
    const CHART_COLORS = {
      grid: '#222222',        // was #1e293b -- kept for now, CartesianGrid removed in Phase 20
      axis: '#666666',        // was #64748b (text-tertiary)
      tooltipBg: '#141414',   // was #0a0f1a (surface-elevated)
      tooltipBorder: '#222222', // was #1e293b (border-divider)
      positive: '#00E676',    // was #34d399
      negative: '#FF3333',    // was #f87171
    }
    ```

    **2. BudgetBarChart.tsx** (line ~21):
    ```typescript
    const CHART_COLORS = {
      grid: '#222222',          // was #1e293b
      axis: '#666666',          // was #64748b
      tooltipBg: '#141414',     // was #0a0f1a
      tooltipBorder: '#222222', // was #1e293b
      budgetMuted: '#444444',   // was #475569 (text-disabled)
    }
    ```

    **3. ExpenseDonutChart.tsx** (line ~12):
    ```typescript
    const CHART_COLORS = {
      tooltipBg: '#141414',     // was #0a0f1a
      tooltipBorder: '#222222', // was #1e293b
    }
    ```

    Note: ExpenseDonutChart uses category colors from props (which come from the database via constants), not from CHART_COLORS. Those are already updated in Task 1.

    Do NOT change the chart structure (no CartesianGrid removal, no stroke width changes) -- that is Phase 20 work. Only update hex values.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && grep -c "#0a0f1a\|#1e293b\|#34d399\|#f87171\|#64748b\|#475569" src/components/charts/TrendAreaChart.tsx src/components/charts/BudgetBarChart.tsx src/components/charts/ExpenseDonutChart.tsx | grep -v ":0$" | wc -l | grep "^0$" && grep -q "#00E676" src/components/charts/TrendAreaChart.tsx && grep -q "#141414" src/components/charts/BudgetBarChart.tsx && echo "PASS: All chart CHART_COLORS updated"</automated>
  </verify>
  <done>All three chart components use Glyph Finance hex values in their CHART_COLORS objects with zero old palette colors remaining</done>
</task>

<task type="auto">
  <name>Task 3: Update all test files asserting old hex values</name>
  <files>src/lib/constants.test.ts, src/lib/dashboard.test.ts, src/lib/budget.test.ts, src/app/configuracion/actions.test.ts, src/components/layout/FAB.test.tsx, src/components/transactions/TransactionForm.test.tsx, src/components/transactions/TransactionRow.test.tsx, src/components/transactions/TransactionList.test.tsx</files>
  <action>
    Update all test fixture data and assertions to use the new category hex colors. These tests use old hex values in mock data and assertion expectations.

    **Migration map for test fixtures** (apply to ALL test files):
    ```
    '#fb923c' -> '#C88A5A'  (Comida)
    '#60a5fa' -> '#7A9EC4'  (Servicios)
    '#a78bfa' -> '#9B89C4'  (Entretenimiento)
    '#f472b6' -> '#C48AA3'  (Suscripciones)
    '#fbbf24' -> '#C4A84E'  (Transporte)
    '#94a3b8' -> '#8A9099'  (Otros)
    '#34d399' -> '#6BAF8E'  (Empleo)
    '#22d3ee' -> '#7AACB8'  (Freelance)
    ```

    **File-by-file changes:**

    1. **src/lib/constants.test.ts** (3 assertions):
       - Line 63: `expect(CATEGORY_COLORS['Comida']).toBe('#C88A5A')`
       - Line 64: `expect(CATEGORY_COLORS['Empleo']).toBe('#6BAF8E')`
       - Line 65: `expect(CATEGORY_COLORS['Freelance']).toBe('#7AACB8')`

    2. **src/lib/dashboard.test.ts** (~8 occurrences):
       - Mock category data: replace all `color: '#fb923c'` with `'#C88A5A'`, `'#60a5fa'` with `'#7A9EC4'`
       - Assertion: `expect(result[0].color).toBe('#7A9EC4')` (was '#60a5fa')
       - All `category: { name: 'Comida', color: '#fb923c' }` -> `color: '#C88A5A'`

    3. **src/lib/budget.test.ts** (~4 occurrences):
       - Mock data: Comida `'#fb923c'` -> `'#C88A5A'`, Transporte `'#fbbf24'` -> `'#C4A84E'`
       - Assertions: `categoryColor: '#C88A5A'` and `categoryColor: '#C4A84E'`

    4. **src/app/configuracion/actions.test.ts** (~3 occurrences):
       - Mock data: `color: '#f472b6'` -> `color: '#C48AA3'`

    5. **src/components/layout/FAB.test.tsx** (1 occurrence):
       - Mock category: `color: '#fb923c'` -> `color: '#C88A5A'`

    6. **src/components/transactions/TransactionForm.test.tsx** (~3 occurrences):
       - Mock categories: `'#fb923c'` -> `'#C88A5A'`, `'#34d399'` -> `'#6BAF8E'`, `'#22d3ee'` -> `'#7AACB8'`

    7. **src/components/transactions/TransactionRow.test.tsx** (~2 occurrences):
       - Mock data: `'#fb923c'` -> `'#C88A5A'`, `'#34d399'` -> `'#6BAF8E'`

    8. **src/components/transactions/TransactionList.test.tsx** (~1 occurrence):
       - Mock data: `'#fb923c'` -> `'#C88A5A'`

    After updating all test files, run `npm test` to verify all 394 tests pass.

    CRITICAL: Do NOT use `it.skip()` or `describe.skip()`. Every test must run and pass. If any test fails for a reason other than hex values, investigate and fix.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 test files updated with new Glyph Finance hex values. `npm test` passes all 394+ tests with zero failures and zero skipped</done>
</task>

</tasks>

<verification>
1. `grep -rn "#fb923c\|#60a5fa\|#a78bfa\|#f472b6\|#fbbf24\|#34d399\|#22d3ee" src/ prisma/` returns zero matches (no old category hex values remain anywhere)
2. `grep -c "#0a0f1a\|#111827\|#1e293b\|#475569" src/components/charts/` returns zero (no old palette hex in charts)
3. `npm test` passes all 394+ tests with zero failures
4. constants.ts, seed-data.ts, and CategoryForm.tsx all have identical sets of 8 category hex colors
</verification>

<success_criteria>
- All 8 category colors updated to Glyph Finance desaturated palette in constants.ts, seed-data.ts, and CategoryForm.tsx
- Income categories (Empleo, Freelance) have reasonable desaturated colors that harmonize with the expense palette
- All 3 chart CHART_COLORS objects use new hex values (no old #0a0f1a, #1e293b, #34d399, #f87171, #64748b, #475569)
- All 8 test files assert the new hex values
- `npm test` passes with zero failures and zero skipped tests
- Zero occurrences of old category hex values anywhere in src/ or prisma/
</success_criteria>

<output>
After completion, create `.planning/phases/17-token-foundation-class-migration/17-02-SUMMARY.md`
</output>
