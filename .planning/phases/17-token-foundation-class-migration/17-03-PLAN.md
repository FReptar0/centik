---
phase: 17-token-foundation-class-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - src/app/layout.tsx
  - src/app/loading.tsx
  - src/app/historial/loading.tsx
  - src/app/ingresos/loading.tsx
  - src/app/deudas/loading.tsx
  - src/app/movimientos/loading.tsx
  - src/app/presupuesto/loading.tsx
  - src/app/configuracion/loading.tsx
  - src/components/categories/CategoryForm.tsx
  - src/components/categories/CategoryList.tsx
  - src/components/debts/DebtCard.tsx
  - src/components/debts/DebtForm.tsx
  - src/components/debts/DebtList.tsx
  - src/components/debts/DebtSummaryCards.tsx
  - src/components/income/IncomeSourceCard.tsx
  - src/components/income/IncomeSourceForm.tsx
  - src/components/income/IncomeSourceList.tsx
  - src/components/income/IncomeSummaryCards.tsx
  - src/components/history/AnnualPivotTable.tsx
  - src/components/history/CloseConfirmationModal.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/layout/MobileMoreSheet.tsx
  - src/components/layout/FAB.tsx
  - src/components/layout/PeriodSelector.tsx
  - src/components/ui/Modal.tsx
  - src/components/dashboard/KPICard.tsx
  - src/components/dashboard/RecentTransactions.tsx
  - src/components/budgets/BudgetProgressList.tsx
  - src/components/budgets/BudgetSummaryRow.tsx
  - src/components/budgets/BudgetTable.tsx
  - src/components/transactions/TransactionForm.tsx
  - src/components/transactions/TransactionList.tsx
  - src/components/transactions/TransactionRow.tsx
  - src/components/transactions/TransactionFilters.tsx
  - src/components/charts/TrendAreaChart.tsx
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-03

must_haves:
  truths:
    - "Zero occurrences of bg-bg-primary, bg-bg-card, bg-bg-elevated, bg-bg-input, bg-bg-card-hover in any .tsx file"
    - "Zero occurrences of text-text-muted in any .tsx file (replaced with text-text-tertiary)"
    - "Zero occurrences of border-border (without -divider suffix) in any .tsx file (replaced with border-border-divider)"
    - "Zero occurrences of border-border-light in any .tsx file (removed)"
    - "Zero occurrences of shadow-sm, shadow-md, shadow-lg, shadow-glow in any .tsx file"
    - "Zero occurrences of border-border-focus or focus:shadow-[0_0_20px in any .tsx file (handled by global :focus-visible)"
    - "npm run build completes with zero errors"
    - "npm test passes all 394+ tests"
  artifacts:
    - path: "src/components/layout/FAB.tsx"
      provides: "FAB without shadow classes"
    - path: "src/components/ui/Modal.tsx"
      provides: "Modal without shadow and border classes, using new token names"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Sidebar with new token class names"
  key_links:
    - from: "src/app/globals.css"
      to: "All 36+ component files"
      via: "Token names in @theme generate Tailwind utility classes used in className props"
      pattern: "bg-bg|bg-surface-elevated|text-text-tertiary|border-border-divider"
---

<objective>
Rename all old Tailwind utility class names across 36+ component files to match the new Glyph Finance token names defined in globals.css Plan 01, then verify the full build and test suite pass.

Purpose: After Plan 01 replaced the @theme block and Plan 02 updated hardcoded hex values, old utility class names (bg-bg-primary, bg-bg-card, text-text-muted, border-border, shadow-glow, etc.) no longer resolve to any CSS. These classes silently produce no styles. This plan restores correct styling by updating every component to use the new token-derived class names, then runs the full Quality Loop to confirm the atomic migration is complete.

Output: All 36+ component files use new class names. Build passes. Tests pass. The app renders with the correct Glyph Finance visual identity.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-token-foundation-class-migration/17-CONTEXT.md
@.planning/phases/17-token-foundation-class-migration/17-02-SUMMARY.md

<interfaces>
<!-- Complete class name migration map -->
<!-- Apply these search-replace operations across ALL .tsx files in src/ -->

BACKGROUND CLASS RENAMES:
  bg-bg-primary      -> bg-bg
  bg-bg-card          -> bg-surface-elevated
  bg-bg-card-hover    -> bg-surface-hover
  bg-bg-elevated      -> bg-surface
  bg-bg-input         -> bg-transparent
  hover:bg-bg-card-hover -> hover:bg-surface-hover

BORDER CLASS RENAMES:
  border-border       -> border-border-divider   (careful: only when NOT already border-border-divider or border-border-light or border-border-focus)
  border-border-light -> REMOVE the entire class (token deleted)
  hover:border-border-light -> REMOVE the entire class

TEXT CLASS RENAMES:
  text-text-muted     -> text-text-tertiary

SHADOW CLASS REMOVALS (delete, do not replace):
  shadow-sm           -> REMOVE
  shadow-md           -> REMOVE
  shadow-lg           -> REMOVE
  shadow-glow         -> REMOVE

FOCUS PATTERN REMOVALS (handled by global :focus-visible in globals.css):
  focus:border-border-focus  -> REMOVE
  focus:outline-none         -> REMOVE (when adjacent to focus:border-border-focus)
  focus:shadow-[0_0_20px_rgba(34,211,238,0.15)]  -> REMOVE

  These focus patterns appear in 7 files: CategoryForm, DebtCard, DebtForm, TransactionForm,
  IncomeSourceForm, BudgetTable, TransactionFilters. After removal, the global :focus-visible
  rule in globals.css provides the accent-colored outline automatically.

BORDER CLASS HANDLING for border-border:
  The class `border-border` is tricky because `border-border-light` and `border-border-focus`
  contain the substring. Use word-boundary-aware replacement:
  - `border-border` (not followed by -light, -focus, or -divider) -> `border-border-divider`
  - `border-border-light` -> remove entirely
  - `border-border-focus` -> remove entirely (focus handled by global rule)

UNCHANGED (value changes handled by token, no class rename needed):
  text-accent, bg-accent, text-positive, text-negative, text-warning
  text-text-primary, text-text-secondary, text-text-disabled
  rounded-sm, rounded-md, rounded-lg, rounded-xl (values updated via token)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename background, text, border, shadow, and focus classes across all component files and verify build + tests</name>
  <files>src/app/loading.tsx, src/app/historial/loading.tsx, src/app/ingresos/loading.tsx, src/app/deudas/loading.tsx, src/app/movimientos/loading.tsx, src/app/presupuesto/loading.tsx, src/app/configuracion/loading.tsx, src/components/categories/CategoryForm.tsx, src/components/categories/CategoryList.tsx, src/components/debts/DebtCard.tsx, src/components/debts/DebtForm.tsx, src/components/debts/DebtList.tsx, src/components/debts/DebtSummaryCards.tsx, src/components/income/IncomeSourceCard.tsx, src/components/income/IncomeSourceForm.tsx, src/components/income/IncomeSourceList.tsx, src/components/income/IncomeSummaryCards.tsx, src/components/history/AnnualPivotTable.tsx, src/components/history/CloseConfirmationModal.tsx, src/components/layout/Sidebar.tsx, src/components/layout/MobileNav.tsx, src/components/layout/MobileMoreSheet.tsx, src/components/layout/FAB.tsx, src/components/layout/PeriodSelector.tsx, src/components/ui/Modal.tsx, src/components/dashboard/KPICard.tsx, src/components/dashboard/RecentTransactions.tsx, src/components/budgets/BudgetProgressList.tsx, src/components/budgets/BudgetSummaryRow.tsx, src/components/budgets/BudgetTable.tsx, src/components/transactions/TransactionForm.tsx, src/components/transactions/TransactionList.tsx, src/components/transactions/TransactionRow.tsx, src/components/transactions/TransactionFilters.tsx, src/components/charts/TrendAreaChart.tsx</files>
  <action>
    Apply the class name migration map across ALL component files. Read each file listed, apply the replacements, and write it back.

    **Apply in this order to avoid substring conflicts:**

    1. **First pass -- multi-word patterns (longest first to avoid partial matches):**
       - `hover:bg-bg-card-hover` -> `hover:bg-surface-hover`
       - `bg-bg-card-hover` -> `bg-surface-hover`
       - `hover:border-border-light` -> (remove the class entirely)
       - `border-border-light` -> (remove the class entirely)
       - `focus:border-border-focus` -> (remove the class entirely)
       - `border-border-focus` -> (remove the class entirely -- appears in DebtCard inline edit input)
       - `bg-bg-primary` -> `bg-bg`
       - `bg-bg-card` -> `bg-surface-elevated`
       - `bg-bg-elevated` -> `bg-surface`
       - `bg-bg-input` -> `bg-transparent`
       - `text-text-muted` -> `text-text-tertiary`

    2. **Second pass -- border-border (after all border-border-* variants are handled):**
       - `border-border` -> `border-border-divider`
       (Only occurrences that are NOT already `border-border-divider`)

    3. **Third pass -- shadow removal:**
       - Remove `shadow-glow` from class strings
       - Remove `shadow-lg` from class strings
       - Remove `shadow-md` from class strings
       - Remove `shadow-sm` from class strings

    4. **Fourth pass -- focus pattern removal:**
       In files that have `focus:outline-none focus:border-border-focus focus:shadow-[0_0_20px_rgba(34,211,238,0.15)]`, remove ALL THREE classes from the className string. The global `:focus-visible` rule in globals.css now handles focus styling automatically via `outline: 2px solid var(--color-accent)`.

       These patterns appear in:
       - `src/components/categories/CategoryForm.tsx` (line ~155)
       - `src/components/debts/DebtForm.tsx` (in the input class constant)
       - `src/components/income/IncomeSourceForm.tsx` (lines ~165, ~210)
       - `src/components/transactions/TransactionForm.tsx` (lines ~260, ~343, ~394, ~416, ~438)
       - `src/components/transactions/TransactionFilters.tsx` (lines ~224, ~242)
       - `src/components/budgets/BudgetTable.tsx` (line ~128)
       - `src/components/debts/DebtCard.tsx` (the inline edit input -- has `border-border-focus` without the full pattern)

    **Specific file notes:**
    - **FAB.tsx**: `shadow-lg shadow-glow` -> remove both. Result: `'rounded-full bg-accent'`. The chartreuse FAB on OLED black provides sufficient contrast without shadow.
    - **Modal.tsx (desktop)**: `border border-border bg-bg-card p-7 shadow-md` -> `border-border-divider bg-surface-elevated p-7` (remove border class? No -- keep `border` shorthand but update `border-border` -> `border-border-divider`, remove `shadow-md`)
    - **Modal.tsx (mobile)**: `bg-bg-card border-t border-border` -> `bg-surface-elevated border-t border-border-divider`
    - **TransactionFilters.tsx**: Has dropdown panels with `shadow-lg` -> remove
    - **TrendAreaChart.tsx**: Has `bg-bg-card border border-border` -> `bg-surface-elevated border border-border-divider`

    **After all replacements, run the full Quality Loop:**

    1. **Final sweep -- verify NO old tokens remain:**
       ```bash
       grep -rn "bg-bg-primary\|bg-bg-card\|bg-bg-elevated\|bg-bg-input\|text-text-muted\|shadow-glow\|shadow-lg\|shadow-md\|shadow-sm\|border-border-light\|border-border-focus" src/ --include="*.tsx"
       ```
       This grep should return ZERO results. If any remain, fix them.

    2. **Run build**:
       ```bash
       npm run build
       ```
       Must pass with zero errors. Tailwind v4 will not error on unknown utility classes (they silently produce no CSS), but TypeScript errors or import issues will be caught here.

    3. **Run tests**:
       ```bash
       npm test
       ```
       All 394+ tests must pass. The tests do NOT assert class names (verified), but structural changes to component files could break rendering tests.

    4. **Run lint** (if configured):
       ```bash
       npm run lint 2>/dev/null || echo "lint not configured"
       ```

    5. **Extended sweep -- verify no old tokens in any file type**:
       ```bash
       # Old font references
       grep -rn "DM.Sans\|dm-sans\|JetBrains" src/ --include="*.tsx" --include="*.ts" --include="*.css"

       # Old hex values in non-test source files
       grep -rn "#0a0f1a\|#111827\|#1a2332\|#0f172a" src/ --include="*.tsx" --include="*.ts" --include="*.css"
       ```
       ALL greps must return zero results.

    6. **If any issues found**: Fix them. Re-run build and tests. Do not proceed until clean.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && count=$(grep -rn "bg-bg-primary\|bg-bg-card[^-]\|bg-bg-elevated\|bg-bg-input\|text-text-muted\|shadow-glow\|shadow-lg\|shadow-md\|shadow-sm\|border-border-light\|border-border-focus" src/ --include="*.tsx" 2>/dev/null | wc -l) && echo "Old class occurrences: $count" && test "$count" -eq 0 && npm run build 2>&1 | tail -3 && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>All 36+ component files use new Tailwind utility class names. Zero occurrences of old class names in any .tsx file. `npm run build` completes with zero errors. `npm test` passes all 394+ tests with zero failures. Zero occurrences of any old token name, shadow class, font reference, or old hex value in the codebase (excluding .planning/ and docs).</done>
</task>

</tasks>

<verification>
1. `grep -rn "bg-bg-primary\|bg-bg-card[^-]\|bg-bg-elevated\|bg-bg-input\|text-text-muted\|shadow-glow\|shadow-lg\|shadow-md\|shadow-sm\|border-border-light\|border-border-focus" src/ --include="*.tsx"` returns zero results
2. `npm run build` passes with zero errors
3. `npm test` passes all 394+ tests with zero failures
4. `grep -rn "DM.Sans\|dm-sans\|#0a0f1a\|#111827" src/ --include="*.tsx" --include="*.ts" --include="*.css"` returns zero results
</verification>

<success_criteria>
- Every component file uses the new Glyph Finance token class names
- Zero old utility class names remain in any .tsx file
- Zero shadow classes remain anywhere in the app
- Focus styling is handled by the global :focus-visible rule only (no inline focus patterns)
- `npm run build` completes with zero errors
- `npm test` passes with zero failures and zero skipped
- The app renders with the correct Glyph Finance background, text, border, and accent colors
</success_criteria>

<output>
After completion, create `.planning/phases/17-token-foundation-class-migration/17-03-SUMMARY.md`
</output>
