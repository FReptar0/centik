---
phase: 03-foundation-libraries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/lib/serialize.ts
  - src/lib/serialize.test.ts
  - src/lib/utils.ts
  - src/lib/utils.test.ts
  - src/lib/constants.ts
  - src/lib/constants.test.ts
  - package.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-08

must_haves:
  truths:
    - "serializeBigInts converts nested BigInt values to strings without data loss"
    - "formatMoney('15075') returns '$150.75' in MXN format"
    - "toCents('150.75') returns '15075' using string-split parsing, no float math"
    - "parseCents('15075') returns 15075n BigInt"
    - "formatRate(4500) returns '45.00%'"
    - "formatUnitAmount('50000123456', 6) returns correctly formatted string"
    - "cn() merges conflicting Tailwind classes correctly"
    - "All unit tests for these functions pass"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Re-exported Prisma types and enums, plus Serialized* variants"
      exports: ["IncomeSource", "Transaction", "Category", "Debt", "Budget", "Period", "MonthlySummary", "TransactionType", "Frequency", "DebtType", "PaymentMethod", "CategoryType", "AssetCategory", "SerializedTransaction", "SerializedDebt", "SerializedBudget", "SerializedIncomeSource", "SerializedMonthlySummary"]
    - path: "src/lib/serialize.ts"
      provides: "serializeBigInts generic function"
      exports: ["serializeBigInts"]
    - path: "src/lib/utils.ts"
      provides: "All formatting/conversion utilities and cn"
      exports: ["formatMoney", "toCents", "parseCents", "formatRate", "formatUnitAmount", "cn"]
    - path: "src/lib/constants.ts"
      provides: "Shared constants (colors, defaults)"
      exports: ["CATEGORY_COLORS", "DEFAULT_CATEGORIES", "INCOME_SOURCE_TYPES", "PAYMENT_METHODS_DISPLAY", "FREQUENCY_DISPLAY"]
    - path: "src/lib/serialize.test.ts"
      provides: "Tests for serializeBigInts"
    - path: "src/lib/utils.test.ts"
      provides: "Tests for all utility functions"
    - path: "src/lib/constants.test.ts"
      provides: "Smoke tests for constant integrity"
  key_links:
    - from: "src/types/index.ts"
      to: "generated/prisma/client"
      via: "re-export"
      pattern: "export type.*from.*generated/prisma"
    - from: "src/lib/utils.ts"
      to: "clsx"
      via: "import for cn()"
      pattern: "import.*clsx"
    - from: "src/lib/utils.ts"
      to: "tailwind-merge"
      via: "import for cn()"
      pattern: "import.*twMerge.*tailwind-merge"
---

<objective>
Create the foundation utility layer: TypeScript type re-exports, BigInt serializer, money formatting/conversion utilities, cn() class name helper, and shared constants. Every function ships with comprehensive tests.

Purpose: Downstream phases (layout, CRUD, dashboard) import these utilities. They must be bulletproof -- 100% tested with edge cases -- because bugs here cascade into every feature.
Output: 7 files (4 source + 3 test) plus updated package.json with @vitest/coverage-v8.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-foundation-libraries/03-CONTEXT.md
@.planning/phases/03-foundation-libraries/03-RESEARCH.md

@prisma/schema.prisma
@src/lib/prisma.ts
@src/types/index.ts
@vitest.config.mts
@package.json
@tsconfig.json

<interfaces>
<!-- Prisma-generated types available at generated/prisma/client -->
<!-- Import path is ../../generated/prisma/client (relative) or re-export through @/types -->

From prisma/schema.prisma (enums available as runtime objects):
- TransactionType: { INCOME, EXPENSE }
- Frequency: { QUINCENAL, MENSUAL, SEMANAL, VARIABLE }
- DebtType: { CREDIT_CARD, PERSONAL_LOAN, AUTO_LOAN, OTHER }
- PaymentMethod: { EFECTIVO, DEBITO, CREDITO, TRANSFERENCIA }
- CategoryType: { EXPENSE, INCOME, BOTH }
- AssetCategory: { PPR, INVESTMENT, SAVINGS, CRYPTO, OTHER }

Models with BigInt fields (need Serialized* variants):
- Transaction: amount (BigInt)
- IncomeSource: defaultAmount (BigInt)
- Debt: currentBalance, creditLimit, minimumPayment, monthlyPayment, originalAmount (all BigInt)
- Budget: quincenalAmount (BigInt)
- MonthlySummary: totalIncome, totalExpenses, totalSavings, debtAtClose, debtPayments (all BigInt)

Already installed (no need to add):
- clsx 2.1.1
- tailwind-merge 3.5.0
- zod 4.3.6
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install coverage dep, create types, serializer, and constants with tests</name>
  <files>package.json, src/types/index.ts, src/lib/serialize.ts, src/lib/serialize.test.ts, src/lib/constants.ts, src/lib/constants.test.ts</files>
  <behavior>
    serializeBigInts:
    - serializeBigInts({ amount: 15075n }) returns { amount: "15075" }
    - serializeBigInts({ nested: { deep: 99n } }) returns { nested: { deep: "99" } }
    - serializeBigInts([1n, 2n, 3n]) returns ["1", "2", "3"]
    - serializeBigInts({ mixed: "hello", amount: 0n, flag: true }) preserves non-BigInt types
    - serializeBigInts(null) returns null
    - serializeBigInts({ date: new Date() }) preserves Date as ISO string (JSON.parse behavior)

    constants:
    - CATEGORY_COLORS contains all 8 default category color hex values
    - DEFAULT_CATEGORIES array has exactly 8 entries (6 expense + 2 income)
    - Each default category has name, icon, color, type fields
    - INCOME_SOURCE_TYPES contains EMPLOYMENT, FREELANCE, OTHER
    - PAYMENT_METHODS_DISPLAY maps enum values to Spanish display names
    - FREQUENCY_DISPLAY maps enum values to Spanish display names
  </behavior>
  <action>
  **Step 0: Install missing dev dependency.**
  ```bash
  npm install --save-dev @vitest/coverage-v8
  ```

  **Step 1: Create `src/types/index.ts`.**
  Re-export all Prisma model types (as type-only exports) and all enum types (as runtime exports) from `../../generated/prisma/client`. Create `Serialized*` type variants for models with BigInt fields (Transaction, IncomeSource, Debt, Budget, MonthlySummary) -- each Omits the BigInt fields and replaces them with `string`. Use mapped types to avoid repetition where possible.

  Note: IncomeSource.type is `String` in Prisma (not an enum), so it stays `string` in the serialized variant too. Only `defaultAmount` needs BigInt-to-string conversion.

  **Step 2: Create `src/lib/serialize.ts`.**
  Single function `serializeBigInts<T>(obj: T): T` using JSON.stringify with a replacer that converts `typeof value === 'bigint'` to `value.toString()`, then JSON.parse to restore. This is the exact pattern from CLAUDE.md and RESEARCH.md. Add a brief JSDoc comment.

  **Step 3: Create `src/lib/serialize.test.ts` (write tests FIRST, then verify they fail, then implementation passes).**
  Test cases per behavior block above. Also test: empty object, empty array, undefined passthrough, string/number/boolean passthrough.

  **Step 4: Create `src/lib/constants.ts`.**
  Define and export:
  - `DEFAULT_CATEGORIES`: Array of `{ name, icon, color, type }` objects matching the seed data from CLAUDE.md (6 expense: Comida/utensils/#fb923c, Servicios/zap/#60a5fa, Entretenimiento/clapperboard/#a78bfa, Suscripciones/smartphone/#f472b6, Transporte/car/#fbbf24, Otros/package/#94a3b8; 2 income: Empleo/briefcase/#34d399, Freelance/laptop/#22d3ee). Import `CategoryType` from `@/types` for the `type` field.
  - `CATEGORY_COLORS`: Object mapping category names to their hex colors (derived from DEFAULT_CATEGORIES).
  - `INCOME_SOURCE_TYPES`: `['EMPLOYMENT', 'FREELANCE', 'OTHER'] as const`
  - `PAYMENT_METHODS_DISPLAY`: Record mapping PaymentMethod enum values to Spanish display names (EFECTIVO -> "Efectivo", DEBITO -> "Debito", CREDITO -> "Credito", TRANSFERENCIA -> "Transferencia")
  - `FREQUENCY_DISPLAY`: Record mapping Frequency enum values to Spanish display names (QUINCENAL -> "Quincenal", MENSUAL -> "Mensual", SEMANAL -> "Semanal", VARIABLE -> "Variable")

  **Step 5: Create `src/lib/constants.test.ts`.**
  Smoke tests: DEFAULT_CATEGORIES has length 8, each has required fields, CATEGORY_COLORS has 8 entries, INCOME_SOURCE_TYPES has 3 entries, display maps have correct keys.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npx vitest run src/lib/serialize.test.ts src/lib/constants.test.ts --reporter=verbose</automated>
  </verify>
  <done>
  - @vitest/coverage-v8 installed in devDependencies
  - src/types/index.ts exports all Prisma types/enums + Serialized* variants
  - serializeBigInts handles nested BigInt, arrays, null, mixed types (all tests pass)
  - constants.ts exports DEFAULT_CATEGORIES (8), CATEGORY_COLORS, INCOME_SOURCE_TYPES, PAYMENT_METHODS_DISPLAY, FREQUENCY_DISPLAY (all smoke tests pass)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create utility functions (formatMoney, toCents, parseCents, formatRate, formatUnitAmount, cn) with tests</name>
  <files>src/lib/utils.ts, src/lib/utils.test.ts</files>
  <behavior>
    formatMoney:
    - formatMoney("15075") returns "$150.75" (MXN format)
    - formatMoney("0") returns "$0.00"
    - formatMoney("1") returns "$0.01"
    - formatMoney("100") returns "$1.00"
    - formatMoney("999999999") handles large amounts without precision loss

    toCents (string-split, NO float math):
    - toCents("150.75") returns "15075"
    - toCents("1500") returns "150000"
    - toCents("0") returns "0"
    - toCents("0.01") returns "1"
    - toCents(".50") returns "50" (leading dot)
    - toCents("1500.") returns "150000" (trailing dot)
    - toCents("99.999") returns "9999" (truncates beyond 2 decimals)
    - toCents("99.9") returns "9990" (pads to 2 decimals)
    - toCents("") throws Error
    - toCents("abc") throws Error
    - toCents("12.34.56") throws Error (multiple dots)

    parseCents:
    - parseCents("15075") returns 15075n
    - parseCents("0") returns 0n
    - parseCents("invalid") throws

    formatRate:
    - formatRate(4500) returns "45.00%"
    - formatRate(0) returns "0.00%"
    - formatRate(50) returns "0.50%"
    - formatRate(10000) returns "100.00%"

    formatUnitAmount:
    - formatUnitAmount("50000123456", 6) returns locale-formatted string with 6 decimal places
    - formatUnitAmount("82900", 2) returns locale-formatted "829.00"

    cn:
    - cn("px-4 py-2", "px-6") returns "py-2 px-6" (merges conflicting Tailwind)
    - cn("text-red", false && "bg-blue") handles falsy values
    - cn("p-4", undefined, "m-2") handles undefined
  </behavior>
  <action>
  **CRITICAL: toCents takes string input, uses string-split parsing -- NO float math. This overrides CLAUDE.md's toCents(pesos: number) signature per locked user decision.**

  **Step 1: Write `src/lib/utils.test.ts` FIRST with all test cases from behavior block.** Organize with describe blocks per function. Use the exact test cases listed plus these additional edge cases:
  - toCents with whitespace: `toCents("  150.75  ")` should return "15075" (trim first)
  - toCents with negative: `toCents("-5")` should throw (no negative amounts)
  - formatMoney with negative cents string: verify behavior (Intl handles it)

  **Step 2: Create `src/lib/utils.ts` with all 6 functions.**

  `formatMoney(centsStr: string): string` -- Use `Intl.NumberFormat('es-MX', { style: 'currency', currency: 'MXN' }).format(Number(centsStr) / 100)`. Add JSDoc.

  `toCents(input: string): string` -- String-split implementation per RESEARCH.md Pattern 2. Steps: trim, reject empty, split on '.', reject >2 parts, validate integer part is digits-only, validate fractional part is digits-only (if present), pad fractional to 2 chars or truncate to 2, concatenate, strip leading zeros. Reject negative inputs (check for leading '-'). Add a code comment explaining this overrides CLAUDE.md's number-based signature to prevent float contamination.

  `parseCents(value: string): bigint` -- `BigInt(value)`. Simple wrapper. Add JSDoc.

  `formatRate(bps: number): string` -- `` `${(bps / 100).toFixed(2)}%` ``. Add JSDoc.

  `formatUnitAmount(amountStr: string, precision: number): string` -- `(Number(amountStr) / Math.pow(10, precision)).toLocaleString('es-MX', { minimumFractionDigits: precision, maximumFractionDigits: precision })`. Add JSDoc.

  `cn(...inputs: ClassValue[]): string` -- Import `clsx` and `twMerge` from their packages, return `twMerge(clsx(inputs))`. Add JSDoc.

  **Step 3: Run tests, verify all pass.** Then run `npx vitest run src/lib/utils.test.ts --coverage` to check coverage of utils.ts specifically.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npx vitest run src/lib/utils.test.ts --reporter=verbose</automated>
  </verify>
  <done>
  - formatMoney correctly formats centavo strings to MXN currency display
  - toCents uses pure string-split parsing with zero float math, handles all edge cases (empty, dots, padding, truncation, whitespace, negative rejection)
  - parseCents converts string to BigInt
  - formatRate converts basis points to percentage string
  - formatUnitAmount respects variable precision for asset display
  - cn merges conflicting Tailwind classes via clsx + tailwind-merge
  - All tests pass with zero failures
  </done>
</task>

</tasks>

<verification>
```bash
# All unit tests pass
npx vitest run src/lib/serialize.test.ts src/lib/utils.test.ts src/lib/constants.test.ts --reporter=verbose

# Build still passes (no type errors introduced)
npm run build

# Lint passes
npm run lint
```
</verification>

<success_criteria>
- serializeBigInts handles nested, array, null, and mixed-type objects (tests prove it)
- formatMoney, toCents, parseCents, formatRate, formatUnitAmount all return correct values for happy path and edge cases
- toCents uses string-split parsing with NO float arithmetic anywhere in its implementation
- cn merges conflicting Tailwind classes
- types/index.ts re-exports all Prisma types + Serialized* variants
- constants.ts provides category defaults, display name maps
- @vitest/coverage-v8 installed and coverage reports generate
- npm run build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-foundation-libraries/03-01-SUMMARY.md`
</output>
