---
phase: 15-ux-interaction-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [UX_RULES.md]
autonomous: true
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "Section 3 documents icon-only bottom tab bar with 5 items (no text labels), 4px chartreuse dot active indicator 8px below icon, and circular 48px accent FAB"
    - "Section 10 documents monospaced amount display with muted smaller dollar sign in --color-text-tertiary, off-white digits, and direction-based color coding"
    - "Section 4.1 documents transaction bottom sheet (85vh) with drag handle, dot-matrix hero amount area, toggle pills, category circular icon grid with accent ring, custom dark numpad, and checkmark success animation"
    - "Zero references to old hex values (#22d3ee, #0a0f1a, #111827) remain in sections 3, 4, and 10 after edits"
  artifacts:
    - path: "UX_RULES.md"
      provides: "Updated sections 3, 4.1, and 10 with Glyph Finance interaction patterns"
      contains: "--color-accent"
  key_links:
    - from: "UX_RULES.md section 3 (nav)"
      to: "STYLE_GUIDE.md > Status Dot spec"
      via: "Cross-reference to Identidad Visual > Status Dot for active tab indicator"
      pattern: "Status Dot|Identidad Visual|status-pulse"
    - from: "UX_RULES.md section 4.1 (transaction flow)"
      to: "STYLE_GUIDE.md > Modales (bottom sheet) + Dot-Matrix + Inputs"
      via: "Cross-references to component specs"
      pattern: "bottom sheet|dot-matrix|Componentes Base"
    - from: "UX_RULES.md section 10 (finance patterns)"
      to: "STYLE_GUIDE.md > Tipografia > Numeros Financieros"
      via: "Cross-reference to monospaced display spec"
      pattern: "IBM Plex Mono|Numeros Financieros|font-mono"
---

<objective>
Rewrite UX_RULES.md sections 3 (Layout y Navegacion), 4.1 (Registro Rapido de Transaccion), and 10 (Patrones Especificos de Finanzas) with Glyph Finance interaction patterns.

Purpose: These three sections define the core interaction model -- how users navigate, how amounts are displayed, and how the most critical flow (transaction registration) works. They must reference new tokens, component specs, and visual signatures from Phases 12-14.

Output: UX_RULES.md with sections 3, 4.1, and 10 fully rewritten using Glyph Finance tokens and patterns.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ux-interaction-patterns/15-CONTEXT.md
@STYLE_GUIDE.md
@UX_RULES.md

<interfaces>
<!-- Key specs from STYLE_GUIDE.md that the executor needs for cross-references -->

Tokens to use (from STYLE_GUIDE.md Paleta de Color):
- --color-bg: #000000, --color-surface: #0A0A0A, --color-surface-elevated: #141414
- --color-border-divider: #222222, --color-dot-matrix: #1E1E1E
- --color-text-primary: #E8E8E8, --color-text-secondary: #999999, --color-text-tertiary: #666666
- --color-accent: #CCFF00, --color-positive: #00E676, --color-negative: #FF3333

Component specs to cross-reference (NOT duplicate):
- Modales > Mobile (bottom sheet): 85vh, handle 40px x 4px, --radius-xl top corners
- Inputs > underline-only, floating labels, amount prefix treatment
- Buttons > Toggle pills: chartreuse active / ghost inactive
- Identidad Visual > Status Dot: 4px, chartreuse, status-pulse 2.5s
- Identidad Visual > Textura Dot-Matrix: SVG background on hero cards
- Identidad Visual > Display Financiero Monoespaciado: IBM Plex Mono, muted $, tabular-nums
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite section 3 (navigation) and section 10 (finance patterns)</name>
  <files>UX_RULES.md</files>
  <action>
Rewrite two sections of UX_RULES.md. Keep section numbers, headings, and Spanish language. Replace all old hex references with Glyph Finance token variable names.

**Section 3 (Layout y Navegacion):**

3.1 Sidebar (Desktop >= 1024px):
- Keep the existing structure but update token references.
- Active item: fondo `--color-accent` al 15% opacity y texto en `--color-accent`. Remove any old color references.
- Inactive items: `--color-text-secondary` (#999999), hover to `--color-text-primary`.

3.2 Bottom Tab Bar (Mobile <768px):
- Rewrite completely per CONTEXT.md locked decisions:
  - 5 items: Inicio, Movimientos, Deudas, Presupuesto, Mas. (NOT Dashboard -- use "Inicio")
  - Icon-only: NO text labels. Remove the "Iconos de 20px con label de 10px debajo" line.
  - Icons: 20px, color `--color-text-secondary` (#999999) for inactive.
  - Active state: 4px chartreuse dot (`--color-accent`) positioned 8px below the active icon. No icon color change. Cross-reference Identidad Visual > Status Dot for the dot spec.
  - FAB stays separate: circular 48px, fondo `--color-accent`, positioned floating above the tab bar center. For transaction creation only.
  - "Mas" overflow: Use a slide-up bottom sheet (matching the STYLE_GUIDE.md bottom sheet modal spec) containing remaining items: Ingresos, Historial, Categorias, Configuracion. Icons + label in the sheet. This is Claude's discretion choice.
- Height: 64px. Position: fixed bottom.

3.3 Header de Pagina:
- Update text style references. Title in nivel Heading (20px, Satoshi Semibold). Subtitle uses `--color-text-secondary`. Remove any mention of specific old hex colors.

3.4 Indicador de Periodo:
- Keep behavior. Add: the active period has a status dot (4px chartreuse, pulsing per status-pulse spec) next to it. Cross-reference Identidad Visual > Status Dot.

**Section 10 (Patrones Especificos de Finanzas):**

10.1 Codigo de Color en Montos:
- Update color references: verde = `--color-positive`, rojo = `--color-negative`, blanco = `--color-text-primary`, acento = `--color-accent` (chartreuse, NOT cyan).
- Add rule: All financial amounts use IBM Plex Mono (`--font-mono`) with `font-variant-numeric: tabular-nums`. Dollar sign "$" rendered at smaller size in `--color-text-tertiary` (muted). Cross-reference STYLE_GUIDE.md > Tipografia > Numeros Financieros for full spec.

10.2 Signos en Transacciones:
- Keep rules. Update: amounts in IBM Plex Mono. Positive prefix "+" in `--color-positive`, negative prefix "-" in `--color-negative`.

10.3 Presupuesto: Sistema de Semaforo:
- Update to reference battery-bar: "Las barras de progreso segmentadas tipo battery-bar cambian de color..."
- Colors: `--color-accent` (chartreuse, 0-79%), `--color-warning` (80-99%), `--color-negative` (100%+).
- Cross-reference STYLE_GUIDE.md > Componentes Base > Progress Bars (Battery-Bar).

10.4 Deudas: Indicadores de Salud:
- Keep thresholds. Update token variable names. Reference battery-bar for utilization display.

10.5 Periodo Cerrado:
- Update: banner uses `--color-info-subtle` fondo, `--color-info` text. Disabled buttons at opacity 50%. FAB se oculta. Ghost button "Reabrir periodo" in `--color-text-secondary`.

Replace ALL old hex values (#22d3ee, #0a0f1a, #111827, #1e293b, etc.) with Glyph Finance token variable names within these sections.
  </action>
  <verify>
    <automated>grep -n "#22d3ee\|#0a0f1a\|#111827\|#1e293b\|cyan\|DM Sans" UX_RULES.md | head -20; echo "---"; grep -c "color-accent\|color-positive\|color-negative\|color-text-secondary\|font-mono\|IBM Plex Mono\|battery-bar\|Status Dot" UX_RULES.md</automated>
  </verify>
  <done>Sections 3 and 10 use only Glyph Finance tokens. Zero old hex references in those sections. Bottom tab bar is icon-only with dot indicator. Amount display references monospaced spec. Budget/debt use battery-bar references.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite section 4.1 (transaction flow) with bottom sheet, numpad, and category grid</name>
  <files>UX_RULES.md</files>
  <action>
Rewrite section 4.1 (Registro Rapido de Transaccion) completely. This is the most critical flow in the app. Keep Spanish language, keep the "4 steps" framing.

**New section 4.1 content:**

Opening line: Keep the criticality statement ("Este es el flujo mas critico de toda la app").

**Flujo optimo (4 pasos):**

1. Tap en FAB circular chartreuse → Abre bottom sheet (85% de la pantalla, desliza hacia arriba).
   - Spec: drag handle (40px ancho, 4px alto, `--color-border-divider`) centrado en la parte superior.
   - Boton X en esquina superior izquierda para cerrar. Boton "GUARDAR" en esquina superior derecha en `--color-accent` (chartreuse), nivel Label (12px, uppercase, letter-spacing +2px).
   - Referencia a STYLE_GUIDE.md > Componentes Base > Modales > Mobile (bottom sheet).

2. Area hero de monto con dot-matrix:
   - Zona superior del sheet con fondo textura dot-matrix (referencia a STYLE_GUIDE.md > Identidad Visual > Textura Dot-Matrix).
   - Input de monto grande, monoespaciado (IBM Plex Mono, Display level ~36px), "$" silenciado en `--color-text-tertiary`.
   - Debajo del monto: toggle pills Gasto/Ingreso (default: Gasto). Estilo: referencia a STYLE_GUIDE.md > Componentes Base > Buttons > Toggle pills.

3. Selector de categoria circular con ring de acento:
   - Layout: Grid de 4 columnas x 2 filas para las 8 categorias default. Claude's discretion: 4x2 grid is better than horizontal scroll because all options are visible simultaneously -- no hidden options.
   - Cada categoria: icono circular (40px) con contenedor de color de categoria al 12% de opacidad. Label debajo en nivel Meta (11px, `--color-text-secondary`).
   - Estado seleccionado: ring de 2px en `--color-accent` (chartreuse) alrededor del circulo del icono.
   - Referencia a STYLE_GUIDE.md > Iconografia > Contenedores de Icono para spec de tamano y color.

4. Custom dark numpad (below category grid):
   - Grid layout: 4 columnas x 4 filas.
   - Row 1: 1, 2, 3, backspace icon (lucide `Delete`)
   - Row 2: 4, 5, 6, (empty or period ".")
   - Row 3: 7, 8, 9, 0
   - Row 4: (empty), ".", 0, "OK" (if no separate save button) -- OR simpler: 3x4 grid with numbers only + backspace + period.
   - Claude's discretion on exact layout: Use a 4-column grid. Columns 1-3 are digit keys (1-9, period, 0, 00). Column 4 is backspace at top, remaining cells empty or period.
   - Surface: `--color-surface-elevated` (#141414) keys on `--color-surface` (#0A0A0A) background.
   - Key specs: min touch target 48px, font IBM Plex Mono nivel Heading (20px), `--color-text-primary`.
   - Key press feedback: background `--color-surface-hover` (#1A1A1A), transition 100ms.

**Campos opcionales:**
- Colapsados por default en una seccion "Mas detalles" debajo del numpad.
- Descripcion, forma de pago, notas, fecha (default: hoy).
- Inputs estilo underline-only (referencia a STYLE_GUIDE.md > Componentes Base > Inputs).

**Comportamiento al guardar:**
- Tap "GUARDAR" → Brief checkmark animation (200ms): the save button text transitions to a check icon (lucide `Check`) in `--color-positive`, then the sheet closes with slide-down animation (300ms).
- Toast de confirmacion aparece: formato "Comida -$150.00" con icono check. Referencia a seccion 8.1 Toasts.
- Si la lista de movimientos esta visible, el nuevo item aparece con efecto pixel-dissolve (referencia a STYLE_GUIDE.md > Identidad Visual > Micro-Animacion Pixel-Dissolve).

Replace all old hex values or old-style references in section 4.1.
  </action>
  <verify>
    <automated>grep -n "bottom sheet\|dot-matrix\|numpad\|IBM Plex Mono\|battery-bar\|category.*grid\|GUARDAR\|checkmark\|pixel-dissolve" UX_RULES.md | head -20; echo "---"; grep -n "#22d3ee\|#0a0f1a\|#111827" UX_RULES.md | head -5</automated>
  </verify>
  <done>Section 4.1 documents the complete transaction flow: bottom sheet opening, dot-matrix hero amount area, toggle pills, 4x2 category circular icon grid with accent ring, custom dark numpad with exact key layout, checkmark save animation, and pixel-dissolve for new list items. All cross-references point to STYLE_GUIDE.md specs. Zero old hex references.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -n "#22d3ee\|#0a0f1a\|#111827\|#1e293b\|cyan\|DM Sans" UX_RULES.md` returns zero matches in sections 3, 4.1, and 10
2. Sections 3.2, 4.1, and 10.1 contain cross-references to STYLE_GUIDE.md specs
3. UX_RULES.md remains valid markdown with consistent section numbering (1-11)
4. Spanish language maintained throughout
</verification>

<success_criteria>
- Navigation section describes icon-only bottom tab bar with dot indicator and circular FAB
- Transaction flow describes bottom sheet with dot-matrix hero, category grid, custom numpad, and save animation
- Finance patterns section references monospaced display, battery-bar, and Glyph Finance color tokens
- All three sections use STYLE_GUIDE.md cross-references instead of duplicating specs
- Zero legacy hex color values in sections 3, 4, and 10
</success_criteria>

<output>
After completion, create `.planning/phases/15-ux-interaction-patterns/15-01-SUMMARY.md`
</output>
