---
phase: 23-layout-grid-responsiveness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/KPIGrid.tsx
  - src/components/income/IncomeSummaryCards.tsx
  - src/app/page.tsx
  - src/components/debts/DebtForm.tsx
  - src/components/income/IncomeSourceForm.tsx
  - src/components/categories/CategoryForm.tsx
  - src/components/transactions/TransactionForm.tsx
autonomous: true
requirements: [RESP-04, RESP-05]

must_haves:
  truths:
    - "KPI cards stack to single column on very narrow screens (320px), show 2 columns at sm, and 3 columns at md"
    - "Income summary cards stack to single column on narrow mobile, show 2 at sm, and 4 at md"
    - "Dashboard chart grids switch to 2-column layout at md (768px) instead of lg (1024px)"
    - "All form grids stack fields to single column on mobile and expand to multi-column on tablet+"
  artifacts:
    - path: "src/components/dashboard/KPIGrid.tsx"
      provides: "KPI grid with 3-tier responsive breakpoints"
      contains: "sm:grid-cols-2"
    - path: "src/components/income/IncomeSummaryCards.tsx"
      provides: "Income summary grid with sm bridge breakpoint"
      contains: "sm:grid-cols-2"
    - path: "src/components/debts/DebtForm.tsx"
      provides: "Debt form with responsive grids"
      contains: "sm:grid-cols-2"
    - path: "src/components/categories/CategoryForm.tsx"
      provides: "Category icon picker with responsive grid"
      contains: "sm:grid-cols-4"
  key_links:
    - from: "src/components/dashboard/KPIGrid.tsx"
      to: "src/app/page.tsx"
      via: "KPI grid rendered inside dashboard page"
      pattern: "KPIGrid"
    - from: "src/components/income/IncomeSummaryCards.tsx"
      to: "src/app/ingresos/"
      via: "Income summary rendered on income page"
      pattern: "IncomeSummaryCards"
---

<objective>
Add missing responsive breakpoints to dashboard/KPI/income grids and make all form grids responsive on mobile.

Purpose: Ensure balanced column distribution at every standard breakpoint (sm, md, lg) for data grids and proper single-column stacking for forms on narrow screens.
Output: 7 files with corrected Tailwind responsive classes.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-layout-grid-responsiveness/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intermediate breakpoints to dashboard and income grids</name>
  <files>src/components/dashboard/KPIGrid.tsx, src/components/income/IncomeSummaryCards.tsx, src/app/page.tsx</files>
  <action>
All changes are Tailwind class updates only. No logic changes.

**KPIGrid.tsx** (line 84):
- Current: `className="grid grid-cols-2 gap-3 md:grid-cols-3"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-3"`
- Fixes RESP-04: stacks to 1 column on very narrow (320px), 2 at 640px, 3 at 768px

**IncomeSummaryCards.tsx** (line 21):
- Current: `className="grid grid-cols-2 md:grid-cols-4 gap-3 mb-8"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-3 mb-8"`
- Fixes RESP-04: stacks to 1 column on narrow mobile, 2 at sm, full 4 at md

**src/app/page.tsx** — charts section (line 54):
- Current: `className="grid grid-cols-1 lg:grid-cols-2 gap-3"`
- Change to: `className="grid grid-cols-1 md:grid-cols-2 gap-3"`
- Fixes RESP-04: show 2-column chart layout at 768px instead of 1024px

**src/app/page.tsx** — trend/recent section (line 62):
- Current: `className="grid grid-cols-1 lg:grid-cols-2 gap-3"`
- Change to: `className="grid grid-cols-1 md:grid-cols-2 gap-3"`
- Same fix for the second chart grid row

NOTE: Plan 01 also modifies src/app/page.tsx (adding max-w-7xl to the wrapper div on line 40). This task modifies different lines (54 and 62). If Plan 01 runs first, the line numbers may shift by 0 lines (the wrapper div change is on a different line). If this plan runs first, Plan 01's line numbers are unaffected. Both plans touch independent class attributes on different elements.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>KPIGrid uses 3-tier breakpoints (1/2/3 cols). IncomeSummaryCards adds sm bridge (1/2/4 cols). Dashboard chart grids switch at md. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Make all form grids responsive</name>
  <files>src/components/debts/DebtForm.tsx, src/components/income/IncomeSourceForm.tsx, src/components/categories/CategoryForm.tsx, src/components/transactions/TransactionForm.tsx</files>
  <action>
All changes are Tailwind class updates only. No logic changes. Pattern: add grid-cols-1 as base with sm: prefix for the target column count on wider screens.

**DebtForm.tsx** — type radio group (line 209):
- Current: `className="grid grid-cols-2 gap-2"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 gap-2"`
- 4 debt types stack vertically on mobile, 2-column at 640px+

**DebtForm.tsx** — cut-off/payment day grid (line 271):
- Current: `className="grid grid-cols-2 gap-3"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 gap-3"`
- Day inputs stack on mobile, side-by-side at 640px+

**IncomeSourceForm.tsx** — frequency radio group (line 171):
- Current: `className="grid grid-cols-2 gap-2"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 gap-2"`
- Frequency options stack on mobile

**IncomeSourceForm.tsx** — type radio group (line 198):
- Current: `className="grid grid-cols-3 gap-2"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-3 gap-2"`
- 3 type options stack on mobile, 3-column at 640px+

**CategoryForm.tsx** — icon picker grid (line 149):
- Current: `className="grid grid-cols-4 gap-2"`
- Change to: `className="grid grid-cols-2 sm:grid-cols-4 gap-2"`
- 16 icons show as 2-column on mobile (still compact), 4-column at 640px+
- Using grid-cols-2 instead of grid-cols-1 because 16 individual icon tiles in single column would be excessively long

**TransactionForm.tsx** — category grid (line 228):
- Current: `className="grid grid-cols-4 gap-3"`
- Change to: `className="grid grid-cols-3 sm:grid-cols-4 gap-3"`
- Category circles show 3 per row on mobile (keeps compact), 4 at 640px+
- Using grid-cols-3 instead of grid-cols-1 because category circles are small and benefit from multi-column even on mobile

**TransactionForm.tsx** — payment method grid (line 307):
- Current: `className="grid grid-cols-2 gap-2"`
- No change needed. 2 columns works well on mobile for pill buttons (they're short text).
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>All form grids have responsive breakpoints. Forms stack to fewer columns on mobile and expand on tablet+. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `npm test` passes (no test changes needed -- CSS-only modifications)
- Grep confirms: `grep -n "sm:grid-cols" src/components/dashboard/KPIGrid.tsx src/components/income/IncomeSummaryCards.tsx` returns matches
- Grep confirms: `grep -n "sm:grid-cols" src/components/debts/DebtForm.tsx src/components/income/IncomeSourceForm.tsx src/components/categories/CategoryForm.tsx src/components/transactions/TransactionForm.tsx` returns matches
</verification>

<success_criteria>
- KPIGrid uses grid-cols-1 sm:grid-cols-2 md:grid-cols-3 (RESP-04)
- IncomeSummaryCards uses grid-cols-1 sm:grid-cols-2 md:grid-cols-4 (RESP-04)
- Dashboard chart grids use md:grid-cols-2 instead of lg:grid-cols-2 (RESP-04)
- DebtForm type and day grids use sm:grid-cols-2 (RESP-05)
- IncomeSourceForm frequency and type grids use responsive prefixes (RESP-05)
- CategoryForm icon grid uses grid-cols-2 sm:grid-cols-4 (RESP-05)
- TransactionForm category grid uses grid-cols-3 sm:grid-cols-4 (RESP-05)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-layout-grid-responsiveness/23-02-SUMMARY.md`
</output>
