---
phase: 04-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/DynamicIcon.tsx
  - src/components/ui/DynamicIcon.test.tsx
  - src/components/ui/Modal.tsx
  - src/components/ui/Modal.test.tsx
  - src/lib/constants.ts
  - src/lib/constants.test.ts
autonomous: true
requirements:
  - LAYOUT-07

must_haves:
  truths:
    - "DynamicIcon renders the correct Lucide icon for all 8 default category icon names"
    - "DynamicIcon renders a fallback Package icon for unknown icon names"
    - "Modal/Sheet primitive opens and closes with backdrop, renders as bottom sheet on mobile and centered modal on desktop"
    - "Nav item constants define all sidebar items, mobile tab items, more menu items, period-aware routes, and Spanish month names"
  artifacts:
    - path: "src/components/ui/DynamicIcon.tsx"
      provides: "Lucide icon resolver by DB string name"
      exports: ["default"]
      contains: "ICON_MAP"
    - path: "src/components/ui/DynamicIcon.test.tsx"
      provides: "Tests for icon resolution and fallback"
      min_lines: 20
    - path: "src/components/ui/Modal.tsx"
      provides: "Responsive modal/sheet primitive"
      exports: ["default"]
      contains: "use client"
    - path: "src/components/ui/Modal.test.tsx"
      provides: "Tests for Modal render, open/close, Escape key, title rendering"
      min_lines: 20
    - path: "src/lib/constants.ts"
      provides: "Nav items, period-aware routes, month names"
      contains: "SIDEBAR_NAV_ITEMS"
    - path: "src/lib/constants.test.ts"
      provides: "Tests for nav item structure"
      min_lines: 15
  key_links:
    - from: "src/components/ui/DynamicIcon.tsx"
      to: "lucide-react"
      via: "static named imports"
      pattern: "import.*from 'lucide-react'"
    - from: "src/lib/constants.ts"
      to: "src/components/ui/DynamicIcon.tsx"
      via: "icon name strings match ICON_MAP keys"
      pattern: "icon: '"
---

<objective>
Create the interface contracts and UI primitives that all subsequent layout components depend on: DynamicIcon for rendering Lucide icons by database string name, navigation constants for sidebar/mobile nav item definitions (Spanish labels, i18n-ready), and a responsive Modal/Sheet primitive for the FAB and future forms.

Purpose: These are the foundational contracts -- DynamicIcon resolves icon names stored in the DB, nav constants define routes and labels shared across Sidebar and MobileNav, and Modal provides the responsive overlay pattern (bottom sheet on mobile, centered modal on desktop). Building these first prevents downstream components from defining their own ad-hoc structures.

Output: 3 new component/module files + 3 test files. Subsequent plans consume these directly.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-layout-shell/04-CONTEXT.md
@.planning/phases/04-layout-shell/04-RESEARCH.md

@src/lib/constants.ts
@src/lib/utils.ts
@src/app/globals.css

<interfaces>
<!-- Existing code the executor needs -->

From src/lib/constants.ts (will be MODIFIED -- add nav items):
```typescript
import { CategoryType } from '@/types'
// ... existing DEFAULT_CATEGORIES, CATEGORY_COLORS, INCOME_SOURCE_TYPES, etc.
// ADD: NavItem interface, SIDEBAR_NAV_ITEMS, MOBILE_TAB_ITEMS, MORE_MENU_ITEMS,
//      PERIOD_AWARE_ROUTES, MONTH_NAMES_ES
```

From src/lib/utils.ts (already exists, used by components):
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/app/globals.css (color tokens available via Tailwind):
```css
--color-accent: #22d3ee;
--color-bg-card: #111827;
--color-bg-elevated: #1e293b;
--color-border: #1e293b;
--color-border-light: #334155;
--color-text-primary: #e2e8f0;
--color-text-secondary: #94a3b8;
--color-text-muted: #64748b;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DynamicIcon component and nav constants with tests</name>
  <files>src/components/ui/DynamicIcon.tsx, src/components/ui/DynamicIcon.test.tsx, src/lib/constants.ts, src/lib/constants.test.ts</files>
  <behavior>
    - DynamicIcon("utensils") renders Utensils icon component
    - DynamicIcon("zap") renders Zap, "clapperboard" renders Clapperboard, etc. for all 8 default category icons
    - DynamicIcon("credit-card") renders CreditCard (hyphenated name mapping)
    - DynamicIcon("unknown-icon") renders Package as fallback
    - DynamicIcon passes through size, className, and other LucideProps
    - SIDEBAR_NAV_ITEMS has 6 items with labels: Inicio, Movimientos, Deudas, Presupuesto, Ingresos, Historial
    - MOBILE_TAB_ITEMS has 4 items: Inicio, Movimientos, Deudas, Presupuesto (5th "Mas" is handled in MobileNav directly)
    - MORE_MENU_ITEMS has 3 items: Ingresos, Historial, Configuracion
    - PERIOD_AWARE_ROUTES contains exactly: /, /movimientos, /presupuesto, /historial
    - MONTH_NAMES_ES has 12 Spanish month names starting with Enero
    - Every icon name string in nav items exists as a key in DynamicIcon's ICON_MAP
  </behavior>
  <action>
    1. Create `src/components/ui/DynamicIcon.tsx`:
       - Static import map of ~20 Lucide icons via individual named imports (NOT the `icons` barrel export which imports all 1,694 icons and destroys tree-shaking)
       - Required icons: Utensils, Zap, Clapperboard, Smartphone, Car, Package, Briefcase, Laptop (8 default category icons)
       - Extra icons for nav + custom categories: LayoutDashboard, ArrowLeftRight, CreditCard, PiggyBank, Banknote, History, Settings, Home, Receipt, ShoppingCart, Heart, GraduationCap, Plane, Gift, Coffee, Dumbbell, Pill, MoreHorizontal, Plus, ChevronLeft, ChevronRight, Lock
       - Map keys are lowercase/hyphenated strings matching DB storage: "utensils", "credit-card", "layout-dashboard", etc.
       - Props interface extends LucideProps with required `name: string`
       - Fallback to Package icon when name not found in map
       - Export the ICON_MAP as a named export so tests and constants can validate icon names exist

    2. Add to `src/lib/constants.ts` (preserve all existing exports):
       - `NavItem` interface: { label: string, href: string, icon: string, periodAware?: boolean }
       - `SIDEBAR_NAV_ITEMS: NavItem[]` with 6 items per CONTEXT.md locked decisions:
         Inicio (/), Movimientos (/movimientos), Deudas (/deudas), Presupuesto (/presupuesto), Ingresos (/ingresos), Historial (/historial)
       - Set periodAware: true on Inicio, Movimientos, Presupuesto, Historial
       - Icon names: layout-dashboard, arrow-left-right, credit-card, piggy-bank, banknote, history
       - `MOBILE_TAB_ITEMS: NavItem[]` with 4 items (Inicio, Movimientos, Deudas, Presupuesto) -- 5th "Mas" is handled specially in MobileNav component
       - `MORE_MENU_ITEMS: NavItem[]` with 3 items: Ingresos (/ingresos, banknote), Historial (/historial, history), Configuracion (/configuracion, settings)
       - `PERIOD_AWARE_ROUTES: string[]` = ['/', '/movimientos', '/presupuesto', '/historial']
       - `MONTH_NAMES_ES` as const array of 12 Spanish month names

    3. Write tests first per TDD:
       - `src/components/ui/DynamicIcon.test.tsx`: Test that each of the 8 default category icon names resolves (not to fallback). Test that unknown name gives fallback. Test that props pass through.
       - `src/lib/constants.test.ts`: Test SIDEBAR_NAV_ITEMS count (6), MOBILE_TAB_ITEMS count (4), MORE_MENU_ITEMS count (3). Test all nav item icon names exist in DynamicIcon's ICON_MAP. Test MONTH_NAMES_ES has 12 entries starting with "Enero" and ending with "Diciembre". Test PERIOD_AWARE_ROUTES contains exactly the 4 expected paths.

    IMPORTANT: Use individual named imports from lucide-react. Do NOT import `{ icons }` from 'lucide-react'. This is a tree-shaking requirement.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npx vitest run src/components/ui/DynamicIcon.test.tsx src/lib/constants.test.ts</automated>
  </verify>
  <done>DynamicIcon resolves all 8 default category icons + nav icons + extras, falls back to Package for unknowns. Nav constants define all sidebar/mobile/more items with valid icon references. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modal/Sheet responsive primitive with tests</name>
  <files>src/components/ui/Modal.tsx, src/components/ui/Modal.test.tsx</files>
  <behavior>
    - Modal renders nothing when isOpen is false
    - Modal renders children when isOpen is true
    - Modal renders title in header when title prop is provided
    - Modal does not render title header when title prop is omitted
    - Modal calls onClose when Escape key is pressed
    - Modal calls onClose when backdrop is clicked
    - Modal renders close button (X icon) when title is provided
  </behavior>
  <action>
    Create `src/components/ui/Modal.tsx` as a Client Component ("use client"):

    Props interface `ModalProps`:
    - `isOpen: boolean` -- controls visibility
    - `onClose: () => void` -- called on backdrop click or Escape key
    - `children: React.ReactNode` -- modal content
    - `title?: string` -- optional header title
    - `maxWidth?: string` -- default "max-w-[480px]" for desktop modal

    Behavior:
    - **Mobile (<md):** Renders as bottom sheet sliding up from bottom. Fixed to `inset-x-0 bottom-0`, max height 90vh, rounded-t-xl border-t, drag handle indicator bar at top (w-10 h-1 bg-border-light rounded-full).
    - **Desktop (md+):** Renders as centered modal. Fixed inset-0 with flex items-center justify-center, max-width 480px per CONTEXT.md, max-height 85vh, rounded-xl border, padding 28px per STYLE_GUIDE.
    - Both render the SAME markup (CSS-based responsive, NOT JS viewport detection -- prevents hydration mismatch per RESEARCH.md pitfall #3).
    - Backdrop: fixed inset-0, bg-black/70, backdrop-blur-sm. Click closes.
    - Animations: CSS transitions via Tailwind classes:
      - Sheet: translate-y-0 (open) / translate-y-full (closed), duration-300 ease-out
      - Modal: opacity-100 scale-100 (open) / opacity-0 scale-95 (closed), duration-200
    - Escape key handler via useEffect with keydown listener
    - When `isOpen` is false, render nothing (return null) -- OR use visibility hidden with transitions. Choose the simpler approach: conditional render with transition states.
    - If `title` provided, render a header row with title text (text-lg font-semibold) and an X close button (Lucide X icon) right-aligned.

    Z-index: z-50 for both backdrop and modal/sheet (per RESEARCH.md z-index scale).

    Use `cn()` from `@/lib/utils` for conditional class merging.
    Import X icon from lucide-react for close button.

    IMPORTANT: Both mobile sheet and desktop modal markup are rendered simultaneously -- Tailwind responsive classes show/hide the appropriate one. This prevents hydration mismatches.

    Create `src/components/ui/Modal.test.tsx` (write tests FIRST per TDD):
    - Test that Modal renders nothing when isOpen=false (no children in DOM)
    - Test that Modal renders children when isOpen=true
    - Test that title renders when provided
    - Test that title header is absent when title prop is omitted
    - Test that onClose callback fires on Escape keydown (use fireEvent.keyDown)
    - Test that onClose callback fires on backdrop click (add data-testid="modal-backdrop" to backdrop div)
    - Test that close button (X) appears when title is provided
    Use @testing-library/react render + screen + fireEvent.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npx vitest run src/components/ui/Modal.test.tsx</automated>
  </verify>
  <done>Modal component exists as a Client Component with passing tests. Renders as bottom sheet on mobile and centered modal on desktop using CSS-only responsive approach. Supports open/close state, backdrop click, Escape key, optional title. All 7 test cases pass. Build passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/ui/DynamicIcon.test.tsx src/lib/constants.test.ts src/components/ui/Modal.test.tsx` -- all tests pass
- `npm run build` -- zero errors, zero warnings
- `npm run lint` -- zero warnings
- DynamicIcon.tsx does NOT import `{ icons }` from lucide-react (tree-shaking check)
- constants.ts still exports all prior constants (DEFAULT_CATEGORIES, CATEGORY_COLORS, etc.)
</verification>

<success_criteria>
- DynamicIcon renders correct icons for all 8 default category names and falls back for unknowns
- Nav constants define sidebar (6 items), mobile tabs (4 items), more menu (3 items), period-aware routes (4 paths), month names (12)
- Modal primitive works as bottom sheet on mobile and centered modal on desktop
- All tests pass (DynamicIcon, constants, Modal), build passes, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-layout-shell/04-01-SUMMARY.md`
</output>
