---
phase: 04-layout-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/components/layout/PageHeader.tsx
  - src/components/layout/PageHeader.test.tsx
  - src/components/layout/PeriodSelector.tsx
  - src/components/layout/PeriodSelector.test.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/movimientos/page.tsx
  - src/app/deudas/page.tsx
  - src/app/presupuesto/page.tsx
  - src/app/ingresos/page.tsx
  - src/app/historial/page.tsx
  - src/app/configuracion/page.tsx
  - src/app/loading.tsx
  - src/app/movimientos/loading.tsx
  - src/app/deudas/loading.tsx
  - src/app/presupuesto/loading.tsx
  - src/app/ingresos/loading.tsx
  - src/app/historial/loading.tsx
  - src/app/configuracion/loading.tsx
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-05
  - LAYOUT-06

must_haves:
  truths:
    - "Every page renders inside the responsive layout with sidebar on desktop/tablet and bottom tabs on mobile"
    - "Page header shows title in text-2xl bold, period indicator on period-aware pages, and primary action slot"
    - "Period selector shows < Abril 2026 > with arrow navigation updating URL search params (?month=X&year=Y)"
    - "Closed periods show lock icon next to month name and 'Periodo cerrado -- solo lectura' banner"
    - "All 7 routes (/, /movimientos, /deudas, /presupuesto, /ingresos, /historial, /configuracion) render placeholder pages inside the layout"
    - "Main content area has correct left margin (0 on mobile, 64px on tablet, 240px on desktop) and bottom padding on mobile (64px for tab bar)"
  artifacts:
    - path: "src/components/layout/PageHeader.tsx"
      provides: "Reusable page header with title, optional period, optional action"
      exports: ["default"]
    - path: "src/components/layout/PageHeader.test.tsx"
      provides: "Tests for PageHeader rendering: title, period slot, action slot, closed banner"
      min_lines: 20
    - path: "src/components/layout/PeriodSelector.tsx"
      provides: "Month/year navigator with URL search params"
      contains: "use client"
      exports: ["default"]
    - path: "src/components/layout/PeriodSelector.test.tsx"
      provides: "Tests for PeriodSelector rendering: month display, arrow buttons, lock icon when closed"
      min_lines: 25
    - path: "src/app/layout.tsx"
      provides: "Root layout composing Sidebar + MobileNav + FAB"
      contains: "Sidebar"
    - path: "src/app/movimientos/page.tsx"
      provides: "Movimientos placeholder page"
    - path: "src/app/deudas/page.tsx"
      provides: "Deudas placeholder page"
    - path: "src/app/presupuesto/page.tsx"
      provides: "Presupuesto placeholder page"
    - path: "src/app/ingresos/page.tsx"
      provides: "Ingresos placeholder page"
    - path: "src/app/historial/page.tsx"
      provides: "Historial placeholder page"
    - path: "src/app/configuracion/page.tsx"
      provides: "Configuracion placeholder page"
    - path: "src/app/loading.tsx"
      provides: "Root loading skeleton"
    - path: "src/app/movimientos/loading.tsx"
      provides: "Movimientos loading skeleton"
    - path: "src/app/deudas/loading.tsx"
      provides: "Deudas loading skeleton"
    - path: "src/app/presupuesto/loading.tsx"
      provides: "Presupuesto loading skeleton"
    - path: "src/app/ingresos/loading.tsx"
      provides: "Ingresos loading skeleton"
    - path: "src/app/historial/loading.tsx"
      provides: "Historial loading skeleton"
    - path: "src/app/configuracion/loading.tsx"
      provides: "Configuracion loading skeleton"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Sidebar.tsx"
      via: "import and render in body"
      pattern: "import.*Sidebar"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/MobileNav.tsx"
      via: "import and render in body"
      pattern: "import.*MobileNav"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/FAB.tsx"
      via: "import and render in body"
      pattern: "import.*FAB"
    - from: "src/components/layout/PeriodSelector.tsx"
      to: "src/lib/constants.ts"
      via: "imports MONTH_NAMES_ES"
      pattern: "MONTH_NAMES_ES"
    - from: "src/app/*/page.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "each page uses PageHeader"
      pattern: "PageHeader"
---

<objective>
Wire the complete layout shell together: modify root layout to compose Sidebar + MobileNav + FAB, create PageHeader and PeriodSelector components, and build all placeholder route pages with loading skeletons. This is the final assembly step that makes the app navigable.

Purpose: After this plan, every route in the app renders inside the responsive layout shell. Period-aware pages show the period selector. Placeholder pages exist for all nav destinations so links work. The visual checkpoint confirms the layout looks and behaves correctly across viewports.

Output: Modified root layout, 2 new layout components + 2 test files, 7 route pages, 7 loading skeletons.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-layout-shell/04-CONTEXT.md
@.planning/phases/04-layout-shell/04-RESEARCH.md
@.planning/phases/04-layout-shell/04-01-SUMMARY.md
@.planning/phases/04-layout-shell/04-02-SUMMARY.md

@src/app/layout.tsx
@src/app/globals.css
@src/lib/constants.ts

<interfaces>
<!-- All layout components from Plans 01 and 02 -->

From src/components/layout/Sidebar.tsx (Plan 02):
```typescript
'use client'
export default function Sidebar(): JSX.Element
// Fixed sidebar: hidden on mobile, 64px on tablet, 240px on desktop
```

From src/components/layout/MobileNav.tsx (Plan 02):
```typescript
'use client'
export default function MobileNav(): JSX.Element
// Fixed bottom tab bar, visible only on mobile (<md)
```

From src/components/layout/FAB.tsx (Plan 02):
```typescript
'use client'
export default function FAB(): JSX.Element
// Fixed bottom-right floating action button, always visible
```

From src/lib/constants.ts (Plan 01):
```typescript
export interface NavItem {
  label: string
  href: string
  icon: string
  periodAware?: boolean
}
export const SIDEBAR_NAV_ITEMS: NavItem[]
export const PERIOD_AWARE_ROUTES: string[]
export const MONTH_NAMES_ES: readonly string[]
```

From src/components/ui/DynamicIcon.tsx (Plan 01):
```typescript
export default function DynamicIcon({ name, ...props }: DynamicIconProps): JSX.Element
```

From src/app/layout.tsx (current -- will be MODIFIED):
```typescript
// Currently: minimal layout with html/body tags, DM_Sans font, metadata
// MODIFY TO: add flex container with Sidebar, main content area, MobileNav, FAB
```

Tailwind color tokens:
```
bg-bg-primary, bg-bg-card, text-text-primary, text-text-secondary, text-text-muted
text-accent, border-border, text-info, bg-info (for closed period banner)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PageHeader, PeriodSelector, and root layout wiring with tests</name>
  <files>src/components/layout/PageHeader.tsx, src/components/layout/PageHeader.test.tsx, src/components/layout/PeriodSelector.tsx, src/components/layout/PeriodSelector.test.tsx, src/app/layout.tsx</files>
  <behavior>
    PageHeader:
    - Renders title text in an h1 element
    - Renders action slot content when action prop is provided
    - Does not render action area when action prop is omitted
    - Renders periodSelector slot content when periodSelector prop is provided
    - Renders closed period banner with "Periodo cerrado -- solo lectura" text when closedBanner is true
    - Does not render banner when closedBanner is false or omitted

    PeriodSelector:
    - Renders current month name in Spanish (from MONTH_NAMES_ES) and year
    - Renders left arrow and right arrow navigation buttons
    - When isClosed is true, renders a lock icon next to the month name
    - When isClosed is false or omitted, does not render lock icon
    - Defaults isClosed to false (placeholder pages pass no isClosed prop)
  </behavior>
  <action>
    Write tests FIRST per TDD, then implement:

    **0. `src/components/layout/PageHeader.test.tsx`** (write FIRST):
    Use @testing-library/react render + screen.
    - Test: renders title in an h1 element
    - Test: renders action slot when provided (pass a button as action prop, verify it appears)
    - Test: does not render action area when action is omitted
    - Test: renders periodSelector content when provided (pass a span with text, verify it appears)
    - Test: renders closed banner with "Periodo cerrado" text when closedBanner=true
    - Test: does not render closed banner when closedBanner is false

    **0b. `src/components/layout/PeriodSelector.test.tsx`** (write FIRST):
    Use @testing-library/react. Mock `next/navigation` useSearchParams to return empty params (defaults to current date), useRouter to return { push: vi.fn() }, usePathname to return '/'.
    - Test: renders current month name in Spanish (e.g., if current month is April, renders "Abril")
    - Test: renders current year (e.g., "2026")
    - Test: renders left arrow button and right arrow button
    - Test: when isClosed=true, renders lock icon (use data-testid="period-lock-icon" on the Lock icon)
    - Test: when isClosed is omitted (defaults to false), does NOT render lock icon
    - Test: right arrow button is disabled when viewing current month/year (cannot navigate to future)

    **1. `src/components/layout/PeriodSelector.tsx`** ("use client"):

    Props interface `PeriodSelectorProps`:
    - `isClosed?: boolean` -- whether the current period is closed (shows lock icon + banner). Defaults to false.

    Uses `useSearchParams()` and `useRouter()` from `next/navigation` for URL-based period state per CONTEXT.md locked decision ("Period state via URL search params (?month=X&year=Y)").
    Uses `usePathname()` to construct updated URLs.

    Behavior:
    - Read `month` and `year` from URL search params. Default to current date if not present: `new Date().getMonth() + 1` and `new Date().getFullYear()`
    - Display: `< Abril 2026 >` format. Use MONTH_NAMES_ES[month - 1] for Spanish month name.
    - Left arrow (ChevronLeft from lucide-react): navigate to previous month. If month is 1, wrap to December of previous year.
    - Right arrow (ChevronRight from lucide-react): navigate to next month. If month is 12, wrap to January of next year. Do NOT allow navigating beyond current month/year.
    - Arrow buttons update URL: `router.push(pathname + '?month=' + newMonth + '&year=' + newYear)`
    - If `isClosed` is true: show Lock icon (from lucide-react, size 14, data-testid="period-lock-icon") next to the month name text
    - Arrow buttons: `p-1.5 rounded-md hover:bg-bg-card-hover transition-colors duration-200`, text-text-secondary hover:text-text-primary
    - Month text: `text-sm font-medium text-text-primary` between the arrows
    - Layout: flex items-center gap-1

    NOTE: In placeholder pages, `isClosed` is not passed (defaults to false). The actual isClosed value will be fetched from the database in later phases when period-aware pages query the Period model.

    **2. `src/components/layout/PageHeader.tsx`** (Server Component -- no "use client"):

    Props interface `PageHeaderProps`:
    - `title: string` -- page title
    - `periodSelector?: React.ReactNode` -- optional PeriodSelector component (period-aware pages pass this)
    - `action?: React.ReactNode` -- optional primary action button (right-aligned)
    - `closedBanner?: boolean` -- whether to show the closed period banner

    Structure:
    - Container: `<header>` element, `mb-6`
    - Top row: flex items-center justify-between
      - Left: `<h1 className="text-2xl font-bold text-text-primary">{title}</h1>`
      - Right: `{action}` if provided
    - Second row (only if periodSelector provided): flex items-center gap-3, mt-1
      - `{periodSelector}`
    - Closed period banner (only if closedBanner is true):
      - `mt-3 px-4 py-2.5 rounded-lg bg-info/10 border border-info/20`
      - Flex items-center gap-2
      - Lock icon (lucide-react, size 14, text-info)
      - Text: "Periodo cerrado -- solo lectura" in text-sm text-info

    **3. Modify `src/app/layout.tsx`** (stays Server Component):

    Keep: metadata, DM_Sans font setup, html lang="es"
    Add: Import Sidebar, MobileNav, FAB. Compose them in the body:

    ```
    <body className="min-h-full bg-bg-primary text-text-primary font-sans">
      <div className="flex min-h-screen">
        <Sidebar />
        <main className="flex-1 md:ml-16 lg:ml-60 pb-16 md:pb-0">
          <div className="p-4 md:p-6 lg:p-8">
            {children}
          </div>
        </main>
      </div>
      <MobileNav />
      <FAB />
    </body>
    ```

    Key layout rules:
    - `<main>` has left margin matching sidebar width: `md:ml-16 lg:ml-60` (0 on mobile since sidebar is hidden)
    - `<main>` has bottom padding on mobile: `pb-16 md:pb-0` (64px for tab bar, removed on tablet+)
    - Content inside main has responsive padding: `p-4 md:p-6 lg:p-8`
    - Sidebar is rendered inside the flex container
    - MobileNav and FAB are rendered outside the flex container (they are fixed-position overlays)
    - Use `<main>` semantic element (a11y per UX_RULES.md section 9.2)

    IMPORTANT: The layout stays a Server Component. Sidebar, MobileNav, and FAB are Client Components that the layout imports. Do NOT add "use client" to layout.tsx.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npx vitest run src/components/layout/PageHeader.test.tsx src/components/layout/PeriodSelector.test.tsx</automated>
  </verify>
  <done>Root layout composes Sidebar + MobileNav + FAB. Main content area has correct margins and padding. PageHeader renders title with optional period selector and action. PeriodSelector navigates months via URL params, shows lock icon when isClosed=true. All PageHeader and PeriodSelector tests pass. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Placeholder route pages with loading skeletons</name>
  <files>src/app/page.tsx, src/app/movimientos/page.tsx, src/app/deudas/page.tsx, src/app/presupuesto/page.tsx, src/app/ingresos/page.tsx, src/app/historial/page.tsx, src/app/configuracion/page.tsx, src/app/loading.tsx, src/app/movimientos/loading.tsx, src/app/deudas/loading.tsx, src/app/presupuesto/loading.tsx, src/app/ingresos/loading.tsx, src/app/historial/loading.tsx, src/app/configuracion/loading.tsx</files>
  <action>
    Create placeholder pages for all 7 routes using PageHeader. Each page is a Server Component.

    **Period-aware pages** (Dashboard /, Movimientos, Presupuesto, Historial) receive searchParams and render PeriodSelector in the header. Note: In Next.js 16, searchParams is a Promise that must be awaited. Make these pages async.

    **Non-period-aware pages** (Deudas, Ingresos, Configuracion) render PageHeader without period selector.

    **IMPORTANT:** For placeholder pages, do NOT pass `isClosed` to PeriodSelector. It defaults to `false`. The actual closed-period state will be fetched from the database in later phases when these pages are implemented with real data. This means the lock icon will not appear on placeholder pages, which is correct behavior.

    **Page template for period-aware pages:**
    ```typescript
    import PageHeader from '@/components/layout/PageHeader'
    import PeriodSelector from '@/components/layout/PeriodSelector'

    interface PageProps {
      searchParams: Promise<{ month?: string; year?: string }>
    }

    export default async function [PageName]Page({ searchParams }: PageProps) {
      const params = await searchParams
      // Period-aware pages show PeriodSelector
      return (
        <div>
          <PageHeader
            title="[Spanish title]"
            periodSelector={<PeriodSelector />}
          />
          {/* Placeholder content */}
          <div className="flex flex-col items-center justify-center py-20 text-center">
            <DynamicIcon name="[relevant-icon]" size={48} className="text-text-muted mb-4" />
            <p className="text-text-secondary text-lg">[Spanish placeholder message]</p>
          </div>
        </div>
      )
    }
    ```

    **Specific pages:**

    1. `src/app/page.tsx` (Dashboard / Inicio):
       - Title: "Inicio"
       - Period-aware: YES
       - Icon: layout-dashboard
       - Placeholder: "El dashboard se construira en una fase posterior"

    2. `src/app/movimientos/page.tsx`:
       - Title: "Movimientos"
       - Period-aware: YES
       - Icon: arrow-left-right
       - Placeholder: "Los movimientos se construiran en una fase posterior"

    3. `src/app/deudas/page.tsx`:
       - Title: "Deudas"
       - Period-aware: NO
       - Icon: credit-card
       - Placeholder: "Las deudas se construiran en una fase posterior"

    4. `src/app/presupuesto/page.tsx`:
       - Title: "Presupuesto"
       - Period-aware: YES
       - Icon: piggy-bank
       - Placeholder: "El presupuesto se construira en una fase posterior"

    5. `src/app/ingresos/page.tsx`:
       - Title: "Ingresos"
       - Period-aware: NO
       - Icon: banknote
       - Placeholder: "Los ingresos se construiran en una fase posterior"

    6. `src/app/historial/page.tsx`:
       - Title: "Historial"
       - Period-aware: YES
       - Icon: history
       - Placeholder: "El historial se construira en una fase posterior"

    7. `src/app/configuracion/page.tsx`:
       - Title: "Configuracion"
       - Period-aware: NO
       - Icon: settings
       - Placeholder: "Proximamente" (per RESEARCH.md recommendation for this route)

    **Loading skeletons** (`loading.tsx` per route):

    Each loading.tsx exports a default function with skeleton markup:
    - Header skeleton: `<div className="h-8 w-48 bg-bg-card rounded-lg animate-pulse" />`
    - Content skeleton: 4-6 rows of `<div className="h-16 bg-bg-card rounded-xl animate-pulse" />`
    - Wrapped in container with `p-0` (parent already provides padding) and `space-y-6`

    Root `src/app/loading.tsx` has a dashboard-style skeleton (grid of 6 card-like rectangles for KPIs + larger rectangles for charts).

    Route-specific skeletons are simple list skeletons (header bar + stacked rows). Keep them minimal -- the full skeleton designs come in Phase 11 (Polish).

    IMPORTANT: Remove the `src/app/test-chart/` directory if it exists -- it was a Recharts validation page from Phase 1 that is no longer needed.

    IMPORTANT: In Next.js 16, `searchParams` in page components is a `Promise<{ [key: string]: string | string[] | undefined }>`. You MUST `await` it before accessing properties. Read the Next.js 16 docs at `node_modules/next/dist/docs/` if unsure about the exact API.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build && npm run lint</automated>
  </verify>
  <done>All 7 route pages render inside the layout shell with PageHeader. Period-aware pages show PeriodSelector (without isClosed -- defaults to false for placeholders). Non-period-aware pages show header without period. All routes have loading skeletons. Build and lint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete layout shell</name>
  <files>none</files>
  <action>
    Human verifies the complete responsive layout shell across three viewport sizes.

    **What was built:** Complete responsive layout shell with desktop sidebar (240px), tablet sidebar (64px icons-only), mobile bottom tab bar (5 items with "Mas" sheet), floating action button, page header with period selector, and 7 placeholder route pages.

    **How to verify:**

    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000 in browser

    **Desktop (>=1024px):**
    - Verify left sidebar shows "Centik" in cyan at top, 6 nav items with icons and labels
    - Click each nav item -- verify active state (accent bg + text), page changes with title
    - Verify cyan circular FAB in bottom-right corner
    - Click FAB -- verify modal opens with "Nuevo movimiento" title and placeholder text
    - Close modal via backdrop click or X button
    - On period-aware pages (Inicio, Movimientos, Presupuesto, Historial): verify < Abril 2026 > period selector in header
    - Click period arrows -- verify month changes and URL updates with ?month=X and year=Y
    - On non-period pages (Deudas, Ingresos): verify no period selector

    **Tablet (768-1023px):**
    - Resize browser to ~900px width
    - Verify sidebar collapses to narrow icons-only strip (~64px)
    - Hover over sidebar icons -- verify tooltip shows label
    - Verify no bottom tab bar
    - Verify FAB still visible

    **Mobile (<768px):**
    - Resize browser to ~375px width (or use DevTools mobile mode)
    - Verify NO sidebar visible
    - Verify bottom tab bar with 5 items: Inicio, Movimientos, Deudas, Presupuesto, Mas
    - Tap nav items -- verify active state and page changes
    - Tap "Mas" -- verify slide-up sheet with Ingresos, Historial, Configuracion
    - Tap an item in sheet -- verify navigation and sheet closes
    - Verify FAB visible above the bottom tab bar
    - Click FAB -- verify bottom sheet slides up (not centered modal)

    3. Verify no console errors in browser DevTools
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build && npx vitest run</automated>
  </verify>
  <done>User confirms layout looks correct across desktop, tablet, and mobile viewports. All navigation works, FAB opens modal/sheet, period selector navigates months, no console errors.</done>
</task>

</tasks>

<verification>
- `npm run build` -- zero errors, zero warnings
- `npm run lint` -- zero warnings
- `npx vitest run` -- all existing tests still pass (including new PageHeader, PeriodSelector tests)
- All 7 routes render inside layout shell
- Period selector updates URL params
- Navigation works across all viewports (desktop, tablet, mobile)
- No hydration warnings in browser console
</verification>

<success_criteria>
- Root layout composes Sidebar + MobileNav + FAB with correct responsive margins
- Desktop shows 240px sidebar, tablet shows 64px collapsed, mobile shows bottom tabs
- FAB visible on all viewports, opens modal/sheet
- Period selector appears on period-aware pages, navigates via URL params
- PeriodSelector shows lock icon when isClosed=true (tested), defaults to false for placeholders
- All 7 routes have placeholder pages with PageHeader and loading skeletons
- All tests pass (PageHeader, PeriodSelector), build and lint pass
- Human verification confirms layout looks correct across viewports
</success_criteria>

<output>
After completion, create `.planning/phases/04-layout-shell/04-03-SUMMARY.md`
</output>
