---
phase: 04-layout-shell
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/layout/MobileMoreSheet.tsx
  - src/components/layout/FAB.tsx
autonomous: true
requirements:
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04

must_haves:
  truths:
    - "Desktop sidebar shows 'Centik' in cyan at the top, 6 nav items with Lucide icons and labels, and active item highlighted with accent bg at 15% opacity"
    - "Tablet sidebar collapses to ~64px with icons only and hover tooltips showing labels"
    - "Mobile bottom tab bar shows 5 equal items: Inicio, Movimientos, Deudas, Presupuesto, Mas"
    - "Tapping 'Mas' in mobile tab bar opens a slide-up sheet with Ingresos, Historial, Configuracion"
    - "FAB is a circular 48px cyan button with white '+' icon, fixed bottom-right, always visible on every page"
    - "FAB click opens the Modal primitive (placeholder content for now -- actual form is Phase 6)"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Desktop/tablet sidebar navigation"
      contains: "use client"
      exports: ["default"]
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Mobile bottom tab bar"
      contains: "use client"
      exports: ["default"]
    - path: "src/components/layout/MobileMoreSheet.tsx"
      provides: "Slide-up sheet for 'Mas' overflow items"
      contains: "use client"
      exports: ["default"]
    - path: "src/components/layout/FAB.tsx"
      provides: "Floating action button for quick transaction entry"
      contains: "use client"
      exports: ["default"]
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/lib/constants.ts"
      via: "imports SIDEBAR_NAV_ITEMS"
      pattern: "SIDEBAR_NAV_ITEMS"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/components/ui/DynamicIcon.tsx"
      via: "renders icons by name"
      pattern: "DynamicIcon"
    - from: "src/components/layout/MobileNav.tsx"
      to: "src/lib/constants.ts"
      via: "imports MOBILE_TAB_ITEMS"
      pattern: "MOBILE_TAB_ITEMS"
    - from: "src/components/layout/MobileMoreSheet.tsx"
      to: "src/lib/constants.ts"
      via: "imports MORE_MENU_ITEMS"
      pattern: "MORE_MENU_ITEMS"
    - from: "src/components/layout/FAB.tsx"
      to: "src/components/ui/Modal.tsx"
      via: "opens Modal on click"
      pattern: "Modal"
---

<objective>
Build the four navigation and interaction components that form the application shell: desktop/tablet Sidebar, mobile MobileNav with MobileMoreSheet, and the floating action button (FAB). These are all Client Components because they use `usePathname` for active state detection or manage interactive state.

Purpose: Navigation is the skeleton of the app. The sidebar, bottom tabs, and FAB are present on every page. Building them as a batch ensures consistent patterns for active state detection, icon rendering, and responsive behavior.

Output: 4 new Client Components in `src/components/layout/`. These will be wired into the root layout in Plan 03.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-layout-shell/04-CONTEXT.md
@.planning/phases/04-layout-shell/04-RESEARCH.md
@.planning/phases/04-layout-shell/04-01-SUMMARY.md

@src/app/globals.css
@src/lib/utils.ts

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/constants.ts (created/modified in Plan 01):
```typescript
export interface NavItem {
  label: string
  href: string
  icon: string       // Lucide icon name for DynamicIcon
  periodAware?: boolean
}

export const SIDEBAR_NAV_ITEMS: NavItem[]   // 6 items: Inicio, Movimientos, Deudas, Presupuesto, Ingresos, Historial
export const MOBILE_TAB_ITEMS: NavItem[]    // 4 items: Inicio, Movimientos, Deudas, Presupuesto
export const MORE_MENU_ITEMS: NavItem[]     // 3 items: Ingresos, Historial, Configuracion
```

From src/components/ui/DynamicIcon.tsx (created in Plan 01):
```typescript
interface DynamicIconProps extends LucideProps {
  name: string
}
export default function DynamicIcon({ name, ...props }: DynamicIconProps): JSX.Element
// Resolves icon name string to Lucide component via static ICON_MAP
```

From src/components/ui/Modal.tsx (created in Plan 01):
```typescript
interface ModalProps {
  isOpen: boolean
  onClose: () => void
  children: React.ReactNode
  title?: string
  maxWidth?: string
}
export default function Modal({ isOpen, onClose, children, title, maxWidth }: ModalProps): JSX.Element
// Bottom sheet on mobile, centered modal on desktop
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

Tailwind color tokens from globals.css:
```
--color-accent: #22d3ee
--color-bg-card: #111827
--color-bg-card-hover: #1a2332
--color-border: #1e293b
--color-text-primary: #e2e8f0
--color-text-secondary: #94a3b8
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar and MobileNav with MobileMoreSheet</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/MobileNav.tsx, src/components/layout/MobileMoreSheet.tsx</files>
  <action>
    Create 3 Client Components for navigation:

    **1. `src/components/layout/Sidebar.tsx`** ("use client"):

    Uses `usePathname()` from `next/navigation` for active state. Imports `SIDEBAR_NAV_ITEMS` from constants. Imports DynamicIcon and Link from next/link.

    Structure:
    - `<aside>` with `<nav>` inside (semantic HTML per UX_RULES.md section 9.2)
    - Fixed positioning: `fixed inset-y-0 left-0`
    - Responsive width: `hidden md:flex md:w-16 lg:w-60` (hidden on mobile, 64px on tablet, 240px on desktop)
    - Background: bg-bg-card, border-r border-border
    - Z-index: z-30 (per RESEARCH.md z-index scale)
    - Flex column layout

    Top section: "Centik" app name
    - Desktop (lg+): Text "Centik" in text-xl font-bold text-accent
    - Tablet (md to lg): Text "C" in text-xl font-bold text-accent (single letter for collapsed mode)
    - Use `lg:block hidden` / `lg:hidden block` responsive toggling

    Nav items: Map over SIDEBAR_NAV_ITEMS
    - Each is a `<Link>` with DynamicIcon + label
    - Active detection: `pathname === item.href || (item.href !== '/' && pathname.startsWith(item.href))`
    - Active style: `bg-accent/15 text-accent` (accent background at 15% opacity + accent text)
    - Inactive style: `text-text-secondary hover:text-text-primary hover:bg-bg-card-hover`
    - Tablet collapsed: `lg:gap-3 lg:px-3` for expanded, `md:justify-center md:px-0` for collapsed
    - Label: `<span className="hidden lg:inline">` -- hidden on tablet, visible on desktop
    - Tooltip on tablet: `title={item.label}` attribute on the Link for native browser tooltip. Also add a custom CSS tooltip using group-hover (absolute positioned span, left-full ml-2, bg-bg-elevated, rounded-md, hidden lg:hidden, visible on md via group-hover:opacity-100)
    - All items: rounded-lg, py-2.5, text-sm font-medium, transition-all duration-200

    Padding: p-4 for nav container, with 24px top padding for "Centik" header area.

    **2. `src/components/layout/MobileNav.tsx`** ("use client"):

    Uses `usePathname()` for active state. Imports `MOBILE_TAB_ITEMS` and DynamicIcon.

    Structure:
    - `<nav>` element with aria-label="Navegacion principal"
    - Fixed bottom: `fixed bottom-0 inset-x-0 md:hidden` (hidden on tablet+)
    - Height: h-16 (64px)
    - Background: bg-bg-card, border-t border-border
    - Z-index: z-40
    - Flex row, items evenly distributed (5 equal items)

    Items: 4 from MOBILE_TAB_ITEMS + 1 hardcoded "Mas" item
    - Each item is a Link (for the 4 nav items) or button (for "Mas")
    - Each: flex flex-col items-center justify-center gap-1
    - Icon: DynamicIcon at size 20
    - Label: text-[10px] font-medium
    - Active: text-accent
    - Inactive: text-text-secondary
    - "Mas" uses MoreHorizontal icon (import directly from lucide-react), label "Mas"
    - "Mas" button onClick toggles MobileMoreSheet open state
    - If any MORE_MENU_ITEMS route is active, highlight the "Mas" tab too

    Internal state: `const [isMoreOpen, setIsMoreOpen] = useState(false)`
    Renders `<MobileMoreSheet isOpen={isMoreOpen} onClose={() => setIsMoreOpen(false)} />`

    **3. `src/components/layout/MobileMoreSheet.tsx`** ("use client"):

    Props: `{ isOpen: boolean, onClose: () => void }`

    Structure:
    - Backdrop: fixed inset-0, z-50, bg-black/70, backdrop-blur-sm, click to close
    - Sheet: fixed inset-x-0 bottom-0, z-50, bg-bg-card, rounded-t-xl, border-t border-border
    - Drag handle indicator: centered w-10 h-1 bg-border-light rounded-full, py-3
    - Animation: transform transition-transform duration-300 ease-out, translate-y-0 (open) / translate-y-full (closed)

    Content: Map over MORE_MENU_ITEMS
    - Each is a Link with DynamicIcon + label, full width
    - On click: call onClose to dismiss the sheet
    - Active state detection same as Sidebar
    - Padding: px-4 py-3 per item, rounded-lg, hover:bg-bg-card-hover
    - Bottom safe area: pb-8 for iOS home indicator

    Use conditional rendering for backdrop (only when isOpen). Use CSS transitions for sheet animation.

    IMPORTANT PATTERNS:
    - Use `cn()` from `@/lib/utils` for all conditional class merging
    - Use `next/link` Link component for all navigation (NOT `<a>` tags)
    - Use `usePathname()` from `next/navigation` (NOT `next/router` which is Pages Router)
    - All responsive behavior via Tailwind classes (NOT JS viewport detection)
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build</automated>
  </verify>
  <done>Sidebar renders on desktop (240px with labels) and tablet (64px icons-only with tooltips), hidden on mobile. MobileNav renders 5 equal bottom tabs on mobile, hidden on tablet+. MobileMoreSheet slides up with Ingresos/Historial/Configuracion. Active states highlight correctly. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Floating Action Button (FAB)</name>
  <files>src/components/layout/FAB.tsx</files>
  <action>
    Create `src/components/layout/FAB.tsx` as a Client Component ("use client"):

    Internal state: `const [isOpen, setIsOpen] = useState(false)`

    FAB button:
    - `<button>` element with aria-label="Registrar movimiento"
    - Fixed position: `fixed z-40`
    - Desktop/tablet: `bottom-6 right-6`
    - Mobile: `bottom-20 right-4` (above the 64px mobile tab bar)
    - Appearance: `w-12 h-12 rounded-full bg-accent hover:bg-accent-hover` (48px circle, cyan)
    - Icon: Plus from lucide-react, size 24, `text-bg-primary` (white/dark "+" on cyan bg)
    - Shadow: shadow-lg for elevation, plus shadow-glow for subtle accent glow
    - Transition: `transition-all duration-200`
    - Hover: scale-105 for subtle enlarge effect
    - Active (pressed): scale-95

    On click: `setIsOpen(true)`

    Modal content (placeholder for Phase 6):
    - Render `<Modal isOpen={isOpen} onClose={() => setIsOpen(false)} title="Nuevo movimiento">`
    - Inside Modal: placeholder text "El formulario de transacciones se construira en una fase posterior." with a centered icon (Receipt from lucide-react, size 48, text-text-muted) and a secondary close button.

    IMPORTANT: The FAB must be always visible on every page per CONTEXT.md locked decision. It is NOT conditionally rendered based on route. (Closed period disabling is a future concern for Phase 6/10, not this phase.)
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build</automated>
  </verify>
  <done>FAB renders as a fixed 48px cyan circular button with white "+" icon. Positioned bottom-right, above mobile tab bar on mobile. Click opens Modal with placeholder content. Always visible. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` -- zero errors, zero warnings
- `npm run lint` -- zero warnings
- Sidebar renders on desktop (240px) and tablet (64px icons-only), hidden on mobile
- MobileNav renders 5 bottom tabs on mobile, hidden on md+
- "Mas" tab opens MobileMoreSheet with 3 items
- FAB is visible on all viewports, click opens Modal
- All components use `usePathname()` from `next/navigation`
- No `{ icons }` barrel import from lucide-react anywhere
</verification>

<success_criteria>
- Desktop sidebar: "Centik" cyan header, 6 nav items with icons and labels, active state highlighting
- Tablet sidebar: 64px collapsed, icons only, hover tooltips
- Mobile: 5-item bottom tab bar, "Mas" opens sheet with 3 overflow items
- FAB: 48px cyan circle, white "+", fixed bottom-right, opens placeholder modal
- All navigation uses next/link Link component
- Build and lint pass with zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04-layout-shell/04-02-SUMMARY.md`
</output>
