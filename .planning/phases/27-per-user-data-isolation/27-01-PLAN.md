---
phase: 27-per-user-data-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth-utils.ts
  - src/lib/auth-utils.test.ts
autonomous: true
requirements:
  - ISOL-01

must_haves:
  truths:
    - "requireAuth() returns { userId } when session exists"
    - "requireAuth() redirects to /login when no session"
    - "getDefaultUserId() no longer exists in auth-utils.ts"
  artifacts:
    - path: "src/lib/auth-utils.ts"
      provides: "requireAuth() helper for Server Actions"
      exports: ["requireAuth"]
    - path: "src/lib/auth-utils.test.ts"
      provides: "Unit tests for requireAuth()"
      min_lines: 30
  key_links:
    - from: "src/lib/auth-utils.ts"
      to: "src/auth.ts"
      via: "import { auth } from '@/auth'"
      pattern: "auth\\(\\)"
    - from: "src/lib/auth-utils.ts"
      to: "next/navigation"
      via: "import { redirect }"
      pattern: "redirect\\("
---

<objective>
Create the requireAuth() helper that replaces getDefaultUserId() -- the security boundary for Server Actions.

Purpose: ISOL-01 requires a session-based auth guard that calls auth(), redirects unauthenticated users, and returns { userId }. This is the CVE-2025-29927 defense-in-depth layer.
Output: src/lib/auth-utils.ts with requireAuth() exported, getDefaultUserId() removed, and unit tests covering both paths (session present, session absent).
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-per-user-data-isolation/27-CONTEXT.md
@.planning/phases/27-per-user-data-isolation/27-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/auth.ts:
```typescript
export const { handlers, signIn, signOut, auth } = NextAuth({...})
// auth() returns Session | null
// Session.user.id contains userId (set by sessionCallback)
```

From src/lib/auth-utils.ts (CURRENT -- will be replaced):
```typescript
export async function getDefaultUserId(): Promise<string>
```

From next/navigation:
```typescript
export function redirect(url: string): never
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace getDefaultUserId with requireAuth</name>
  <files>src/lib/auth-utils.ts, src/lib/auth-utils.test.ts</files>
  <behavior>
    - Test 1: requireAuth() returns { userId: 'test-id' } when auth() returns a session with user.id
    - Test 2: requireAuth() calls redirect('/login') when auth() returns null (no session)
    - Test 3: requireAuth() calls redirect('/login') when session exists but user.id is undefined
  </behavior>
  <action>
1. Create `src/lib/auth-utils.test.ts`:
   - Mock `@/auth` with `auth: vi.fn()` -- mock next-auth module init same as auth.test.ts pattern (mock next-auth, @auth/prisma-adapter, next-auth/providers/credentials)
   - Mock `next/navigation` with `redirect: vi.fn()` -- redirect throws in real code but we just verify it's called
   - Test: auth() returns `{ user: { id: 'user-123' } }` => requireAuth() resolves `{ userId: 'user-123' }`
   - Test: auth() returns `null` => redirect('/login') is called
   - Test: auth() returns `{ user: {} }` (no id) => redirect('/login') is called
   - Run tests (MUST FAIL -- requireAuth doesn't exist yet)

2. Replace `src/lib/auth-utils.ts` content entirely:
   - Remove prisma import and getDefaultUserId function
   - Import `redirect` from `next/navigation` and `auth` from `@/auth`
   - Export `requireAuth()` function:
     ```typescript
     export async function requireAuth(): Promise<{ userId: string }> {
       const session = await auth()
       if (!session?.user?.id) {
         redirect('/login')
       }
       return { userId: session.user.id }
     }
     ```
   - CRITICAL: redirect() throws internally. The `if` block with redirect() never reaches the return statement. TypeScript may need the non-null assertion or a type guard. The redirect() function's return type is `never`, so TypeScript should understand the control flow after the if block.

3. Run tests (MUST PASS)
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npx vitest run src/lib/auth-utils.test.ts --reporter=verbose</automated>
  </verify>
  <done>requireAuth() exported from auth-utils.ts, getDefaultUserId() removed, 3 unit tests passing</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/auth-utils.test.ts --reporter=verbose` -- all requireAuth tests pass
- `grep -r "getDefaultUserId" src/lib/auth-utils.ts` -- returns no matches (function removed)
- `grep "export.*requireAuth" src/lib/auth-utils.ts` -- confirms export exists
</verification>

<success_criteria>
- requireAuth() is the only export from src/lib/auth-utils.ts
- getDefaultUserId() is completely removed
- 3 unit tests cover session-present, no-session, and no-user-id cases
- Tests run in under 10 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/27-per-user-data-isolation/27-01-SUMMARY.md`
</output>
