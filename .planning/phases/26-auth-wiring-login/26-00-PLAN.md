---
phase: 26-auth-wiring-login
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/auth.test.ts
  - src/proxy.test.ts
  - src/actions/auth.test.ts
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Test stub files exist and vitest discovers them without errors"
    - "Each stub file has at least one describe block with a placeholder test"
  artifacts:
    - path: "src/auth.test.ts"
      provides: "Test stubs for authorize callback and JWT/session callbacks"
      contains: "describe"
    - path: "src/proxy.test.ts"
      provides: "Test stubs for proxy route protection"
      contains: "describe"
    - path: "src/actions/auth.test.ts"
      provides: "Test stubs for login server action"
      contains: "describe"
  key_links: []
---

<objective>
Create test stub files for all auth-related test suites so that Nyquist sampling can reference them from Wave 1 onward.

Purpose: Wave 0 establishes test file existence before any production code is written, ensuring verify commands can run vitest against these files throughout execution.
Output: Three stub test files with describe blocks and todo placeholders.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-auth-wiring-login/26-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for auth.ts, proxy.ts, and auth actions</name>
  <files>
    src/auth.test.ts
    src/proxy.test.ts
    src/actions/auth.test.ts
  </files>
  <action>
    Create three test stub files with describe blocks and `it.todo()` placeholders. These stubs will be filled in by Plan 03 (auth test suite). The purpose here is just file existence so vitest can discover them.

    1. Create `src/auth.test.ts`:
       ```typescript
       import { describe, it } from 'vitest'

       describe('authorizeUser', () => {
         it.todo('returns user object for valid credentials')
         it.todo('returns null for wrong password')
         it.todo('returns null for non-existent email')
         it.todo('returns null for unapproved user')
         it.todo('returns null for empty credentials')
         it.todo('returns null for user with null hashedPassword')
       })

       describe('jwtCallback', () => {
         it.todo('adds userId to token on initial sign-in')
         it.todo('preserves existing token when no user')
       })

       describe('sessionCallback', () => {
         it.todo('sets session.user.id from token.userId')
       })
       ```

    2. Create `src/proxy.test.ts`:
       ```typescript
       import { describe, it } from 'vitest'

       describe('proxy', () => {
         it.todo('redirects unauthenticated request to /login')
         it.todo('preserves callbackUrl for protected routes')
         it.todo('allows authenticated request to pass through')
         it.todo('allows unauthenticated access to /login')
         it.todo('allows unauthenticated access to /register')
         it.todo('redirects authenticated user from /login to /')
       })
       ```

    3. Create `src/actions/auth.test.ts`:
       ```typescript
       import { describe, it } from 'vitest'

       describe('loginAction', () => {
         it.todo('returns error for invalid email format')
         it.todo('returns error for empty password')
         it.todo('returns error when AuthError is thrown')
         it.todo('re-throws non-AuthError exceptions')
         it.todo('calls signIn with correct params for valid data')
       })
       ```
  </action>
  <verify>
    <automated>npx vitest run src/auth.test.ts src/proxy.test.ts src/actions/auth.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Three test stub files exist, vitest discovers all todo tests without errors. Wave 0 complete -- subsequent plans can reference these files in verify commands.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/auth.test.ts src/proxy.test.ts src/actions/auth.test.ts` -- all files discovered, todo tests listed
2. No import errors (stubs have no production imports yet)
</verification>

<success_criteria>
- src/auth.test.ts exists with 9 todo test cases
- src/proxy.test.ts exists with 6 todo test cases
- src/actions/auth.test.ts exists with 5 todo test cases
- vitest runs without errors on all three files
</success_criteria>

<output>
After completion, create `.planning/phases/26-auth-wiring-login/26-00-SUMMARY.md`
</output>
