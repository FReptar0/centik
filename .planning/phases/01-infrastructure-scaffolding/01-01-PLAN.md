---
phase: 01-infrastructure-scaffolding
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - .gitignore
  - .env
  - .env.example
  - .env.test
  - docker-compose.yml
  - docker-compose.test.yml
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/favicon.ico
  - src/components/.gitkeep
  - src/components/ui/.gitkeep
  - src/lib/.gitkeep
  - src/types/index.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-07

must_haves:
  truths:
    - "Project directory structure matches CLAUDE.md src/ layout"
    - "All core + dev dependencies are installed and resolvable"
    - "Docker Compose config files are syntactically valid for dev DB (port 5432) and test DB (port 5433)"
    - "Environment files exist with correct connection strings"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout file in correct location"
    - path: "src/app/globals.css"
      provides: "Tailwind import in correct location"
    - path: "src/app/page.tsx"
      provides: "Home page placeholder"
    - path: "docker-compose.yml"
      provides: "Dev PostgreSQL container config"
    - path: "docker-compose.test.yml"
      provides: "Test PostgreSQL container config (tmpfs)"
    - path: ".env"
      provides: "Dev database connection string"
    - path: ".env.example"
      provides: "Placeholder environment template"
    - path: ".env.test"
      provides: "Test database connection string"
    - path: "package.json"
      provides: "All dependencies and npm scripts"
    - path: "tsconfig.json"
      provides: "Updated path alias @/* -> ./src/*"
  key_links:
    - from: "tsconfig.json"
      to: "src/"
      via: "paths @/* -> ./src/*"
      pattern: '"@/\\*".*"\\./src/\\*"'
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: '"prisma".*"@prisma/client".*"zod"'
---

<objective>
Restructure the project from the default Create Next App scaffold into the CLAUDE.md src/ layout, install all project dependencies, configure Docker Compose for dev and test PostgreSQL, and create environment files.

Purpose: Establish the physical file structure and dependency tree that all subsequent plans build on.
Output: Working directory structure with all deps installed, Docker configs ready, environment files in place.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-scaffolding/01-CONTEXT.md
@.planning/phases/01-infrastructure-scaffolding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure directory layout and install all dependencies</name>
  <files>package.json, package-lock.json, tsconfig.json, .gitignore, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/app/favicon.ico, src/components/.gitkeep, src/components/ui/.gitkeep, src/lib/.gitkeep, src/types/index.ts</files>
  <action>
1. Create the src/ directory structure:
   - `src/app/` (move files here)
   - `src/components/` (empty with .gitkeep)
   - `src/components/ui/` (empty with .gitkeep)
   - `src/lib/` (empty with .gitkeep)
   - `src/types/` (with empty index.ts)

2. Move existing files from `app/` to `src/app/`:
   - `app/globals.css` -> `src/app/globals.css` (keep as-is for now; Plan 03 will rewrite with @theme tokens)
   - `app/layout.tsx` -> `src/app/layout.tsx` (keep as-is for now; Plan 03 will update font and metadata)
   - `app/page.tsx` -> `src/app/page.tsx` (replace with a minimal placeholder: just an h1 "Centik" centered on screen)
   - `app/favicon.ico` -> `src/app/favicon.ico`
   - Delete the now-empty `app/` directory

3. Update `tsconfig.json`:
   - Change `paths` from `"@/*": ["./*"]` to `"@/*": ["./src/*"]`
   - Add `"**/*.mts"` to `include` array if not already present

4. Update `package.json`:
   - Add ALL npm scripts per RESEARCH.md (dev, build, start, lint, lint:fix, format:check, format, test, test:run, test:coverage, test:integration, test:e2e, quality, postinstall)
   - Use `npm run` style commands. The "quality" script: `"npm run build && npm run lint && npm run format:check && npm run test:run"`
   - The "lint" script: `"eslint ."` (NOT `next lint` -- removed in Next.js 16)
   - The "postinstall" script: `"prisma generate"`
   - Add `"overrides"` section: `{ "react-is": "$react-is" }` (npm syntax for matching own dependency version)
   - Install core dependencies: `npm install @prisma/client @prisma/adapter-pg pg zod recharts react-is lucide-react clsx tailwind-merge dotenv`
   - Install dev dependencies: `npm install -D prisma @types/pg tsx vitest @vitejs/plugin-react vite-tsconfig-paths jsdom @testing-library/react @testing-library/dom @playwright/test prettier eslint-config-prettier`
   - IMPORTANT: The "overrides" field must be added to package.json BEFORE running `npm install` so react-is resolution is correct

5. Update `.gitignore` -- add these entries:
   - `.env` and `.env.test` (gitignore secrets)
   - `generated/` (Prisma generated client)
   - `/coverage` (already present)
   - `/e2e-results/` and `/playwright-report/` (Playwright artifacts)

6. Create `src/types/index.ts` with a placeholder comment: `// Shared TypeScript types -- populated in Phase 3`

Per user decision: use npm (NOT pnpm). All commands use `npm run`.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && test -f src/app/layout.tsx && test -f src/app/globals.css && test -f src/app/page.tsx && test ! -d app && node -e "const p=require('./package.json'); const ok=p.dependencies.zod && p.dependencies.recharts && p.devDependencies.prisma && p.devDependencies.vitest && p.overrides && p.overrides['react-is']; if(!ok) process.exit(1)" && echo "PASS"</automated>
  </verify>
  <done>src/ directory structure created, all files moved from app/ to src/app/, tsconfig paths updated to @/* -> ./src/*, all dependencies installed, .gitignore updated, package.json has all scripts and overrides</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose files and environment files</name>
  <files>docker-compose.yml, docker-compose.test.yml, .env, .env.example, .env.test</files>
  <action>
1. Create `docker-compose.yml` (dev database):
   ```yaml
   services:
     db:
       image: postgres:16-alpine
       ports:
         - "5432:5432"
       environment:
         POSTGRES_USER: misfinanzas
         POSTGRES_PASSWORD: misfinanzas_dev
         POSTGRES_DB: misfinanzas
       volumes:
         - pgdata:/var/lib/postgresql/data
   volumes:
     pgdata:
   ```

2. Create `docker-compose.test.yml` (test database -- tmpfs for speed, no persistence):
   ```yaml
   services:
     db-test:
       image: postgres:16-alpine
       ports:
         - "5433:5432"
       environment:
         POSTGRES_USER: misfinanzas
         POSTGRES_PASSWORD: misfinanzas_test
         POSTGRES_DB: misfinanzas_test
       tmpfs:
         - /var/lib/postgresql/data
   ```

3. Create `.env` (dev -- gitignored):
   ```
   DATABASE_URL="postgresql://misfinanzas:misfinanzas_dev@localhost:5432/misfinanzas"
   ```

4. Create `.env.example` (committed, placeholders only):
   ```
   DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
   ```

5. Create `.env.test` (test -- gitignored):
   ```
   DATABASE_URL="postgresql://misfinanzas:misfinanzas_test@localhost:5433/misfinanzas_test"
   ```

IMPORTANT: .env and .env.test must be in .gitignore (Task 1 handles this). .env.example is committed.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && test -f docker-compose.yml && test -f docker-compose.test.yml && test -f .env && test -f .env.example && test -f .env.test && grep -q "5432" docker-compose.yml && grep -q "5433" docker-compose.test.yml && grep -q "tmpfs" docker-compose.test.yml && docker compose config -q 2>&1 && docker compose -f docker-compose.test.yml config -q 2>&1 && echo "PASS"</automated>
  </verify>
  <done>Docker Compose files created for dev (port 5432, persistent volume) and test (port 5433, tmpfs) and validated with `docker compose config`. Environment files created with correct connection strings. .env.example has placeholder values only.</done>
</task>

</tasks>

<verification>
- `ls src/app/layout.tsx src/app/globals.css src/app/page.tsx` -- all exist
- `ls app/` -- directory no longer exists
- `node -e "const p=require('./package.json'); console.log(Object.keys(p.scripts).length)"` -- should show 14+ scripts
- `cat tsconfig.json | grep 'src'` -- shows ./src/* in paths
- `docker compose config -q` -- validates docker-compose.yml syntax
- `docker compose -f docker-compose.test.yml config -q` -- validates test compose syntax
- `cat .env | grep 5432` -- dev DB connection string
- `cat .env.test | grep 5433` -- test DB connection string
</verification>

<success_criteria>
- Project uses src/ directory structure matching CLAUDE.md
- All 20+ dependencies installed (core + dev)
- package.json has all scripts using npm run, overrides for react-is
- tsconfig paths point to ./src/*
- Docker Compose files ready for dev (persistent) and test (tmpfs) PostgreSQL, validated with `docker compose config`
- Environment files created (.env, .env.example, .env.test)
- .gitignore updated with .env, .env.test, generated/, playwright artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-scaffolding/01-01-SUMMARY.md`
</output>
