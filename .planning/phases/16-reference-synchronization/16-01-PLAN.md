---
phase: 16-reference-synchronization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md]
autonomous: true
requirements: [REF-01]

must_haves:
  truths:
    - "CLAUDE.md Styling Guidelines section references Glyph Finance colors (#000000 bg, #CCFF00 accent) instead of old cyan/dark palette"
    - "CLAUDE.md font references specify Satoshi + IBM Plex Mono instead of DM Sans"
    - "CLAUDE.md radius values match STYLE_GUIDE.md (16px cards, 9999px buttons, 24px modals)"
    - "CLAUDE.md seed data colors reference desaturated Glyph Finance category palette"
    - "No contradictions exist between CLAUDE.md and STYLE_GUIDE.md styling tokens"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project instructions with Glyph Finance design tokens"
      contains: "#CCFF00"
  key_links:
    - from: "CLAUDE.md"
      to: "STYLE_GUIDE.md"
      via: "color tokens, font families, radius values must match"
      pattern: "#000000|#CCFF00|Satoshi|IBM Plex Mono"
---

<objective>
Update CLAUDE.md to reference Glyph Finance design tokens consistently, replacing all remnants of the old cyan/dark palette.

Purpose: CLAUDE.md is automatically loaded by every Claude session. If it references the old palette (#22d3ee cyan, #0a0f1a bg, DM Sans), Claude will generate code with wrong colors and fonts. Synchronizing with the updated STYLE_GUIDE.md ensures consistent design system context.

Output: Updated CLAUDE.md with Glyph Finance tokens in Styling Guidelines and Seed Data sections.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@STYLE_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md Styling Guidelines section with Glyph Finance tokens</name>
  <files>CLAUDE.md</files>
  <action>
Replace the "Styling Guidelines" section (lines 453-463 of CLAUDE.md) with Glyph Finance tokens from STYLE_GUIDE.md. The new section must contain:

**Theme/Colors** — Replace old palette entirely:
- Background: #000000 (OLED black, replaces #0a0f1a)
- Surface: #0A0A0A (secondary bg), #141414 (cards/elevated, replaces #111827)
- Borders: #222222 (replaces #1e293b)
- Text: #E8E8E8 (primary), #999999 (secondary), #666666 (tertiary)

**Accent** — Replace cyan with chartreuse:
- Primary accent: #CCFF00 (chartreuse, replaces #22d3ee cyan)
- Accent hover: #B8E600

**Semantic colors** — Replace old values:
- Positive: #00E676 (replaces #34d399)
- Negative: #FF3333 (replaces #f87171)
- Warning: #FF9100 (replaces #fb923c)
- Info: #448AFF (replaces #a78bfa purple)

**Typography** — Replace DM Sans:
- Headings/body: Satoshi (geometric sans-serif, replaces DM Sans)
- Financial numbers: IBM Plex Mono (monospaced, for ALL monetary amounts)
- Load both via `next/font`
- Remove the `font-sans` (DM Sans via Google Fonts) reference entirely

**Border radius** — Update to new scale:
- Cards: `rounded-2xl` (16px, was 12px)
- Inputs/tooltips: `rounded-xl` (12px, was 8px)
- Buttons/badges: `rounded-full` (9999px, pill shape)
- Modals: 24px

**Elevation** — Add note about no shadows:
- Depth communicated via background-shift only (#000000 -> #0A0A0A -> #141414)
- No box-shadow tokens, no decorative borders on cards

Keep existing bullet points that are NOT palette-specific:
- Iconography (lucide-react) — keep as-is
- No default Tailwind colors — keep but update reference to CSS @theme tokens
- Transitions — keep as-is
- Spacing — keep as-is

Do NOT change any other sections of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "#22d3ee\|#0a0f1a\|#111827\|#1e293b\|#f87171\|DM Sans" CLAUDE.md | grep -q "^0$" && grep -c "#CCFF00\|#000000.*OLED\|Satoshi\|IBM Plex Mono" CLAUDE.md | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Styling Guidelines section references only Glyph Finance tokens. Zero occurrences of old palette colors (#22d3ee, #0a0f1a, #111827, #1e293b, #f87171) or DM Sans in the section. Presence of #CCFF00, #000000, Satoshi, IBM Plex Mono confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Update Seed Data category colors and sweep for remaining old-palette references</name>
  <files>CLAUDE.md</files>
  <action>
Update the "Seed Data" section (lines 444-451) to reference the desaturated Glyph Finance category colors from STYLE_GUIDE.md:

**Expense categories** — replace saturated colors with desaturated:
- Comida: #C88A5A (was #fb923c)
- Servicios: #7A9EC4 (was #60a5fa)
- Entretenimiento: #9B89C4 (was #a78bfa)
- Suscripciones: #C48AA3 (was #f472b6)
- Transporte: #C4A84E (was #fbbf24)
- Otros: #8A9099 (was #94a3b8)

**Income categories** — replace with Glyph Finance semantic colors:
- Empleo: #00E676 (was #34d399, now uses --color-positive)
- Freelance: #CCFF00 (was #22d3ee cyan, now uses --color-accent)

After updating seed data, do a full sweep of CLAUDE.md for any remaining old-palette hex codes:
- #22d3ee, #0a0f1a, #111827, #1e293b, #34d399, #f87171, #fb923c, #a78bfa, #f472b6, #fbbf24, #94a3b8, #60a5fa

If ANY remain, replace them with the correct Glyph Finance equivalent. The final file must have zero occurrences of old-palette hex codes.

Do NOT modify sections unrelated to styling/colors (architecture, security, testing, etc. all stay as-is).
  </action>
  <verify>
    <automated>grep -cE "#22d3ee|#0a0f1a|#111827|#1e293b|#34d399|#f87171|#fb923c|#a78bfa|#f472b6|#fbbf24|#94a3b8|#60a5fa" CLAUDE.md | grep -q "^0$" && echo "PASS — zero old palette references" || echo "FAIL — old palette references remain"</automated>
  </verify>
  <done>Seed data section uses desaturated Glyph Finance category colors. Full sweep confirms zero occurrences of any old-palette hex code anywhere in CLAUDE.md. A developer reading CLAUDE.md gets consistent Glyph Finance guidance matching STYLE_GUIDE.md.</done>
</task>

</tasks>

<verification>
Cross-check CLAUDE.md against STYLE_GUIDE.md for consistency:
1. Background color in CLAUDE.md matches --color-bg in STYLE_GUIDE.md (#000000)
2. Accent color in CLAUDE.md matches --color-accent in STYLE_GUIDE.md (#CCFF00)
3. Font families in CLAUDE.md match --font-sans and --font-mono in STYLE_GUIDE.md (Satoshi, IBM Plex Mono)
4. Radius values in CLAUDE.md match --radius-* tokens in STYLE_GUIDE.md
5. Semantic colors in CLAUDE.md match STYLE_GUIDE.md (positive=#00E676, negative=#FF3333, warning=#FF9100, info=#448AFF)
6. Category colors in seed data match STYLE_GUIDE.md desaturated palette
7. Zero occurrences of old-palette hex codes anywhere in CLAUDE.md
</verification>

<success_criteria>
- CLAUDE.md Styling Guidelines section fully references Glyph Finance tokens
- CLAUDE.md Seed Data section uses desaturated category colors
- grep for old palette codes (#22d3ee, #0a0f1a, #111827, etc.) returns 0 matches
- grep for new palette codes (#CCFF00, #000000, Satoshi, IBM Plex Mono) returns matches
- No contradictions between CLAUDE.md and STYLE_GUIDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-synchronization/16-01-SUMMARY.md`
</output>
