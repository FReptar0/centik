---
phase: 24-touch-targets-table-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/history/AnnualPivotTable.tsx
  - src/components/budgets/BudgetTable.tsx
autonomous: true
requirements: [TABLE-01, TABLE-02]

must_haves:
  truths:
    - "AnnualPivotTable is readable on a 375px screen without requiring excessive horizontal scrolling"
    - "BudgetTable input cells have sufficient height for comfortable touch input on mobile"
  artifacts:
    - path: "src/components/history/AnnualPivotTable.tsx"
      provides: "Mobile-optimized table with reduced min-width and better scroll UX"
      contains: "sticky left-0"
    - path: "src/components/budgets/BudgetTable.tsx"
      provides: "44px minimum height on input cells for touch accessibility"
      contains: "min-h-\\[44px\\]"
  key_links:
    - from: "src/components/history/AnnualPivotTable.tsx"
      to: "table element"
      via: "Tailwind classes"
      pattern: "min-w-"
    - from: "src/components/budgets/BudgetTable.tsx"
      to: "FloatingInput wrapper"
      via: "Tailwind classes"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Optimize AnnualPivotTable and BudgetTable for mobile usability -- better scroll experience on the annual history table and touch-friendly input cells on the budget table.

Purpose: The AnnualPivotTable's min-w-[900px] forces extreme horizontal scrolling on mobile (375px screens need 2.4x viewport width). BudgetTable input cells lack minimum height for comfortable finger tapping.
Output: Two updated table components with improved mobile usability.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/history/AnnualPivotTable.tsx
@src/components/budgets/BudgetTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize AnnualPivotTable mobile scroll experience</name>
  <files>src/components/history/AnnualPivotTable.tsx</files>
  <action>
The AnnualPivotTable currently has `min-w-[900px]` (line 58) forcing horizontal scroll on any screen below 900px. The table has a sticky first column already (lines 62, 125), which is good. The mobile strategy is to keep the horizontal scroll (this is a 14-column data table -- it genuinely needs scroll on mobile) but make it more usable:

1. **Reduce min-width**: Change `min-w-[900px]` to `min-w-[700px]`. This reduces the scroll range by 22% while keeping columns readable. The cell content is short (formatted money values like "$1,500.00") and month headers are 3-letter abbreviations, so columns can compress.

2. **Add scroll indicator styling**: On the outer `overflow-x-auto` div (line 57), add visual cues that more content is available. Add a CSS gradient shadow hint. Update the wrapper div class from:
```
"overflow-x-auto rounded-2xl bg-surface-elevated"
```
to:
```
"overflow-x-auto rounded-2xl bg-surface-elevated [&::-webkit-scrollbar]:h-1 [&::-webkit-scrollbar-thumb]:rounded-full [&::-webkit-scrollbar-thumb]:bg-text-tertiary/30"
```
This adds a thin, styled scrollbar so users know the table is scrollable.

3. **Tighten cell padding on mobile**: Change the data cell padding from `px-3 py-3` to `px-2 py-3` on the td elements (line 131) to squeeze more columns into view:
```
"px-2 py-3 text-center font-mono tabular-nums text-sm"
```
Also change the month header th padding from `px-3 py-3` to `px-2 py-3` (line 76):
```
"px-2 py-3 text-center text-xs font-medium uppercase tracking-[2px] text-text-secondary"
```
And the annual total column td padding from `px-3 py-3` to `px-2 py-3` (line 137):
```
"px-2 py-3 text-center font-mono tabular-nums text-sm font-semibold"
```
And the annual total header th from `px-3 py-3` to `px-2 py-3` (line 116):
```
"px-2 py-3 text-center text-xs font-medium uppercase tracking-[2px] text-accent"
```

Keep the sticky first column padding at `px-4` (both th and td) unchanged since it needs visual separation from scrolling content.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && grep -c "min-w-\[700px\]" src/components/history/AnnualPivotTable.tsx && grep -c "scrollbar" src/components/history/AnnualPivotTable.tsx</automated>
  </verify>
  <done>AnnualPivotTable uses min-w-[700px] (reduced from 900px), has styled scrollbar indicators, and tighter cell padding. Sticky first column preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add touch-friendly minimum height to BudgetTable input cells</name>
  <files>src/components/budgets/BudgetTable.tsx</files>
  <action>
The BudgetTable input cells (the "Quincenal" column, line 113-121) use a FloatingInput wrapped in a div. The FloatingInput itself renders an input element but the containing td has no minimum height constraint, making the tap target uncomfortably small on mobile.

1. **Add min-height to the input wrapper div** (line 114): Change from:
```html
<div className="ml-auto w-28">
```
to:
```html
<div className="ml-auto w-28 min-h-[44px] flex items-center">
```
This ensures the FloatingInput wrapper has at least 44px of height for comfortable touch targeting. The `flex items-center` vertically centers the input within the taller container.

2. **Add min-height to table rows**: Update the tbody tr (line 94) to ensure consistent row height. The current class is `border-b border-border-divider`. No change needed here since the cell's min-h already enforces the row height.

3. **Ensure the Guardar (save) button also meets 44px touch target** (lines 146-154): Check the save button -- it uses `rounded-full bg-accent px-4 py-2 text-sm`. The py-2 (8px) + text-sm (~20px) = ~36px total. Update to add `min-h-[44px]` to the button class:
```
'rounded-full bg-accent px-4 py-2 min-h-[44px] text-sm font-semibold text-black',
```
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && grep -c "min-h-\[44px\]" src/components/budgets/BudgetTable.tsx</automated>
  </verify>
  <done>BudgetTable input cells have min-h-[44px] wrapper for comfortable touch input. Save button also has min-h-[44px]. Count should be 2 (input wrapper + save button).</done>
</task>

</tasks>

<verification>
npm run build passes with zero errors
npm test passes (no logic changes, only CSS classes and min-width value)
AnnualPivotTable min-width reduced from 900px to 700px
BudgetTable has min-h-[44px] on input wrapper and save button
</verification>

<success_criteria>
- AnnualPivotTable scrolls less on mobile (700px vs 900px min-width) with visible scrollbar
- BudgetTable input cells have 44px minimum height for comfortable touch
- Sticky first column on AnnualPivotTable still works
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-touch-targets-table-optimization/24-02-SUMMARY.md`
</output>
