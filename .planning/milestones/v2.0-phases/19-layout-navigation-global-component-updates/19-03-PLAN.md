---
phase: 19-layout-navigation-global-component-updates
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/dashboard/KPICard.tsx
  - src/components/dashboard/KPIGrid.tsx
  - src/components/layout/FAB.tsx
  - src/components/income/IncomeSourceCard.tsx
  - src/components/income/IncomeSourceCard.test.tsx
  - src/components/income/IncomeSummaryCards.tsx
  - src/components/income/IncomeSourceList.tsx
  - src/components/income/IncomeSourceForm.tsx
  - src/components/categories/CategoryList.tsx
  - src/components/categories/CategoryForm.tsx
  - src/components/debts/DebtCard.tsx
  - src/components/debts/DebtList.tsx
  - src/components/debts/DebtForm.tsx
  - src/components/debts/DebtSummaryCards.tsx
  - src/components/budgets/BudgetTable.tsx
  - src/components/budgets/BudgetSummaryRow.tsx
  - src/components/transactions/TransactionRow.tsx
  - src/components/transactions/TransactionList.tsx
  - src/components/transactions/TransactionFilters.tsx
  - src/components/transactions/TransactionForm.tsx
  - src/components/dashboard/RecentTransactions.tsx
  - src/components/history/AnnualPivotTable.tsx
  - src/components/history/CloseConfirmationModal.tsx
  - src/components/charts/TrendAreaChart.tsx
  - src/components/charts/ExpenseDonutChart.tsx
  - src/components/charts/BudgetBarChart.tsx
  - src/components/layout/PageHeader.tsx
autonomous: true
requirements: [UPDATE-01, UPDATE-02, UPDATE-09]

must_haves:
  truths:
    - "Every button in the app uses rounded-full (9999px pill shape) with active:scale-[0.98] press interaction"
    - "Primary buttons use bg-accent text-black, danger buttons use bg-negative text-white, secondary buttons use transparent with 1px border-divider border"
    - "All cards use bg-surface-elevated with NO visible border -- border border-border-divider removed from all card containers"
    - "Hero cards (dashboard KPIs, first two) have dot-matrix-hero class for texture background"
    - "All badges use rounded-full (pill shape) with semantic color variants using subtle backgrounds"
    - "Stacked card lists use 1px border-border-divider separator between items instead of individual card borders"
  artifacts:
    - path: "src/components/dashboard/KPICard.tsx"
      provides: "Borderless KPI card with optional dot-matrix hero variant"
      contains: "dot-matrix-hero"
    - path: "src/components/dashboard/KPIGrid.tsx"
      provides: "KPI grid that passes hero={true} to first two KPICard instances"
      contains: "hero={true}"
    - path: "src/components/income/IncomeSourceCard.tsx"
      provides: "Borderless income card with pill badges"
      contains: "rounded-full"
    - path: "src/components/categories/CategoryList.tsx"
      provides: "Category rows with pill badges and borderless cards"
      contains: "rounded-full"
  key_links:
    - from: "all card components"
      to: "bg-surface-elevated (no border)"
      via: "removal of border border-border-divider class"
      pattern: "bg-surface-elevated(?!.*border-border)"
    - from: "all button elements"
      to: "rounded-full active:scale-[0.98]"
      via: "class name update"
      pattern: "rounded-full.*active:scale"
    - from: "src/components/dashboard/KPIGrid.tsx"
      to: "src/components/dashboard/KPICard.tsx"
      via: "hero={true} prop on first two KPICard instances"
      pattern: "hero=\\{true\\}"
---

<objective>
Sweep all component files to apply three visual patterns: (1) pill-shaped buttons everywhere, (2) borderless cards with background-shift elevation, (3) pill-shaped badges. This is a broad class-name migration touching ~27 files.

Purpose: UPDATE-01 (pill buttons), UPDATE-02 (borderless cards + hero dot-matrix), UPDATE-09 (pill badges) are visual consistency requirements. Every screen must match the Glyph Finance spec. Depends on Plan 01 for dot-matrix-hero CSS class.
Output: All component files updated with pill buttons, borderless cards, and pill badges
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-layout-navigation-global-component-updates/19-CONTEXT.md
@.planning/phases/19-layout-navigation-global-component-updates/19-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pill buttons + press interaction across all components</name>
  <files>src/components/layout/FAB.tsx, src/components/categories/CategoryList.tsx, src/components/categories/CategoryForm.tsx, src/components/income/IncomeSourceList.tsx, src/components/income/IncomeSourceForm.tsx, src/components/income/IncomeSourceCard.tsx, src/components/debts/DebtList.tsx, src/components/debts/DebtForm.tsx, src/components/debts/DebtCard.tsx, src/components/budgets/BudgetTable.tsx, src/components/transactions/TransactionForm.tsx, src/components/transactions/TransactionFilters.tsx, src/components/transactions/TransactionList.tsx, src/components/history/CloseConfirmationModal.tsx, src/components/history/AnnualPivotTable.tsx, src/components/layout/PageHeader.tsx</files>
  <action>
Apply Glyph Finance button spec to every button element in the app. The rules:

**Button variants (per STYLE_GUIDE.md):**
- **Primary**: `rounded-full bg-accent text-black font-semibold active:scale-[0.98] transition-all duration-200 hover:bg-accent-hover`
  - Replace: `text-bg-primary` with `text-black` (pure black on chartreuse)
- **Secondary**: `rounded-full border border-border-divider bg-transparent text-text-secondary font-semibold active:scale-[0.98] transition-all duration-200 hover:bg-surface-hover hover:text-text-primary`
- **Danger**: `rounded-full bg-negative text-white font-semibold active:scale-[0.98] transition-all duration-200 hover:bg-negative/90`
- **Ghost**: `rounded-full bg-transparent text-text-secondary active:scale-[0.98] transition-all duration-200 hover:text-text-primary`

**What to change in each file:**

Search for all `<button` and interactive elements with `rounded-lg` or `rounded-md` and change to `rounded-full`. Add `active:scale-[0.98]` to every button that doesn't already have it.

Specific files and their buttons:

1. **FAB.tsx**: Already `rounded-full`. Change `active:scale-95` to `active:scale-[0.98]` for spec consistency. Change `text-bg-primary` to `text-black`.
2. **CategoryList.tsx**: "Agregar categoria" button -- change `rounded-lg` to `rounded-full`, change `text-bg-primary` to `text-black`
3. **CategoryForm.tsx**: Form buttons (submit/cancel) -- change `rounded-lg` to `rounded-full`, add `active:scale-[0.98]`; color picker circles can stay `rounded-full` (already correct)
4. **IncomeSourceList.tsx**: "Agregar fuente" button -- `rounded-lg` to `rounded-full`, `text-bg-primary` to `text-black`
5. **IncomeSourceForm.tsx**: Submit/cancel buttons, frequency toggle buttons -- `rounded-lg` to `rounded-full`, add `active:scale-[0.98]`
6. **IncomeSourceCard.tsx**: Edit/delete action buttons -- add `active:scale-[0.98]` (these are icon-only ghost buttons)
7. **DebtList.tsx**: "Agregar deuda" button -- `rounded-lg` to `rounded-full`, `text-bg-primary` to `text-black`
8. **DebtForm.tsx**: Submit/cancel, type toggle buttons -- `rounded-lg` to `rounded-full`, add `active:scale-[0.98]`
9. **DebtCard.tsx**: Inline edit buttons -- add `active:scale-[0.98]`
10. **BudgetTable.tsx**: "Guardar cambios" button -- `rounded-lg` to `rounded-full`, `text-bg-primary` to `text-black`
11. **TransactionForm.tsx**: Type toggle buttons, submit button -- `rounded-lg` to `rounded-full`, add `active:scale-[0.98]`; category selector circles can stay `rounded-full`
12. **TransactionFilters.tsx**: Filter chips/buttons -- `rounded-lg` to `rounded-full`, add `active:scale-[0.98]`
13. **TransactionList.tsx**: Any action buttons -- `rounded-full`, `active:scale-[0.98]`
14. **CloseConfirmationModal.tsx**: Cancel + confirm buttons -- `rounded-lg` to `rounded-full`, add `active:scale-[0.98]`. The confirm/danger button: `rounded-full bg-negative text-white`
15. **AnnualPivotTable.tsx**: Any action buttons
16. **PageHeader.tsx**: Closed banner -- `rounded-lg` to `rounded-full` on the banner container (this is informational, not a button, so keep as-is for the banner div)

**Do NOT change:**
- Select/dropdown `<select>` elements -- those stay with their current radius (will be replaced with FloatingInput in Phase 21)
- Input fields -- those stay `rounded-lg` or will be replaced with FloatingInput in Phase 21
- Navigation items -- those have their own styling (handled in Plan 02)
- Tooltip containers in Sidebar

After all changes, run `npm run build` to verify zero errors.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Every button element in the app uses rounded-full pill shape with active:scale-[0.98] press interaction. Primary buttons use text-black (not text-bg-primary). FAB uses active:scale-[0.98] and text-black. Build passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Borderless cards + hero dot-matrix + pill badges + stacked separators</name>
  <files>src/components/dashboard/KPICard.tsx, src/components/dashboard/KPIGrid.tsx, src/components/income/IncomeSourceCard.tsx, src/components/income/IncomeSourceCard.test.tsx, src/components/income/IncomeSummaryCards.tsx, src/components/debts/DebtCard.tsx, src/components/debts/DebtSummaryCards.tsx, src/components/categories/CategoryList.tsx, src/components/budgets/BudgetTable.tsx, src/components/budgets/BudgetSummaryRow.tsx, src/components/transactions/TransactionRow.tsx, src/components/transactions/TransactionList.tsx, src/components/dashboard/RecentTransactions.tsx, src/components/history/AnnualPivotTable.tsx, src/components/charts/TrendAreaChart.tsx, src/components/charts/ExpenseDonutChart.tsx, src/components/charts/BudgetBarChart.tsx</files>
  <action>
Apply three patterns across all files:

### Pattern A: Borderless Cards
Remove `border border-border-divider` from every card container. Cards use background-shift elevation only (`bg-surface-elevated`).

Files with `border border-border-divider` on card containers (grep confirmed 17 files):
1. **KPICard.tsx**: Remove `border border-border-divider` from outer div. Keep `rounded-xl bg-surface-elevated p-5`. Also change `rounded-xl` to `rounded-lg` (per spec: cards use --radius-lg = 16px, not xl = 24px).
2. **IncomeSourceCard.tsx**: Remove `border border-border-divider`. Change `rounded-xl` to `rounded-lg`.
3. **IncomeSummaryCards.tsx**: Remove `border border-border-divider`. Change `rounded-xl` to `rounded-lg`.
4. **DebtCard.tsx**: Remove `border border-border-divider`. Change `rounded-xl` to `rounded-lg`.
5. **DebtSummaryCards.tsx**: Remove `border border-border-divider`. Change `rounded-xl` to `rounded-lg`.
6. **CategoryList.tsx**: Remove `border border-border-divider` from CategoryRow. Change `rounded-xl` to `rounded-lg`.
7. **BudgetTable.tsx**: Remove `border border-border-divider` from table container. Change `rounded-xl` to `rounded-lg`.
8. **BudgetSummaryRow.tsx**: Remove any border class.
9. **TransactionRow.tsx**: This is a list item, not a standalone card. Remove border if present.
10. **TransactionList.tsx**: Remove `border border-border-divider` from list container. Change `rounded-xl` to `rounded-lg`.
11. **RecentTransactions.tsx**: Remove `border border-border-divider`. Change `rounded-xl` to `rounded-lg`.
12. **AnnualPivotTable.tsx**: Remove `border border-border-divider`. Change `rounded-xl` to `rounded-lg`.
13. **Chart wrappers (TrendAreaChart, ExpenseDonutChart, BudgetBarChart)**: Remove `border border-border-divider`. Change `rounded-xl` to `rounded-lg`.

For stacked lists (TransactionList items, CategoryList rows), use `divide-y divide-border-divider` on the parent container to create 1px separators between items instead of individual card borders.

### Pattern B: Hero Card Dot-Matrix Texture
Add `dot-matrix-hero` class to the first two KPI cards (Ingreso Mensual and Gastos del Mes -- the "balance" hero cards).

In **KPICard.tsx**, add an optional `hero` prop:
```typescript
export interface KPICardProps {
  label: string
  value: string
  icon: string
  color: SemanticColor
  subtitle?: string
  hero?: boolean  // NEW: adds dot-matrix texture
}
```

In the card container div, conditionally add `dot-matrix-hero`:
```tsx
<div className={cn(
  'rounded-lg bg-surface-elevated p-5 transition-all duration-200',
  hero && 'dot-matrix-hero',
)}>
  <div className="relative z-[2]">
    {/* All card content goes inside this z-[2] wrapper to sit above the dot-matrix ::before */}
    ...existing content...
  </div>
</div>
```

In **KPIGrid.tsx**, pass `hero={true}` to the first two KPICard instances. The "Disponible" (Available/balance) and the second summary KPI are the hero cards per spec. Find the first two `<KPICard` usages and add the `hero={true}` prop to each. This is the consumer that triggers the dot-matrix texture rendering.

### Pattern C: Pill Badges
Change all badge-like elements from `rounded-sm` to `rounded-full`. Badges are inline labels showing category type, frequency, status, etc.

Badge spec from STYLE_GUIDE.md:
- `rounded-full` (9999px pill shape)
- Padding: `px-2 py-0.5` (8px horizontal, 2px vertical)
- Font: `text-[11px] font-semibold uppercase tracking-wide`
- 6 semantic variants using subtle backgrounds:
  - Accent: `bg-accent-subtle text-accent`
  - Positive: `bg-positive-subtle text-positive`
  - Negative: `bg-negative-subtle text-negative`
  - Warning: `bg-warning-subtle text-warning`
  - Info: `bg-info-subtle text-info`
  - Neutral: `bg-[rgba(232,232,232,0.08)] text-text-secondary`

Files with badges:
1. **IncomeSourceCard.tsx**: Type badge (`rounded-sm bg-accent/15 text-accent`) and frequency badge (`rounded-sm bg-positive/15 text-positive`). Change `rounded-sm` to `rounded-full`. Change `bg-accent/15` to `bg-accent-subtle` and `bg-positive/15` to `bg-positive-subtle`.
2. **CategoryList.tsx**: Type badge (`rounded-sm`). Change to `rounded-full`. Change `bg-negative/15` to `bg-negative-subtle`, `bg-positive/15` to `bg-positive-subtle`, `bg-info/15` to `bg-info-subtle`.
3. **TransactionFilters.tsx**: Filter chips. Change to `rounded-full` (some may already be). Ensure active filter uses `bg-accent-subtle text-accent`.

Also update the TYPE_DISPLAY map in CategoryList.tsx to use subtle tokens:
```typescript
const TYPE_DISPLAY = {
  EXPENSE: { label: 'Gasto', className: 'bg-negative-subtle text-negative' },
  INCOME: { label: 'Ingreso', className: 'bg-positive-subtle text-positive' },
  BOTH: { label: 'Ambos', className: 'bg-info-subtle text-info' },
}
```

### Test Updates
- **IncomeSourceCard.test.tsx**: If any test checks for `rounded-sm` or `border border-border-divider` on the card, update to `rounded-full` and no border respectively. Check for any class-specific assertions.

After all changes, run `npm run build` to verify zero errors, then run the full test suite to catch any broken assertions.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All cards are borderless (bg-surface-elevated, no border class), hero KPI cards have dot-matrix-hero texture, KPIGrid passes hero={true} to first two KPICard instances, all badges are pill-shaped with semantic subtle backgrounds, stacked lists use divide-y separators, all tests pass, build succeeds</done>
</task>

</tasks>

<verification>
Full visual sweep verified:
- `npm run build` -- zero errors
- `npx vitest run` -- all tests pass
- Grep confirms no remaining `border border-border-divider` on card containers (only on dividers/separators)
- Grep confirms no remaining `rounded-sm` on badges
- Grep confirms no remaining `rounded-lg` on buttons (only on cards, inputs, nav items)
- Hero KPI cards render dot-matrix texture (KPIGrid passes hero={true}, KPICard applies dot-matrix-hero class)
- FAB uses active:scale-[0.98] and text-black
</verification>

<success_criteria>
1. Every button in the app uses rounded-full with active:scale-[0.98] press interaction
2. Primary buttons use bg-accent text-black, danger uses bg-negative text-white, secondary uses transparent with border
3. All card containers use bg-surface-elevated with NO border border-border-divider
4. Card containers use rounded-lg (16px) instead of rounded-xl (24px, reserved for modals)
5. First two KPI cards have dot-matrix-hero texture via ::before pseudo-element (KPIGrid passes hero={true})
6. All badges use rounded-full pill shape with semantic subtle background tokens
7. Stacked lists use divide-y divide-border-divider for separators
8. FAB uses active:scale-[0.98] and text-black
9. All tests pass, build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-layout-navigation-global-component-updates/19-03-SUMMARY.md`
</output>
