---
phase: 22-visual-qa-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/movimientos/page.tsx
  - src/components/transactions/TransactionForm.tsx
  - src/components/transactions/TransactionList.tsx
  - src/components/transactions/TransactionRow.tsx
  - src/components/transactions/TransactionFilters.tsx
  - src/components/transactions/Numpad.tsx
  - src/app/deudas/page.tsx
  - src/components/debts/DebtCard.tsx
  - src/components/debts/DebtForm.tsx
  - src/components/debts/DebtList.tsx
  - src/components/debts/DebtSummaryCards.tsx
  - src/app/presupuesto/page.tsx
  - src/components/budgets/BudgetProgressList.tsx
  - src/components/budgets/BudgetSummaryRow.tsx
  - src/components/budgets/BudgetTable.tsx
autonomous: true
requirements:
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - "Transaction list renders with Glyph Finance tokens, color-coded amounts, category icons in circular containers"
    - "FAB opens bottom sheet with custom numpad for amount entry (not OS keyboard)"
    - "Transaction bottom sheet has dot-matrix hero zone, toggle pills, circular category grid, and numpad"
    - "Debt cards show BatteryBar for utilization with correct threshold colors (green <31%, orange 31-70%, red 71%+)"
    - "Inline debt balance editing uses FloatingInput with underline styling"
    - "Budget progress uses BatteryBar per category with traffic-light colors (chartreuse <80%, orange 80-99%, red 100%+)"
    - "Budget configuration table uses FloatingInput for amount inputs"
    - "All monetary amounts on these pages use MoneyAmount component with IBM Plex Mono"
  artifacts:
    - path: "src/app/movimientos/page.tsx"
      provides: "Transactions page with Glyph Finance design"
    - path: "src/components/transactions/TransactionForm.tsx"
      provides: "Bottom sheet transaction form with numpad"
    - path: "src/app/deudas/page.tsx"
      provides: "Debts page with BatteryBar utilization"
    - path: "src/app/presupuesto/page.tsx"
      provides: "Budget page with BatteryBar progress"
  key_links:
    - from: "src/components/transactions/TransactionForm.tsx"
      to: "src/components/transactions/Numpad.tsx"
      via: "Custom numpad for amount entry"
      pattern: "Numpad|value|onChange"
    - from: "src/components/debts/DebtCard.tsx"
      to: "src/components/ui/BatteryBar.tsx"
      via: "BatteryBar for utilization display"
      pattern: "BatteryBar|percentage|thresholds"
    - from: "src/components/budgets/BudgetProgressList.tsx"
      to: "src/components/ui/BatteryBar.tsx"
      via: "BatteryBar for budget progress"
      pattern: "BatteryBar|percentage|thresholds"
---

<objective>
Audit and fix the Transactions, Debts, and Budget pages against STYLE_GUIDE.md specifications.

Purpose: These are the three core feature pages where users spend most of their time. Transaction creation flow (bottom sheet + numpad) is the signature interaction. Debts and Budget rely heavily on BatteryBar and FloatingInput -- the new Glyph Finance primitives.

Output: Three feature pages that match STYLE_GUIDE.md exactly, with all visual discrepancies fixed.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@STYLE_GUIDE.md
@UX_RULES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transactions page visual audit and fixes (QA-02)</name>
  <files>
    src/app/movimientos/page.tsx,
    src/components/transactions/TransactionForm.tsx,
    src/components/transactions/TransactionList.tsx,
    src/components/transactions/TransactionRow.tsx,
    src/components/transactions/TransactionFilters.tsx,
    src/components/transactions/Numpad.tsx
  </files>
  <action>
    Read each Transactions-related component file and compare against STYLE_GUIDE.md and UX_RULES.md specs. Check and fix ALL of the following:

    **Transaction List:**
    - Transaction rows: borderless cards with background-shift elevation, 1px `--color-border-divider` separator between stacked items
    - Category icon in 36x36px circular container with category color at 12% opacity background, icon in solid category color
    - DynamicIcon renders at strokeWidth 1.5px with `shapeRendering="crispEdges"`
    - Amounts use MoneyAmount component: "+" prefix in green for income, "-" prefix in red for expenses
    - Dates in Meta style (11px, `--color-text-tertiary`)
    - Description in Body style (14px, `--color-text-primary`)
    - Payment method badge: pill-shaped (`--radius-full`), 11px uppercase

    **Transaction Filters:**
    - Filter chips use pill shape (`--radius-full`)
    - Active filter chips show accent styling
    - "Limpiar filtros" visible when filters active
    - Toggle pills for type filter (Gasto/Ingreso) -- TogglePills component
    - FloatingInput for any text/date filter inputs

    **Transaction Bottom Sheet (TransactionForm):**
    - Opens as bottom sheet on mobile (85vh), centered modal on desktop
    - Drag handle: 4px height, 40px width, `--color-border-divider`, centered, 12px top margin
    - Close button (X) top-left, GUARDAR button top-right in `--color-accent`, Label style (12px, uppercase, letter-spacing +2px)
    - Hero zone with dot-matrix texture background
    - Amount display: IBM Plex Mono Display level (~36px), muted "$" in `--color-text-tertiary`
    - Toggle pills below amount: Gasto/Ingreso, active=chartreuse fill + black text, inactive=ghost
    - Category grid: 4x2 grid, circular icons (40px) with category color at 12% opacity, accent ring on selected (2px `--color-accent`)
    - Category labels: Meta style (11px, `--color-text-secondary`)

    **Custom Numpad:**
    - 4x4 grid layout per UX_RULES.md 4.1
    - Keys on `--color-surface-elevated` (#141414) over `--color-surface` (#0A0A0A) background
    - IBM Plex Mono Heading level (20px) for digits
    - Min touch target: 48px per key
    - Key press feedback: `--color-surface-hover` (#1A1A1A), transition 100ms
    - Backspace icon: Lucide `Delete`
    - Row layout: [1,2,3,backspace], [4,5,6,.], [7,8,9,00], [empty,0,empty,empty]

    **Pixel-dissolve animation:**
    - New transaction rows appear with `animate-scanline-reveal` class
    - Animation disabled when `prefers-reduced-motion` is active

    For each discrepancy found: fix the code, note what was wrong and what was changed.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build && npm test</automated>
  </verify>
  <done>
    Transactions page renders with: Glyph Finance tokens on list, filters use pill shapes, FAB triggers bottom sheet with dot-matrix hero + numpad + toggle pills + category grid. Pixel-dissolve works on new rows. Zero visual discrepancies. Build and tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Debts and Budget pages visual audit and fixes (QA-03, QA-04)</name>
  <files>
    src/app/deudas/page.tsx,
    src/components/debts/DebtCard.tsx,
    src/components/debts/DebtForm.tsx,
    src/components/debts/DebtList.tsx,
    src/components/debts/DebtSummaryCards.tsx,
    src/app/presupuesto/page.tsx,
    src/components/budgets/BudgetProgressList.tsx,
    src/components/budgets/BudgetSummaryRow.tsx,
    src/components/budgets/BudgetTable.tsx
  </files>
  <action>
    Read each Debts and Budget component file and compare against STYLE_GUIDE.md and UX_RULES.md specs. Check and fix ALL of the following:

    **Debts Page (QA-03):**

    *DebtCard:*
    - Card surface: `--color-surface-elevated`, NO borders, NO shadows, `--radius-lg` (16px)
    - Debt name in Heading style
    - All monetary amounts through MoneyAmount (IBM Plex Mono, muted $ prefix, direction colors)
    - Credit card utilization: BatteryBar with thresholds [31, 71] (green <31%, orange 31-70%, red 71%+)
    - Loan payoff progress: BatteryBar (higher = better, so all-accent segments)
    - Metrics (annual rate, monthly payment, etc.) in proper Label/Body hierarchy
    - Inline balance editing: tapping balance value transforms to FloatingInput with underline styling

    *DebtForm (modal):*
    - Desktop: centered modal, `--radius-xl` (24px), `--color-surface-elevated`, no border
    - Mobile: bottom sheet (85vh), drag handle, top-corner radius only
    - All inputs use FloatingInput (underline-only, floating labels)
    - Amount inputs: "$" prefix in `--color-text-tertiary`, IBM Plex Mono digits
    - Percentage inputs: "%" suffix in `--color-text-tertiary`
    - Save button: pill primary (chartreuse, black text, `--radius-full`)
    - Cancel button: pill secondary (transparent, `--color-text-secondary`, subtle border)

    *DebtSummaryCards:*
    - Borderless surface-elevated cards
    - Monospaced monetary values
    - Proper Label style for summary labels

    **Budget Page (QA-04):**

    *BudgetProgressList:*
    - Each category row shows: category icon (circular container, 36px, 12% opacity bg), category name, BatteryBar, percentage, amounts
    - BatteryBar thresholds: [80, 100] for budget (chartreuse <80%, orange 80-99%, red 100%+)
    - Percentage text adopts same color as BatteryBar segments
    - Overflow shows "+N%" in `--color-negative` at Meta level (11px)
    - Amounts: "spent / budget" format with MoneyAmount

    *BudgetTable (configuration):*
    - Table headers: Label style (12px, uppercase, +2px letter-spacing, `--color-text-secondary`)
    - Amount inputs use FloatingInput with "$" prefix (empty-label variant for compact table cells)
    - Row separators: 1px `--color-border-divider`
    - No alternating row backgrounds

    *BudgetSummaryRow:*
    - Total row with emphasis styling
    - MoneyAmount for total values

    **Cross-cutting checks for both pages:**
    - No smooth/continuous progress bars remaining anywhere -- ALL must be BatteryBar
    - No box-style inputs remaining -- ALL must be FloatingInput underline-only
    - No shadows or decorative borders on cards
    - Consistent padding: 20px standard cards, 24px generous cards

    For each discrepancy found: fix the code, note what was wrong and what was changed.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build && npm test</automated>
  </verify>
  <done>
    Debts page shows BatteryBar utilization on credit cards (green/orange/red thresholds), BatteryBar payoff on loans, inline editing with FloatingInput. Budget page shows BatteryBar per category with traffic-light colors, FloatingInput in configuration table. No smooth bars, no box inputs remain. Build and tests pass.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors and zero warnings
- `npm test` passes with zero failures
- Transactions page matches STYLE_GUIDE.md: list styling, bottom sheet with numpad, pixel-dissolve
- Debts page matches: BatteryBar utilization, inline editing, MoneyAmount everywhere
- Budget page matches: BatteryBar progress with traffic-light, FloatingInput in table
- No smooth progress bars remain anywhere
- No box-style inputs remain anywhere
</verification>

<success_criteria>
- Transaction list shows color-coded amounts with MoneyAmount
- Bottom sheet transaction form has all spec elements (hero, numpad, toggles, category grid)
- Debt cards show BatteryBar with correct threshold colors
- Budget progress shows BatteryBar with traffic-light (chartreuse/orange/red)
- All inputs are FloatingInput underline-only style
- All monetary amounts use MoneyAmount component
- Zero visual discrepancies remain
</success_criteria>

<output>
After completion, create `.planning/phases/22-visual-qa-accessibility/22-02-SUMMARY.md`
</output>
