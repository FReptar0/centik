---
phase: 24-touch-targets-table-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/debts/DebtCard.tsx
  - src/components/transactions/TransactionRow.tsx
  - src/components/layout/PeriodSelector.tsx
autonomous: true
requirements: [TOUCH-01, TOUCH-02, TOUCH-03]

must_haves:
  truths:
    - "DebtCard edit/delete action buttons have at least 44x44px tap area on mobile"
    - "TransactionRow edit/delete buttons have at least 44x44px tap area on mobile"
    - "PeriodSelector prev/next navigation arrows have at least 44x44px tap area on mobile"
  artifacts:
    - path: "src/components/debts/DebtCard.tsx"
      provides: "44px touch targets on edit/delete buttons"
      contains: "min-w-\\[44px\\] min-h-\\[44px\\]"
    - path: "src/components/transactions/TransactionRow.tsx"
      provides: "44px touch targets on edit/delete buttons"
      contains: "min-w-\\[44px\\] min-h-\\[44px\\]"
    - path: "src/components/layout/PeriodSelector.tsx"
      provides: "44px touch targets on prev/next buttons"
      contains: "min-w-\\[44px\\] min-h-\\[44px\\]"
  key_links:
    - from: "src/components/debts/DebtCard.tsx"
      to: "button elements"
      via: "Tailwind classes"
      pattern: "min-w-\\[44px\\] min-h-\\[44px\\]"
    - from: "src/components/transactions/TransactionRow.tsx"
      to: "button elements"
      via: "Tailwind classes"
      pattern: "min-w-\\[44px\\] min-h-\\[44px\\]"
    - from: "src/components/layout/PeriodSelector.tsx"
      to: "button elements"
      via: "Tailwind classes"
      pattern: "min-w-\\[44px\\] min-h-\\[44px\\]"
---

<objective>
Enforce 44px minimum touch targets on all action buttons across DebtCard, TransactionRow, and PeriodSelector.

Purpose: Mobile users currently have tiny 24-32px tap areas on critical action buttons (edit, delete, navigate). This causes frustrating mis-taps. WCAG 2.1 AAA recommends 44x44px minimum.
Output: Three updated components with properly sized touch targets.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/debts/DebtCard.tsx
@src/components/transactions/TransactionRow.tsx
@src/components/layout/PeriodSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce 44px touch targets on DebtCard and TransactionRow action buttons</name>
  <files>src/components/debts/DebtCard.tsx, src/components/transactions/TransactionRow.tsx</files>
  <action>
**DebtCard.tsx** (lines 215-228): The edit and delete buttons in the expanded action section currently use `rounded-full p-2` with a 16px icon, giving ~32px total touch area. Update both buttons to add `min-w-[44px] min-h-[44px] flex items-center justify-center` while keeping `rounded-full p-2`. The icon size (16) stays the same -- we are expanding the tap area, not the icon.

Current edit button class (line 217):
```
"rounded-full p-2 text-text-tertiary transition-all duration-200 hover:text-text-primary hover:bg-surface-hover active:scale-[0.98]"
```
Change to:
```
"rounded-full p-2 min-w-[44px] min-h-[44px] flex items-center justify-center text-text-tertiary transition-all duration-200 hover:text-text-primary hover:bg-surface-hover active:scale-[0.98]"
```

Current delete button class (line 223):
```
"rounded-full p-2 text-text-tertiary transition-all duration-200 hover:text-negative hover:bg-negative/10 active:scale-[0.98]"
```
Change to:
```
"rounded-full p-2 min-w-[44px] min-h-[44px] flex items-center justify-center text-text-tertiary transition-all duration-200 hover:text-negative hover:bg-negative/10 active:scale-[0.98]"
```

**TransactionRow.tsx** (lines 132-145): Same pattern. The edit and delete buttons use `rounded-full p-2` with 16px icons.

Current edit button class (line 134):
```
"rounded-full p-2 text-text-tertiary transition-all duration-200 hover:text-text-primary hover:bg-surface-hover active:scale-[0.98]"
```
Change to:
```
"rounded-full p-2 min-w-[44px] min-h-[44px] flex items-center justify-center text-text-tertiary transition-all duration-200 hover:text-text-primary hover:bg-surface-hover active:scale-[0.98]"
```

Current delete button class (line 140):
```
"rounded-full p-2 text-text-tertiary transition-all duration-200 hover:text-negative hover:bg-negative/10 active:scale-[0.98]"
```
Change to:
```
"rounded-full p-2 min-w-[44px] min-h-[44px] flex items-center justify-center text-text-tertiary transition-all duration-200 hover:text-negative hover:bg-negative/10 active:scale-[0.98]"
```
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && grep -c "min-w-\[44px\] min-h-\[44px\]" src/components/debts/DebtCard.tsx src/components/transactions/TransactionRow.tsx</automated>
  </verify>
  <done>Both DebtCard and TransactionRow edit/delete buttons have min-w-[44px] min-h-[44px] classes ensuring 44px minimum touch area. Icon sizes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce 44px touch targets on PeriodSelector navigation buttons</name>
  <files>src/components/layout/PeriodSelector.tsx</files>
  <action>
**PeriodSelector.tsx** (lines 52-63, 79-93): The prev and next navigation buttons currently use `p-1.5 rounded-md` with 16px chevron icons, giving ~28px total touch area.

Update the "previous" button (line 57) class from:
```
'p-1.5 rounded-md',
```
to:
```
'p-1.5 rounded-md min-w-[44px] min-h-[44px] flex items-center justify-center',
```

Update the "next" button (line 84) class from:
```
'p-1.5 rounded-md',
```
to:
```
'p-1.5 rounded-md min-w-[44px] min-h-[44px] flex items-center justify-center',
```

Keep all other classes identical (text colors, hover states, disabled state). The chevron icon size (16) stays the same.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && grep -c "min-w-\[44px\] min-h-\[44px\]" src/components/layout/PeriodSelector.tsx</automated>
  </verify>
  <done>PeriodSelector prev/next buttons have min-w-[44px] min-h-[44px] classes. Count should be 2 (one per button). Chevron size unchanged.</done>
</task>

</tasks>

<verification>
npm run build passes with zero errors
npm test passes (no logic changes, only CSS classes)
Grep confirms min-w-[44px] min-h-[44px] appears in all 3 files (2 in DebtCard, 2 in TransactionRow, 2 in PeriodSelector = 6 total instances)
</verification>

<success_criteria>
- All 6 action buttons across 3 components have 44px minimum touch targets
- No visual regressions (icons same size, layout unchanged except tap area)
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-touch-targets-table-optimization/24-01-SUMMARY.md`
</output>
