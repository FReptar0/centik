---
phase: 23-layout-grid-responsiveness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/debts/DebtList.tsx
  - src/components/debts/DebtCard.tsx
  - src/components/debts/DebtSummaryCards.tsx
  - src/app/page.tsx
  - src/app/presupuesto/PresupuestoClientWrapper.tsx
  - src/components/history/HistorialClientWrapper.tsx
autonomous: true
requirements: [BUG-01, BUG-02, RESP-01, RESP-02, RESP-03]

must_haves:
  truths:
    - "Expanding one DebtCard on desktop does not resize or shift the adjacent card in the grid"
    - "Dashboard, Budget, and History pages constrain content width on wide screens instead of stretching edge-to-edge"
    - "Debt cards display in a 2-column grid starting at md (768px) breakpoint, not only at lg (1024px)"
    - "DebtCard metric grids stack to single column on narrow mobile screens"
    - "DebtSummaryCards stack to single column on narrow mobile and show 3 columns at sm (640px)"
  artifacts:
    - path: "src/components/debts/DebtList.tsx"
      provides: "Grid with items-start alignment and md breakpoint"
      contains: "items-start"
    - path: "src/app/page.tsx"
      provides: "Dashboard max-width container"
      contains: "max-w-7xl"
    - path: "src/app/presupuesto/PresupuestoClientWrapper.tsx"
      provides: "Budget max-width container"
      contains: "max-w-7xl"
    - path: "src/components/history/HistorialClientWrapper.tsx"
      provides: "History max-width container"
      contains: "max-w-7xl"
  key_links:
    - from: "src/components/debts/DebtList.tsx"
      to: "src/components/debts/DebtCard.tsx"
      via: "CSS grid alignment prevents row stretching on expand"
      pattern: "items-start"
---

<objective>
Fix the DebtCard expansion bug, add max-width containers to Dashboard/Budget/History pages, and add missing responsive breakpoints to debt-related grids.

Purpose: Resolve 2 layout bugs and 3 grid responsiveness issues in debt-related components and page-level containers.
Output: 6 files with corrected Tailwind classes.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-layout-grid-responsiveness/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DebtCard expansion bug and DebtList grid breakpoints</name>
  <files>src/components/debts/DebtList.tsx, src/components/debts/DebtCard.tsx, src/components/debts/DebtSummaryCards.tsx</files>
  <action>
All changes are Tailwind class updates only. No logic changes.

**DebtList.tsx** (line 34):
- Current: `className="grid grid-cols-1 lg:grid-cols-2 gap-4 mt-6"`
- Change to: `className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6 items-start"`
- `items-start` fixes BUG-01 (expansion bug) by preventing CSS grid row stretching when one card expands
- `md:grid-cols-2` replaces `lg:grid-cols-2` to fix RESP-01 (show 2 columns at 768px)

**DebtCard.tsx** — CreditCardDetails metric grid (line 265):
- Current: `className="grid grid-cols-2 gap-3"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 gap-3"`
- Fixes RESP-02: metrics stack on narrow mobile, 2 columns at 640px+

**DebtCard.tsx** — LoanDetails metric grid (line 305):
- Current: `className="grid grid-cols-2 gap-3"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 gap-3"`
- Same RESP-02 fix for the loan variant

**DebtSummaryCards.tsx** (line 24):
- Current: `className="grid grid-cols-2 sm:grid-cols-3 gap-4 mb-8"`
- Change to: `className="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-8"`
- Fixes RESP-03: stack on very narrow screens, 3-column at 640px+
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>DebtList grid uses items-start and md breakpoint. DebtCard metric grids use sm breakpoint. DebtSummaryCards stack on mobile. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add max-width containers to Dashboard, Budget, and History pages</name>
  <files>src/app/page.tsx, src/app/presupuesto/PresupuestoClientWrapper.tsx, src/components/history/HistorialClientWrapper.tsx</files>
  <action>
All changes are Tailwind class updates only. No logic changes. Follow the existing pattern from DeudasClientWrapper (max-w-4xl) but use max-w-7xl for these wider pages.

**src/app/page.tsx** (line 40):
- Current: `<div className="space-y-8">`
- Change to: `<div className="max-w-7xl space-y-8">`
- Dashboard has charts and KPIs that benefit from wider max-width than Deudas

**src/app/presupuesto/PresupuestoClientWrapper.tsx** (line 59):
- Current: `<div>`
- Change to: `<div className="max-w-7xl">`
- Budget page has a 2-column layout (config + progress) that needs width

**PresupuestoClientWrapper.tsx** also fix the budget grid (line 78):
- Current: `className="grid gap-8 lg:grid-cols-2"`
- Change to: `className="grid gap-8 md:grid-cols-2"`
- This is part of RESP-04 (dashboard/budget grids missing md breakpoint) but touches this file

**src/components/history/HistorialClientWrapper.tsx** (line 115):
- Current: `<div>`
- Change to: `<div className="max-w-7xl">`
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/centik && npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard, Budget, and History pages have max-w-7xl container. Budget grid uses md breakpoint. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `npm test` passes (no test changes needed -- CSS-only modifications)
- Grep confirms changes: `grep -n "items-start" src/components/debts/DebtList.tsx` returns match
- Grep confirms changes: `grep -n "max-w-7xl" src/app/page.tsx src/app/presupuesto/PresupuestoClientWrapper.tsx src/components/history/HistorialClientWrapper.tsx` returns matches
</verification>

<success_criteria>
- DebtList grid has `items-start` and `md:grid-cols-2` (BUG-01 + RESP-01)
- DebtCard metric grids use `sm:grid-cols-2` instead of fixed `grid-cols-2` (RESP-02)
- DebtSummaryCards uses `grid-cols-1 sm:grid-cols-3` (RESP-03)
- Dashboard, Budget, History pages have `max-w-7xl` wrapper (BUG-02)
- Budget grid uses `md:grid-cols-2` instead of `lg:grid-cols-2` (partial RESP-04)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-layout-grid-responsiveness/23-01-SUMMARY.md`
</output>
