---
phase: 15-ux-interaction-patterns
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [UX_RULES.md]
autonomous: true
requirements: [UX-04, UX-05]

must_haves:
  truths:
    - "Section 6 responsive patterns reference new component specs: pill buttons, underline inputs, battery-bar progress, elevation-only cards, bottom sheet modals"
    - "Section 7 form patterns document underline-only inputs with floating labels, amount input with $ prefix in IBM Plex Mono, and circular category grid selector with accent ring"
    - "Full old-token sweep complete: zero instances of #22d3ee, #0a0f1a, #111827, #1e293b, cyan, DM Sans, --shadow-sm, --shadow-md, --shadow-lg, --shadow-glow anywhere in UX_RULES.md"
    - "All 11 sections maintained with consistent numbering; no section removed or restructured"
  artifacts:
    - path: "UX_RULES.md"
      provides: "Fully updated UX_RULES.md with Glyph Finance tokens in all 11 sections"
      contains: "--color-accent"
  key_links:
    - from: "UX_RULES.md section 6 (responsive)"
      to: "STYLE_GUIDE.md > Componentes Base (all)"
      via: "Cross-references to pill buttons, bottom sheet, battery-bar, elevation hierarchy"
      pattern: "pill|battery-bar|bottom sheet|--color-surface-elevated"
    - from: "UX_RULES.md section 7 (forms)"
      to: "STYLE_GUIDE.md > Componentes Base > Inputs"
      via: "Cross-reference to underline-only input spec with floating labels"
      pattern: "underline|floating label|Componentes Base.*Inputs"
---

<objective>
Update UX_RULES.md sections 6 (Responsive Design) and 7 (Formularios) with Glyph Finance component references, then sweep ALL remaining sections (1, 2, 4.2-4.5, 5, 8, 9, 11) to replace every old-token reference.

Purpose: Complete the UX_RULES.md migration so the entire document references only Glyph Finance tokens. After this plan, a developer reading UX_RULES.md gets consistent Glyph Finance guidance with zero contradictions to STYLE_GUIDE.md.

Output: UX_RULES.md fully migrated to Glyph Finance tokens across all 11 sections.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ux-interaction-patterns/15-CONTEXT.md
@.planning/phases/15-ux-interaction-patterns/15-01-SUMMARY.md
@STYLE_GUIDE.md
@UX_RULES.md

<interfaces>
<!-- Key specs from STYLE_GUIDE.md needed for responsive and form pattern updates -->

Component specs to cross-reference:
- Buttons: pill-shaped (radius-full 9999px), primary accent fill, secondary outline, toggle pills
- Inputs: underline-only, floating labels (12px uppercase +2px on focus), amount prefix "$" in --color-text-tertiary, IBM Plex Mono for digits
- Cards: borderless, --color-surface-elevated, no shadows
- Modales: desktop centered (max 480px), mobile bottom sheet (85vh, handle indicator)
- Progress Bars: battery-bar 10 segments, traffic-light colors
- Badges: pill-shaped (radius-full), 6 semantic variants

Elevation model:
- Level 0: --color-bg (#000000)
- Level 1: --color-surface (#0A0A0A)
- Level 2: --color-surface-elevated (#141414)
- Hover: --color-surface-hover (#1A1A1A)
- NO shadows, NO decorative borders

Typography:
- Satoshi for headings/body, IBM Plex Mono for all financial numbers
- Label: 12px uppercase, letter-spacing +2px, --color-text-secondary
- Focus ring: 2px solid --color-accent outline (no glow)

Old tokens to find-and-replace during sweep:
- #22d3ee → --color-accent (#CCFF00)
- #0a0f1a → --color-bg (#000000)
- #111827 → --color-surface-elevated (#141414)
- #1e293b → --color-border-divider (#222222)
- cyan → chartreuse / --color-accent
- DM Sans → Satoshi
- --shadow-glow → "2px solid --color-accent outline"
- --shadow-sm/md/lg → eliminated (elevation via background-shift)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite section 6 (responsive) and section 7 (forms) with Glyph Finance patterns</name>
  <files>UX_RULES.md</files>
  <action>
Rewrite two sections. Keep section numbers, Spanish language, subsection structure.

**Section 6 (Responsive Design):**

6.1 Breakpoints: Keep the three breakpoints (Mobile <768px, Tablet 768-1023px, Desktop >=1024px). Update: Mobile uses bottom tab bar (icon-only, per section 3.2 updated in Plan 01).

6.2 Adaptaciones por Breakpoint: Update each subsection to reference Glyph Finance component specs:

- **Dashboard KPIs:** Keep grid columns. Add: KPI cards use `--color-surface-elevated` con padding 24px, montos en IBM Plex Mono. Referencia a Componentes Base > Cards.
- **Graficas:** Keep stacking rules. Add: graficas sin grid lines, stroke 1.5px, puntos de 4px en endpoints. Referencia a Componentes Base > Charts.
- **Tablas:** Keep table-to-card conversion on mobile. Update: cards sin bordes, fondo `--color-surface-elevated`. Desktop: headers en nivel Label (12px, uppercase, letter-spacing +2px). Referencia a Componentes Base > Tablas.
- **Formularios (modales):** Update completely:
  - Mobile: bottom sheet (85vh) con drag handle, desliza desde abajo. Referencia a Componentes Base > Modales > Mobile.
  - Desktop: modal centrado, max-width 480px, fondo `--color-surface-elevated`, border-radius `--radius-xl` (24px). Referencia a Componentes Base > Modales > Desktop.
  - All inputs: underline-only con floating labels. Referencia a Componentes Base > Inputs.
- **Sidebar:** Keep the mobile overlay / desktop fixed behavior. No specific token changes needed beyond what Plan 01 already updated in section 3.

6.3 Touch Targets: Keep 44x44px rule. Add: botones pill usan min-height 48px (tamano lg) en mobile para mejor area de tap. Referencia a Componentes Base > Buttons > Tamanos.

**Section 7 (Formularios):**

7.1 Estructura: Rewrite to reflect underline-only input pattern:
- Label: estilo floating label -- comienza como placeholder en `--color-text-tertiary`, al focus/fill flota a nivel Label (12px, uppercase, letter-spacing +2px, `--color-text-secondary`). Referencia a STYLE_GUIDE.md > Componentes Base > Inputs > Floating labels.
- Gap entre campos: 14px (keep).
- Campos opcionales: marcados con "(opcional)" en el label (keep).
- Agrupacion: separadores de 1px `--color-border-divider` (update token name).

7.2 Validacion:
- Keep blur validation rules.
- Error state: underline inferior cambia a `--color-negative`. Mensaje de error debajo en nivel Meta (11px, `--color-negative`). Referencia a Componentes Base > Inputs > Estado error.
- Remove old "borde del input cambia a color-negative" phrasing -- underline inputs only have bottom border.

7.3 Inputs de Monto:
- `inputMode="decimal"` (keep).
- Prefijo "$" visual en `--color-text-tertiary` a tamano menor, posicionado al inicio de la linea underline. Estatico, no flota.
- Digitos en IBM Plex Mono (`--font-mono`), alineados a la derecha.
- Formatear con comas al blur (keep).
- Max 2 decimales (keep).
- Referencia a Componentes Base > Inputs > Inputs de montos.

7.4 Selects y Categorias:
- Listas cortas (< 6 opciones): radio group visual con botones pill (referencia a Componentes Base > Buttons > Toggle pills) o grid de botones, no dropdown.
- Categorias de gasto: grid circular de iconos (4 columnas). Cada icono en contenedor circular de 40px con fondo de color de categoria al 12%. Seleccion: ring de 2px `--color-accent` alrededor del circulo. Label debajo en nivel Meta. This is the same pattern as the transaction flow category selector.
- Referencia a Iconografia > Contenedores de Icono.
- Listas largas: dropdown con busqueda (keep).

Replace ALL old hex values or old references in sections 6 and 7.
  </action>
  <verify>
    <automated>grep -n "underline\|floating label\|pill\|battery-bar\|bottom sheet\|IBM Plex Mono\|--color-surface-elevated" UX_RULES.md | grep -E "^(1[5-9][0-9]|2[0-9]{2}|[6-7])" | head -15; echo "---"; grep -n "#22d3ee\|#0a0f1a\|#111827\|DM Sans\|--shadow-" UX_RULES.md | head -10</automated>
  </verify>
  <done>Section 6 references Glyph Finance component specs for all responsive adaptations. Section 7 documents underline-only inputs with floating labels, monospaced amount inputs, and circular category grid selector. Zero old hex references in sections 6 and 7.</done>
</task>

<task type="auto">
  <name>Task 2: Full old-token sweep across all remaining UX_RULES.md sections</name>
  <files>UX_RULES.md</files>
  <action>
Sweep EVERY section of UX_RULES.md not already rewritten (sections 1, 2, 4.2-4.5, 5, 8, 9, 11) and replace any remaining old-token references. Also verify sections 3, 4.1, 6, 7, 10 (already rewritten) are clean.

**Find and replace targets (document-wide):**
- `#22d3ee` → `--color-accent` (or remove hex, keep variable name)
- `#0a0f1a` → `--color-bg`
- `#111827` → `--color-surface-elevated`
- `#1e293b` → `--color-border-divider`
- `cyan` (when referring to accent color) → `chartreuse` or `--color-accent`
- `DM Sans` → `Satoshi`
- `--shadow-glow` → replace with "outline de 2px `--color-accent`" (focus ring)
- `--shadow-sm`, `--shadow-md`, `--shadow-lg` → remove or replace with elevation note
- Any mention of "bordes decorativos" on cards → update to borderless/elevation-only
- `text-2xl, bold` in section 3.3 → "nivel Heading (20px, Satoshi Semibold)" for consistency with STYLE_GUIDE.md type hierarchy

**Section-specific updates during sweep:**

Section 5.1 (Empty States): Update "32px" icon to "24px" (matching STYLE_GUIDE.md icon size for empty states). Update "text-muted" to `--color-text-tertiary`. CTA uses pill button (referencia a Componentes Base > Buttons).

Section 5.2 (Loading States): Update "grises pulsando" to reference `--color-surface-elevated` as skeleton base color. Remove any shadow references.

Section 5.3 (Error States): Update "color-negative" references -- already correct token names, verify no old hex. Update "borde del input" to "linea inferior del input" (underline-only).

Section 8.3 (Transiciones y Animaciones):
- Update modal animation: keep "fade in overlay + scale desde 0.95 (200ms)" for desktop.
- Add: Mobile modals use slide-up animation (referencia a Componentes Base > Modales > Mobile).
- Add: Datos nuevos pueden usar efecto pixel-dissolve (referencia a Identidad Visual > Micro-Animacion Pixel-Dissolve) where applicable.

Section 9.1 (Accesibilidad): Update focus ring: "outline de 2px `--color-accent`" (remove any `--shadow-glow` reference). Keep WCAG AA requirement.

**Final validation pass:**
After all replacements, read through the entire file and confirm:
1. Zero instances of: #22d3ee, #0a0f1a, #111827, #1e293b, cyan (as color), DM Sans, --shadow-glow, --shadow-sm, --shadow-md, --shadow-lg
2. All 11 sections present with original numbering
3. Spanish language consistent throughout
4. Cross-references to STYLE_GUIDE.md use the pattern "STYLE_GUIDE.md > [Section] > [Subsection]" or "Componentes Base > [Component]"
  </action>
  <verify>
    <automated>echo "=== Old token check (expect 0 matches) ==="; grep -c "#22d3ee\|#0a0f1a\|#111827\|#1e293b\|--shadow-glow\|--shadow-sm\|--shadow-md\|--shadow-lg" UX_RULES.md; echo "=== DM Sans check ==="; grep -ci "DM Sans" UX_RULES.md; echo "=== Cyan check ==="; grep -ni "cyan" UX_RULES.md; echo "=== Section count (expect 11) ==="; grep -c "^## [0-9]" UX_RULES.md; echo "=== Glyph Finance token usage ==="; grep -c "color-accent\|color-positive\|color-negative\|color-text-secondary\|color-surface-elevated\|font-mono\|IBM Plex Mono\|Satoshi" UX_RULES.md</automated>
  </verify>
  <done>UX_RULES.md has zero old-token references across all 11 sections. Every color, font, shadow, and component reference uses Glyph Finance tokens. All 11 sections maintained with original numbering. Document is fully consistent with STYLE_GUIDE.md.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "#22d3ee\|#0a0f1a\|#111827\|#1e293b\|--shadow-glow\|--shadow-sm\|--shadow-md\|--shadow-lg" UX_RULES.md` returns zero matches
2. `grep -i "cyan\|DM Sans" UX_RULES.md` returns zero matches
3. `grep "^## [0-9]" UX_RULES.md` returns exactly 11 section headers
4. `grep "STYLE_GUIDE.md\|Componentes Base\|Identidad Visual" UX_RULES.md` returns multiple cross-references
5. `grep "underline\|floating label\|pill\|battery-bar\|IBM Plex Mono" UX_RULES.md` returns matches in sections 6 and 7
</verification>

<success_criteria>
- Section 6 responsive patterns reference pill buttons, underline inputs, battery-bar, elevation-only cards, and bottom sheet modals
- Section 7 form patterns document underline-only inputs, floating labels, monospaced amount inputs, and circular category grid
- Full old-token sweep complete: zero legacy references in entire document
- All 11 sections maintained; no restructuring
- Cross-references to STYLE_GUIDE.md component and identity specs throughout
</success_criteria>

<output>
After completion, create `.planning/phases/15-ux-interaction-patterns/15-02-SUMMARY.md`
</output>
